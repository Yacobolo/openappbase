### Fetch PostgreSQL Driver

Source: https://docs.sqlc.dev/en/latest/_sources/tutorials/getting-started-postgresql

Fetches the pgx/v5 PostgreSQL driver, which is used by the Go application for database connectivity.

```shell
go get github.com/jackc/pgx/v5
go build ./...

```

---

### Fetch MySQL driver and build Go project

Source: https://docs.sqlc.dev/en/latest/_sources/tutorials/getting-started-mysql

Commands to download the MySQL driver for Go and compile the Go project. These are necessary steps before running the application.

```shell
go get github.com/go-sql-driver/mysql
go build ./...
```

---

### Fetch SQLite Driver and Build

Source: https://docs.sqlc.dev/en/latest/tutorials/getting-started-sqlite

Fetches the necessary SQLite driver for Go and then builds the Go project. This ensures all dependencies are met and the program is compiled.

```bash
go get modernc.org/sqlite
go build ./...
```

---

### Go Application Using Generated Code

Source: https://docs.sqlc.dev/en/latest/_sources/tutorials/getting-started-postgresql

A Go program that utilizes the code generated by sqlc to interact with a PostgreSQL database. It demonstrates listing, creating, and fetching authors.

```go
package main

import (
	"context"
	"log"
	"reflect"

	"github.com/jackc/pgx/v5"
	"github.com/jackc/pgx/v5/pgtype"

	"tutorial.sqlc.dev/app/tutorial"
)

func run() error {
ctx := context.Background()

	conn, err := pgx.Connect(ctx, "user=pqgotest dbname=pqgotest sslmode=verify-full")
	if err != nil {
		return err
	}
	defer conn.Close(ctx)

	queries := tutorial.New(conn)

	// list all authors
	authors, err := queries.ListAuthors(ctx)
	if err != nil {
		return err
	}
	log.Println(authors)

	// create an author
	insertedAuthor, err := queries.CreateAuthor(ctx, tutorial.CreateAuthorParams{
		Name: "Brian Kernighan",
		Bio:  pgtype.Text{String: "Co-author of The C Programming Language and The Go Programming Language", Valid: true},
	})
	if err != nil {
		return err
	}
	log.Println(insertedAuthor)

	// get the author we just inserted
	fetchedAuthor, err := queries.GetAuthor(ctx, insertedAuthor.ID)
	if err != nil {
		return err
	}

	// prints true
	log.Println(reflect.DeepEqual(insertedAuthor, fetchedAuthor))
	return nil
}

func main() {
	if err := run(); err != nil {
		log.Fatal(err)
	}
}

```

---

### Verify Queries Against Schema

Source: https://docs.sqlc.dev/en/latest/_sources/tutorials/getting-started-postgresql

This command verifies your previously published queries against a specified target, such as a database schema version represented by `tutorial`. It ensures that your queries remain compatible with schema changes.

```shell
$ sqlc verify --against tutorial
```

---

### sqlc.yaml configuration for MySQL

Source: https://docs.sqlc.dev/en/latest/_sources/tutorials/getting-started-mysql

Configuration file for sqlc to specify the database engine (MySQL), query files, schema files, and Go code generation settings.

```yaml
version: "2"
sql:
  - engine: "mysql"
    queries: "query.sql"
    schema: "schema.sql"
    gen:
      go:
        package: "tutorial"
        out: "tutorial"
```

---

### Configure sqlc for PostgreSQL

Source: https://docs.sqlc.dev/en/latest/_sources/tutorials/getting-started-postgresql

Defines the sqlc configuration using YAML, specifying the database engine (PostgreSQL), query and schema files, and Go code generation settings.

```yaml
version: "2"
sql:
  - engine: "postgresql"
    queries: "query.sql"
    schema: "schema.sql"
    gen:
      go:
        package: "tutorial"
        out: "tutorial"
        sql_package: "pgx/v5"
```

---

### Fetch SQLite Driver and Build Go Program

Source: https://docs.sqlc.dev/en/latest/_sources/tutorials/getting-started-sqlite

Fetches the necessary SQLite driver for Go and builds the Go program. These commands ensure that the Go application has the required dependencies and can be compiled.

```shell
go get modernc.org/sqlite
go build ./...

```

---

### Initialize Go Module

Source: https://docs.sqlc.dev/en/latest/_sources/tutorials/getting-started-sqlite

Initializes a new Go module for the project. This command is essential for managing dependencies in a Go project.

```shell
go mod init tutorial.sqlc.dev/app
```

---

### Go Program Using Generated Code

Source: https://docs.sqlc.dev/en/latest/tutorials/getting-started-postgresql

Demonstrates how to use the Go code generated by sqlc to interact with a PostgreSQL database. It includes connecting to the database, creating an author, listing authors, and fetching a specific author.

```go
package main

import (
	"context"
	"log"
	"reflect"

	"github.com/jackc/pgx/v5"
	"github.com/jackc/pgx/v5/pgtype"

	"tutorial.sqlc.dev/app/tutorial"
)

func run() error {
	ctx := context.Background()

	conn, err := pgx.Connect(ctx, "user=pqgotest dbname=pqgotest sslmode=verify-full")
	if err != nil {
		return err
	}
	defer conn.Close(ctx)

	queries := tutorial.New(conn)

	// list all authors
	authors, err := queries.ListAuthors(ctx)
	if err != nil {
		return err
	}
	log.Println(authors)

	// create an author
	insertedAuthor, err := queries.CreateAuthor(ctx, tutorial.CreateAuthorParams{
		Name: "Brian Kernighan",
		Bio:  pgtype.Text{String: "Co-author of The C Programming Language and The Go Programming Language", Valid: true},
	})
	if err != nil {
		return err
	}
	log.Println(insertedAuthor)

	// get the author we just inserted
	fetchedAuthor, err := queries.GetAuthor(ctx, insertedAuthor.ID)
	if err != nil {
		return err
	}

	// prints true
	log.Println(reflect.DeepEqual(insertedAuthor, fetchedAuthor))
	return nil
}

func main() {
	if err := run(); err != nil {
		log.Fatal(err)
	}
}

```

---

### Define SQLite Queries

Source: https://docs.sqlc.dev/en/latest/tutorials/getting-started-sqlite

Specifies a set of SQL queries for interacting with the 'authors' table, including functions to get, list, create, update, and delete authors.

```sql
-- name: GetAuthor :one
SELECT * FROM authors
WHERE id = ? LIMIT 1;

-- name: ListAuthors :many
SELECT * FROM authors
ORDER BY name;

-- name: CreateAuthor :one
INSERT INTO authors (
  name, bio
) VALUES (
  ?, ?
)
RETURNING *;

-- name: UpdateAuthor :exec
UPDATE authors
set name = ?,
bio = ?
WHERE id = ?;

-- name: DeleteAuthor :exec
DELETE FROM authors
WHERE id = ?;
```

---

### Configure sqlc with YAML

Source: https://docs.sqlc.dev/en/latest/tutorials/getting-started-sqlite

Defines the configuration for sqlc, specifying the database engine (SQLite), query and schema file paths, and Go code generation settings.

```yaml
version: "2"sql:
  - engine: "sqlite"
    queries: "query.sql"
    schema: "schema.sql"
    gen:
      go:
        package: "tutorial"
        out: "tutorial"
```

---

### Push Code with Tag using Sqlc CLI

Source: https://docs.sqlc.dev/en/latest/_sources/tutorials/getting-started-mysql

This command shows how to use the sqlc CLI to push your code with a specific tag, likely for versioning or deployment purposes related to sqlc Cloud.

```shell
$ sqlc push --tag tutorial
```

---

### Set SQLC_AUTH_TOKEN Environment Variable

Source: https://docs.sqlc.dev/en/latest/tutorials/getting-started-postgresql

This command sets the `SQLC_AUTH_TOKEN` environment variable, which is required for authenticating with sqlc Cloud. Ensure you replace `<your sqlc auth token>` with your actual token.

```bash
export SQLC_AUTH_TOKEN="<your sqlc auth token>"

```

---

### Go program using generated sqlc code

Source: https://docs.sqlc.dev/en/latest/_sources/tutorials/getting-started-mysql

A Go program demonstrating how to use the generated Go code from sqlc to interact with a MySQL database. Includes database connection, query execution, and error handling.

```go
package main

import (
	"context"
	"database/sql"
	"log"
	"reflect"

	_ "github.com/go-sql-driver/mysql"

	"tutorial.sqlc.dev/app/tutorial"
)

func run() error {
	ctx := context.Background()

	db, err := sql.Open("mysql", "user:password@/dbname?parseTime=true")
	if err != nil {
		return err
	}

	queries := tutorial.New(db)

	// list all authors
	authors, err := queries.ListAuthors(ctx)
	if err != nil {
		return err
	}
	log.Println(authors)

	// create an author
	result, err := queries.CreateAuthor(ctx, tutorial.CreateAuthorParams{
		Name: "Brian Kernighan",
		Bio:  sql.NullString{String: "Co-author of The C Programming Language and The Go Programming Language", Valid: true},
	})
	if err != nil {
		return err
	}

	insertedAuthorID, err := result.LastInsertId()
	if err != nil {
		return err
	}
	log.Println(insertedAuthorID)

	// get the author we just inserted
	fetchedAuthor, err := queries.GetAuthor(ctx, insertedAuthorID)
	if err != nil {
		return err
	}

	// prints true
	log.Println(reflect.DeepEqual(insertedAuthorID, fetchedAuthor.ID))
	return nil
}

func main() {
	if err := run(); err != nil {
		log.Fatal(err)
	}
}
```

---

### Push Queries to sqlc Cloud

Source: https://docs.sqlc.dev/en/latest/tutorials/getting-started-postgresql

The `sqlc push` command publishes your queries to sqlc Cloud, making them available for verification against your database schema. The `--tag` flag allows you to specify a version or identifier for your queries.

```bash
$ sqlc push --tag tutorial

```

---

### Define PostgreSQL Queries

Source: https://docs.sqlc.dev/en/latest/_sources/tutorials/getting-started-postgresql

Specifies various SQL queries for interacting with the 'authors' table in PostgreSQL, including selecting, inserting, updating, and deleting authors.

```sql
-- name: GetAuthor :one
SELECT * FROM authors
WHERE id = $1 LIMIT 1;

-- name: ListAuthors :many
SELECT * FROM authors
ORDER BY name;

-- name: CreateAuthor :one
INSERT INTO authors (
  name,
  bio
)
VALUES (
  $1, $2
)
RETURNING *;

-- name: UpdateAuthor :exec
UPDATE authors
  set name = $2,
  bio = $3
WHERE id = $1;

-- name: DeleteAuthor :exec
DELETE FROM authors
WHERE id = $1;

```

---

### Define PostgreSQL Schema

Source: https://docs.sqlc.dev/en/latest/_sources/tutorials/getting-started-postgresql

Defines the database schema for a PostgreSQL table named 'authors'. It includes columns for ID, name, and bio.

```sql
CREATE TABLE authors (
  id   BIGSERIAL PRIMARY KEY,
  name text      NOT NULL,
  bio  text
);

```

---

### Use Generated Go Code with SQLite

Source: https://docs.sqlc.dev/en/latest/_sources/tutorials/getting-started-sqlite

A Go program demonstrating the usage of sqlc-generated code to interact with an in-memory SQLite database. It includes opening the database, creating tables, creating an author, fetching an author, and verifying the data.

```go
package main

import (
	"context"
	"database/sql"
	_ "embed"
	"log"
	"reflect"

	_ "modernc.org/sqlite"

	"tutorial.sqlc.dev/app/tutorial"
)

//go:embed schema.sql
var ddl string

func run() error {
	ctx := context.Background()

	db, err := sql.Open("sqlite", ":memory:")
	if err != nil {
		return err
	}

	// create tables
	if _, err := db.ExecContext(ctx, ddl); err != nil {
		return err
	}

	queries := tutorial.New(db)

	// list all authors
	authors, err := queries.ListAuthors(ctx)
	if err != nil {
		return err
	}
	log.Println(authors)

	// create an author
	insertedAuthor, err := queries.CreateAuthor(ctx, tutorial.CreateAuthorParams{
		Name: "Brian Kernighan",
		Bio:  sql.NullString{String: "Co-author of The C Programming Language and The Go Programming Language", Valid: true},
	})
	if err != nil {
		return err
	}
	log.Println(insertedAuthor)

	// get the author we just inserted
	fetchedAuthor, err := queries.GetAuthor(ctx, insertedAuthor.ID)
	if err != nil {
		return err
	}

	// prints true
	log.Println(reflect.DeepEqual(insertedAuthor, fetchedAuthor))
	return nil
}

func main() {
	if err := run(); err != nil {
		log.Fatal(err)
	}
}

```

---

### Configure sqlc for SQLite

Source: https://docs.sqlc.dev/en/latest/_sources/tutorials/getting-started-sqlite

Defines the sqlc configuration for a SQLite database. It specifies the database engine, query and schema file locations, and the output package and directory for generated Go code.

```yaml
version: "2"
sql:
  - engine: "sqlite"
    queries: "query.sql"
    schema: "schema.sql"
    gen:
      go:
        package: "tutorial"
        out: "tutorial"
```

---

### Define SQLite Schema

Source: https://docs.sqlc.dev/en/latest/tutorials/getting-started-sqlite

Defines the database schema for SQLite, creating an 'authors' table with 'id', 'name', and 'bio' columns.

```sql
CREATE TABLE authors (
  id   INTEGER PRIMARY KEY,
  name text    NOT NULL,
  bio  text
);
```

---

### Set SQLC_AUTH_TOKEN Environment Variable

Source: https://docs.sqlc.dev/en/latest/_sources/tutorials/getting-started-mysql

This command demonstrates how to set the SQLC_AUTH_TOKEN environment variable, which is required for authentication with sqlc Cloud. Ensure you replace `<your sqlc auth token>` with your actual token.

```shell
export SQLC_AUTH_TOKEN="<your sqlc auth token>"
```

---

### Go Application Using Generated Code

Source: https://docs.sqlc.dev/en/latest/tutorials/getting-started-mysql

A Go program that utilizes the code generated by sqlc to interact with a MySQL database. It demonstrates connecting to the database, listing authors, creating an author, and fetching the created author.

```go
package main

import (
	"context"
	"database/sql"
	"log"
	"reflect"

	_ "github.com/go-sql-driver/mysql"

	"tutorial.sqlc.dev/app/tutorial"
)

func run() error {
 ctx := context.Background()

 db, err := sql.Open("mysql", "user:password@/dbname?parseTime=true")
	if err != nil {
		return err
	}

 queries := tutorial.New(db)

	// list all authors
	authors, err := queries.ListAuthors(ctx)
	if err != nil {
		return err
	}
	log.Println(authors)

	// create an author
	result, err := queries.CreateAuthor(ctx, tutorial.CreateAuthorParams{
		Name: "Brian Kernighan",
		Bio:  sql.NullString{String: "Co-author of The C Programming Language and The Go Programming Language", Valid: true},
	})
	if err != nil {
		return err
	}

 insertedAuthorID, err := result.LastInsertId()
	if err != nil {
		return err
	}
	log.Println(insertedAuthorID)

	// get the author we just inserted
	fetchedAuthor, err := queries.GetAuthor(ctx, insertedAuthorID)
	if err != nil {
		return err
	}

	// prints true
	log.Println(reflect.DeepEqual(insertedAuthorID, fetchedAuthor.ID))
	return nil
}

func main() {
	if err := run(); err != nil {
		log.Fatal(err)
	}
}
```

---

### SQL Queries for Author Operations

Source: https://docs.sqlc.dev/en/latest/tutorials/getting-started-mysql

Defines four SQL queries for interacting with the 'authors' table: GetAuthor, ListAuthors, CreateAuthor, and DeleteAuthor. These queries are used by sqlc to generate Go functions.

```sql
-- name: GetAuthor :one
SELECT * FROM authors
WHERE id = ? LIMIT 1;

-- name: ListAuthors :many
SELECT * FROM authors
ORDER BY name;

-- name: CreateAuthor :execresult
INSERT INTO authors (
  name,
  bio
) VALUES (
  ?, ?
);

-- name: DeleteAuthor :exec
DELETE FROM authors
WHERE id = ?;
```

---

### Start Databases with Docker Compose

Source: https://docs.sqlc.dev/en/latest/_sources/guides/development

Starts database services in detached mode using Docker Compose. Requires Docker and Docker Compose to be installed.

```bash
docker compose up -d
```

---

### MySQL Queries for Go Code Generation

Source: https://docs.sqlc.dev/en/latest/_sources/tutorials/getting-started-mysql

SQL queries used by sqlc to generate Go functions for database operations. Supports fetching, listing, creating, and deleting authors.

```sql
-- name: GetAuthor :one
SELECT * FROM authors
WHERE id = ? LIMIT 1;

-- name: ListAuthors :many
SELECT * FROM authors
ORDER BY name;

-- name: CreateAuthor :execresult
INSERT INTO authors (
  name,
  bio
)
VALUES (
  ?, ?
);

-- name: DeleteAuthor :exec
DELETE FROM authors
WHERE id = ?;
```

---

### Configure sqlc.yaml for Cloud Verification

Source: https://docs.sqlc.dev/en/latest/tutorials/getting-started-postgresql

This configuration snippet shows how to add your sqlc Cloud project ID to the `cloud` block in your `sqlc.yaml` file. This enables sqlc Cloud's enhanced verification features.

```yaml
version: "2"
cloud:
  # Replace <PROJECT_ID> with your project ID from the sqlc Cloud dashboard
  project: "<PROJECT_ID>"
sql:
  - engine: "postgresql"
    queries: "query.sql"
    schema: "schema.sql"
    gen:
      go:
        package: "tutorial"
        out: "tutorial"
        sql_package: "pgx/v5"
```

---

### MySQL Schema Definition

Source: https://docs.sqlc.dev/en/latest/_sources/tutorials/getting-started-mysql

Defines the 'authors' table schema for the MySQL database. It includes columns for ID, name, and bio.

```sql
CREATE TABLE authors (
  id   BIGINT  NOT NULL AUTO_INCREMENT PRIMARY KEY,
  name text    NOT NULL,
  bio  text
);
```

---

### Configure sqlc.yaml for Cloud Integration

Source: https://docs.sqlc.dev/en/latest/_sources/tutorials/getting-started-postgresql

This snippet shows how to add the `cloud` block to your `sqlc.yaml` file, specifying your project ID obtained from the sqlc Cloud dashboard. This enables sqlc Cloud's verification features.

```yaml
version: "2"
cloud:
  # Replace <PROJECT_ID> with your project ID from the sqlc Cloud dashboard
  project: "<PROJECT_ID>"
sql:
  - engine: "postgresql"
    queries: "query.sql"
    schema: "schema.sql"
    gen:
      go:
        package: "tutorial"
        out: "tutorial"
        sql_package: "pgx/v5"
```

---

### Verify Queries Against Database Schema

Source: https://docs.sqlc.dev/en/latest/tutorials/getting-started-postgresql

The `sqlc verify` command checks your published queries against the current database schema to ensure compatibility. The `--against` flag specifies the target database or schema to verify against.

```bash
$ sqlc verify --against tutorial

```

---

### Configure sqlc.yaml for Cloud Verification

Source: https://docs.sqlc.dev/en/latest/tutorials/getting-started-mysql

This snippet shows how to configure the `sqlc.yaml` file to enable sqlc Cloud's query verification. It requires adding your project ID to the `cloud.project` field. Ensure you replace `<PROJECT_ID>` with your actual project ID.

```yaml
version: "2"
cloud:
  # Replace <PROJECT_ID> with your project ID from the sqlc Cloud dashboard
  project: "<PROJECT_ID>"
sql:
  - engine: "mysql"
    queries: "query.sql"
    schema: "schema.sql"
    gen:
      go:
        package: "tutorial"
        out: "tutorial"
```

---

### Generate Go Code with sqlc

Source: https://docs.sqlc.dev/en/latest/_sources/tutorials/getting-started-sqlite

Executes the sqlc generate command to produce Go source code based on the defined schema and queries. This command should run without output if successful.

```shell
sqlc generate

```

---

### Configure sqlc.yaml for Cloud Integration

Source: https://docs.sqlc.dev/en/latest/_sources/tutorials/getting-started-mysql

This snippet shows how to add the `cloud` block to your sqlc.yaml file, including your project ID obtained from the sqlc Cloud dashboard. This enables sqlc Cloud's verification features.

```yaml
version: "2"
cloud:
  # Replace <PROJECT_ID> with your project ID from the sqlc Cloud dashboard
  project: "<PROJECT_ID>"
sql:
  - engine: "mysql"
    queries: "query.sql"
    schema: "schema.sql"
    gen:
      go:
        package: "tutorial"
        out: "tutorial"
```

---

### Update PostgreSQL Query with RETURNING

Source: https://docs.sqlc.dev/en/latest/_sources/tutorials/getting-started-postgresql

An alternative definition for the 'UpdateAuthor' query that includes a RETURNING clause to return the updated record.

```sql
-- name: UpdateAuthor :one
UPDATE authors
  set name = $2,
  bio = $3
WHERE id = $1
RETURNING *;

```

---

### Go Program Using Generated Code

Source: https://docs.sqlc.dev/en/latest/tutorials/getting-started-sqlite

A Go program that utilizes the sqlc-generated code to interact with an in-memory SQLite database. It demonstrates creating tables, inserting, fetching, and comparing author records.

```go
package main

import (
	"context"
	"database/sql"
	_ "embed"
	"log"
	"reflect"

	_ "modernc.org/sqlite"

	"tutorial.sqlc.dev/app/tutorial"
)

//go:embed schema.sql
var ddl string

func run() error {
 ctx := context.Background()

	db, err := sql.Open("sqlite", ":memory:")
	if err != nil {
		return err
	}

	// create tables
	if _, err := db.ExecContext(ctx, ddl); err != nil {
		return err
	}

	queries := tutorial.New(db)

	// list all authors
	authors, err := queries.ListAuthors(ctx)
	if err != nil {
		return err
	}
	log.Println(authors)

	// create an author
	insertedAuthor, err := queries.CreateAuthor(ctx, tutorial.CreateAuthorParams{
		Name: "Brian Kernighan",
		Bio:  sql.NullString{String: "Co-author of The C Programming Language and The Go Programming Language", Valid: true},
	})
	if err != nil {
		return err
	}
	log.Println(insertedAuthor)

	// get the author we just inserted
	fetchedAuthor, err := queries.GetAuthor(ctx, insertedAuthor.ID)
	if err != nil {
		return err
	}

	// prints true
	log.Println(reflect.DeepEqual(insertedAuthor, fetchedAuthor))
	return nil
}

func main() {
	if err := run(); err != nil {
		log.Fatal(err)
	}
}
```

---

### Golang-migrate Migration File Example

Source: https://docs.sqlc.dev/en/latest/_sources/howto/ddl

An SQL file example for golang-migrate migrations, showing the 'up' SQL to create a 'post' table. Files should be zero-padded for correct lexicographic ordering by sqlc.

```sql
CREATE TABLE post (
    id    int NOT NULL,
    title text,
    body  text,
    PRIMARY KEY(id)
);
```

---

### Run Go Tests with Examples Tag

Source: https://docs.sqlc.dev/en/latest/_sources/guides/development

Executes Go tests specifically tagged with 'examples'. This is useful for testing example implementations.

```bash
go test --tags=examples ./...
```

---

### Update Author Query with Return

Source: https://docs.sqlc.dev/en/latest/tutorials/getting-started-sqlite

An alternative definition for the 'UpdateAuthor' query that returns the updated record after the modification.

```sql
-- name: UpdateAuthor :one
UPDATE authors
set name = ?,
bio = ?
WHERE id = ?
RETURNING *;
```

---

### Define SQLite Schema

Source: https://docs.sqlc.dev/en/latest/_sources/tutorials/getting-started-sqlite

Creates an 'authors' table in the SQLite database. The table includes an integer primary key 'id', a non-null 'name', and an optional 'bio' field.

```sql
CREATE TABLE authors (
  id   INTEGER PRIMARY KEY,
  name text    NOT NULL,
  bio  text
);

```

---

### SQLC Rule Examples for EXPLAIN Output

Source: https://docs.sqlc.dev/en/latest/_sources/howto/vet

Illustrates how to define custom rules in sqlc's configuration file that analyze EXPLAIN output. It includes examples for PostgreSQL to check total cost and identify sequential scans, and for MySQL to evaluate query cost and ensure primary key usage.

```yaml
rules:
  - name: postgresql-query-too-costly
    message: "Query cost estimate is too high"
    rule: "postgresql.explain.plan.total_cost > 1.0"
  - name: postgresql-no-seq-scan
    message: "Query plan results in a sequential scan"
    rule: "postgresql.explain.plan.node_type == 'Seq Scan'"
  - name: mysql-query-too-costly
    message: "Query cost estimate is too high"
    rule: "has(mysql.explain.query_block.cost_info) && double(mysql.explain.query_block.cost_info.query_cost) > 2.0"
  - name: mysql-must-use-primary-key
    message: "Query plan doesn't use primary key"
    rule: "has(mysql.explain.query_block.table.key) && mysql.explain.query_block.table.key != 'PRIMARY'"
```

---

### Install sqlc on Ubuntu using Snap

Source: https://docs.sqlc.dev/en/latest/_sources/overview/install

Installs the sqlc CLI tool on Ubuntu using the Snap package manager. Snap provides a sandboxed environment for applications.

```shell
sudo snap install sqlc
```

---

### sqlc Configuration Example (PostgreSQL & MySQL)

Source: https://docs.sqlc.dev/en/latest/reference/config

A sample sqlc configuration file demonstrating settings for both PostgreSQL and MySQL databases, including schema and query paths, and Go code generation.

```yaml
version: "2"
cloud:
  project: "<PROJECT_ID>"
sql:
  - schema: "postgresql/schema.sql"
    queries: "postgresql/query.sql"
    engine: "postgresql"
    gen:
      go:
        package: "authors"
        out: "postgresql"
    database:
      managed: true
    rules:
      - sqlc/db-prepare
  - schema: "mysql/schema.sql"
    queries: "mysql/query.sql"
    engine: "mysql"
    gen:
      go:
        package: "authors"
        out: "mysql"
```

---

### Golang-migrate Down Migration File Example

Source: https://docs.sqlc.dev/en/latest/_sources/howto/ddl

An SQL file example for golang-migrate, showing the 'down' SQL to drop the 'post' table.

```sql
DROP TABLE post;
```

---

### Install sqlc using Go

Source: https://docs.sqlc.dev/en/latest/_sources/overview/install

Installs the latest version of the sqlc CLI tool using the Go toolchain. Requires Go version 1.21 or higher.

```go
go install github.com/sqlc-dev/sqlc/cmd/sqlc@latest
```

---

### Update sqlc Installation Path

Source: https://docs.sqlc.dev/en/latest/reference/changelog

Demonstrates the correct way to install sqlc using the go tool with the new repository path. It highlights the incorrect older path for comparison.

```bash
# INCORRECT: old import path
go install github.com/kyleconroy/sqlc/cmd/sqlc@v1.20.0

# CORRECT: new import path
go install github.com/sqlc-dev/sqlc/cmd/sqlc@v1.20.0
```

---

### Define SQLite Queries

Source: https://docs.sqlc.dev/en/latest/_sources/tutorials/getting-started-sqlite

Specifies various SQL queries for interacting with the 'authors' table. These include fetching a single author, listing all authors, creating, updating, and deleting authors. The update query can optionally return the updated record.

```sql
-- name: GetAuthor :one
SELECT * FROM authors
WHERE id = ? LIMIT 1;

-- name: ListAuthors :many
SELECT * FROM authors
ORDER BY name;

-- name: CreateAuthor :one
INSERT INTO authors (
  name,
  bio
) VALUES (
  ?, ?
)
RETURNING *;

-- name: UpdateAuthor :exec
UPDATE authors
set name = ?,
bio = ?
WHERE id = ?;

-- name: DeleteAuthor :exec
DELETE FROM authors
WHERE id = ?;

```

```sql
-- name: UpdateAuthor :one
UPDATE authors
set name = ?,
bio = ?
WHERE id = ?
RETURNING *;

```

---

### sqlc diff example

Source: https://docs.sqlc.dev/en/latest/_sources/howto/ci-cd

Demonstrates the output of `sqlc diff` which highlights changes in generated SQL code, ensuring generated files are up-to-date.

````diff
```diff
% sqlc diff
--- a/postgresql/query.sql.go
+++ b/postgresql/query.sql.go
@@ -55,7 +55,7 @@

 const listAuthors = `-- name: ListAuthors :many
 SELECT id, name, bio FROM authors
-ORDER BY name
+ORDER BY bio
 `
````

````

--------------------------------

### Build sqlc-dev with Go

Source: https://docs.sqlc.dev/en/latest/_sources/guides/development

Builds the sqlc-dev executable and places it in the go bin directory. Requires Go installation.

```bash
go build -o ~/go/bin/sqlc-dev ./cmd/sqlc
````

---

### dbmate Up and Down Migrations

Source: https://docs.sqlc.dev/en/latest/_sources/howto/ddl

Example dbmate migration script showing an 'up' command to create a 'foo' table and a 'down' command to drop it.

```sql
-- migrate:up
CREATE TABLE foo (bar INT NOT NULL);

-- migrate:down
DROP TABLE foo;
```

---

### Run sqlc diff in GitHub Actions

Source: https://docs.sqlc.dev/en/latest/_sources/howto/ci-cd

A GitHub Actions workflow to automatically run `sqlc diff` on every push. It utilizes the `setup-sqlc` action for installation.

````yaml
```yaml
name: sqlc
on: [push]
jobs:
  diff:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: sqlc-dev/setup-sqlc@v3
      with:
        sqlc-version: '1.30.0'
    - run: sqlc diff
````

````

--------------------------------

### sqlc diff command example

Source: https://docs.sqlc.dev/en/latest/howto/ci-cd

Demonstrates the output of the `sqlc diff` command, which compares generated code against the version on disk to catch discrepancies. This is useful for ensuring generated code is up-to-date.

```diff
%% sqlc diff
---
+++
@@ -55,7 +55,7 @@

 const listAuthors = "-- name: ListAuthors :many\n SELECT id, name, bio FROM authors\n-ORDER BY name
+ORDER BY bio
 "


````

---

### Install sqlc on macOS using Homebrew

Source: https://docs.sqlc.dev/en/latest/_sources/overview/install

Installs the sqlc CLI tool on macOS using the Homebrew package manager. Homebrew is a popular package manager for macOS.

```shell
brew install sqlc
```

---

### Run sqlc generate with Docker on Windows Command Prompt

Source: https://docs.sqlc.dev/en/latest/overview/install

Executes the sqlc generate command within a Docker container on Windows Command Prompt. It mounts the current directory (`%cd%`) to the container's /src and sets it as the working directory. This is useful for running sqlc without installing it directly on Windows.

```docker
docker run --rm -v "%cd%:/src" -w /src sqlc/sqlc generate
```

---

### SQL Schema Example

Source: https://docs.sqlc.dev/en/latest/howto/verify

Defines two tables, `users` and `user_actions`, with their respective columns and primary keys. This serves as the initial database schema for demonstrating `sqlc verify`.

```sql
CREATE TABLE users (
  id UUID PRIMARY KEY
);

CREATE TABLE user_actions (
  id UUID PRIMARY KEY,
  user_id UUID NOT NULL,
  action TEXT,
  created_at TIMESTAMP
);

```

---

### sqlc Configuration with Lint Rules

Source: https://docs.sqlc.dev/en/latest/howto/vet

An example of a sqlc configuration file demonstrating how to define schema, query sources, Go generation settings, and custom linting rules. This includes rules for engine type, SQL operations, parameter count, and command types.

```yaml
version: 2
sql:
  - schema: "query.sql"
    queries: "query.sql"
    engine: "postgresql"
    gen:
      go:
        package: "authors"
        out: "db"
    rules:
      - no-pg
      - no-delete
      - only-one-param
      - no-exec
rules:
  - name: no-pg
    message: "invalid engine: postgresql"
    rule: |
      config.engine == "postgresql"
  - name: no-delete
    message: "don't use delete statements"
    rule: |
      query.sql.contains("DELETE")
  - name: only-one-param
    message: "too many parameters"
    rule: |
      query.params.size() > 1
  - name: no-exec
    message: "don't use exec"
    rule: |
      query.cmd == "exec"
```

---

### Goose Up and Down Migration SQL

Source: https://docs.sqlc.dev/en/latest/_sources/howto/ddl

Example SQL for Goose migrations, detailing the 'Up' section to create a 'post' table and the 'Down' section to drop it.

```sql
-- +goose Up
CREATE TABLE post (
    id    int NOT NULL,
    title text,
    body  text,
    PRIMARY KEY(id)
);

-- +goose Down
DROP TABLE post;
```

---

### golang-migrate SQL Migration Example (Corrected Naming)

Source: https://docs.sqlc.dev/en/latest/howto/ddl

Example of golang-migrate migration filenames that ensure correct lexicographical and numerical parsing by sqlc. Prepends zeros to numeric prefixes.

```sql
001_initial.up.sql
...
009_foo.up.sql
010_bar.up.sql

```

---

### Migrate package configuration to use codegen (JSON)

Source: https://docs.sqlc.dev/en/latest/_sources/guides/migrating-to-sqlc-gen-kotlin

Example of a sqlc package configuration in JSON, updated to use the 'codegen' directive with the 'kt' plugin and options for Kotlin code generation.

```json
  "sql": [
    {
      "schema": "schema.sql",
      "queries": "query.sql",
      "engine": "postgresql",
      "codegen": [
        {
          "out": "src/main/kotlin/com/example/foo",
          "plugin": "kt",
          "options": {
            "package": "com.example.foo"
          }
        }
      ]
    }
  ]
```

---

### Push Project with Tags using sqlc push

Source: https://docs.sqlc.dev/en/latest/howto/push

This command uploads your project and associates it with a specific tag, such as 'main'. Tags are useful for referencing specific pushes later, for example, with `sqlc verify`.

```bash
$ sqlc push --tag main

```

---

### SQL Query Example

Source: https://docs.sqlc.dev/en/latest/howto/verify

A SQL query to join user actions with users, ordered by creation time. This query is used to demonstrate how schema changes can impact existing application code.

```sql
-- name: GetUserActions :many
SELECT * FROM users u
JOIN user_actions ua ON u.id = ua.user_id
ORDER BY created_at;

```

---

### Pull sqlc Docker image

Source: https://docs.sqlc.dev/en/latest/_sources/overview/install

Pulls the latest official sqlc Docker image from Docker Hub. This image contains the sqlc CLI.

```docker
docker pull sqlc/sqlc
```

---

### Migrate package configuration to use codegen (YAML)

Source: https://docs.sqlc.dev/en/latest/_sources/guides/migrating-to-sqlc-gen-kotlin

Example of a sqlc package configuration in YAML, updated to use the 'codegen' directive with the 'kt' plugin and options for Kotlin code generation.

```yaml
sql:
  - schema: "schema.sql"
    queries: "query.sql"
    engine: "postgresql"
    codegen:
      - plugin: "kt"
        out: "src/main/kotlin/com/example/foo"
        options:
          package: "com.example.foo"
```

---

### Configure sqlc for PostgreSQL Migrations

Source: https://docs.sqlc.dev/en/latest/_sources/howto/ddl

An example YAML configuration for sqlc to parse PostgreSQL migrations from a specified directory. It points to the schema directory and defines Go code generation settings.

```yaml
version: "2"
sql:
  - engine: "postgresql"
    queries: "query.sql"
    schema: "db/migrations"
    gen:
      go:
        package: "tutorial"
        out: "tutorial"
```

---

### Perform atomic record update within a transaction (pgx/v5)

Source: https://docs.sqlc.dev/en/latest/_sources/howto/transactions

Demonstrates atomic record update using a transaction with the `github.com/jackc/pgx/v5` driver. Similar to the `lib/pq` example, it starts a transaction, defers rollback, uses `WithTx`, retrieves a record, updates it, and commits.

```go
// Using `github.com/jackc/pgx/v5` as the driver.
func bumpCounter(ctx context.Context, db *pgx.Conn, queries *tutorial.Queries, id int32) error {
	tx, err := db.Begin(ctx)
	if err != nil {
		return err
	}
	defer tx.Rollback(ctx)
	qtx := queries.WithTx(tx)
	r, err := qtx.GetRecord(ctx, id)
	if err != nil {
		return err
	}
	if err := qtx.UpdateRecord(ctx, tutorial.UpdateRecordParams{
		ID:      r.ID,
		Counter: r.Counter + 1,
	}); err != nil {
		return err
	}
	return tx.Commit(ctx)
}

```

---

### sqlc Plugin Configuration (Python)

Source: https://docs.sqlc.dev/en/latest/reference/config

Example of configuring sqlc to use a custom plugin for Python code generation, specifying the output directory, plugin details, and generation options.

```yaml
version: "2"
plugins:
  - name: py
    wasm:
      url: https://github.com/sqlc-dev/sqlc-gen-python/releases/download/v0.16.0-alpha/sqlc-gen-python.wasm
      sha256: 428476c7408fd4c032da4ec74e8a7344f4fa75e0f98a5a3302f238283b9b95f2
sql:
  - schema: "schema.sql"
    queries: "query.sql"
    engine: postgresql
    codegen:
      - out: src/authors
        plugin: py
        options:
          package: authors
          emit_sync_querier: true
          emit_async_querier: true
          query_parameter_limit: 5
```

---

### Run sqlc generate command via Docker

Source: https://docs.sqlc.dev/en/latest/_sources/overview/install

Executes the sqlc generate command within a Docker container. It mounts the current directory to /src in the container and sets the working directory to /src.

```docker
docker run --rm -v $(pwd):/src -w /src sqlc/sqlc generate
```

---

### PostgreSQL EXPLAIN query format

Source: https://docs.sqlc.dev/en/latest/_sources/howto/vet

Shows the SQL command executed by sqlc to get EXPLAIN output for PostgreSQL, including specific options for detailed analysis. The output is parsed into a PostgreSQLExplain proto message.

```sql
EXPLAIN (ANALYZE false, VERBOSE, COSTS, SETTINGS, BUFFERS, FORMAT JSON) ...
```

---

### Migrate package configuration for sqlc-gen-python (JSON)

Source: https://docs.sqlc.dev/en/latest/_sources/guides/migrating-to-sqlc-gen-python

Example of a package configuration in JSON format after migrating to the sqlc-gen-python plugin. It replaces the 'gen' mapping with 'codegen' and moves Python-specific options into an 'options' object.

```json
  "sql": [
    {
      "schema": "schema.sql",
      "queries": "query.sql",
      "engine": "postgresql",
      "codegen": [
        {
          "out": "src",
          "plugin": "py",
          "options": {
            "package": "authors",
            "emit_sync_querier": true,
            "emit_async_querier": true,
            "query_parameter_limit": 5
          }
        }
      ]
    }
  ]
```

---

### Goose SQL Migration Example (Corrected Naming)

Source: https://docs.sqlc.dev/en/latest/howto/ddl

Example of Goose migration filenames that ensure correct lexicographical and numerical parsing by sqlc. Prepends zeros to numeric prefixes.

```sql
001_initial.sql
...
009_foo.sql
010_bar.sql

```

---

### Migrate package configuration for sqlc-gen-python (YAML)

Source: https://docs.sqlc.dev/en/latest/_sources/guides/migrating-to-sqlc-gen-python

Example of a package configuration in YAML format after migrating to the sqlc-gen-python plugin. It replaces the 'gen' mapping with 'codegen' and moves Python-specific options into an 'options' object.

```yaml
sql:
  - schema: "schema.sql"
    queries: "query.sql"
    engine: "postgresql"
    codegen:
      - plugin: "py"
        out: "src"
        options:
          package: "foo"
          emit_sync_querier: true
          emit_async_querier: true
          query_parameter_limit: 5
```

---

### Run sqlc generate command via Docker on Windows Command Prompt

Source: https://docs.sqlc.dev/en/latest/_sources/overview/install

Executes the sqlc generate command within a Docker container on Windows Command Prompt. It mounts the current directory to /src in the container and sets the working directory to /src.

```docker
docker run --rm -v "%cd%":/src -w /src sqlc/sqlc generate
```

---

### dbmate SQL Migration Example

Source: https://docs.sqlc.dev/en/latest/howto/ddl

A dbmate migration script demonstrating 'up' and 'down' commands. The 'up' command creates a 'foo' table with a 'bar' column, which sqlc can parse.

```sql
-- migrate:up
CREATE TABLE foo (bar INT NOT NULL);

-- migrate:down
DROP TABLE foo;

```

---

### sql-migrate SQL Migration Example

Source: https://docs.sqlc.dev/en/latest/howto/ddl

An example of sql-migrate directives ('-- +migrate Up' and '-- +migrate Down') for applying and rolling back a schema change. sqlc parses the 'Up' section.

```sql
-- +migrate Up
-- SQL in section 'Up' is executed when this migration is applied
CREATE TABLE people (id int);


-- +migrate Down
-- SQL section 'Down' is executed when this migration is rolled back
DROP TABLE people;

```

---

### Add sqlc-gen-kotlin plugin configuration (YAML)

Source: https://docs.sqlc.dev/en/latest/_sources/guides/migrating-to-sqlc-gen-kotlin

Configuration for adding the sqlc-gen-kotlin plugin to your sqlc setup in YAML format. This includes the plugin name and its WASM URL and SHA256 checksum.

```yaml
version: "2"
plugins:
- name: "kt"
  wasm:
    url: "https://downloads.sqlc.dev/plugin/sqlc-gen-kotlin_1.0.0.wasm"
    sha256: "7620dc5d462de41fdc90e2011232c842117b416c98fd5c163d27c5738431a45c
```

---

### Connection Pooling with pgxpool in Go

Source: https://docs.sqlc.dev/en/latest/_sources/guides/using-go-and-pgx

This Go code example shows how to use pgxpool for managing database connections efficiently. It's recommended for production applications to handle concurrent requests. The generated code can be used with the pool in the same manner as with a single connection.

```go
import (
    "github.com/jackc/pgx/v5/pgxpool"
    "example.com/sqlc-tutorial/db"
)

func main() {
    pool, err := pgxpool.New(context.Background(), os.Getenv("DATABASE_URL"))
    if err != nil {
        fmt.Fprintf(os.Stderr, "Unable to create connection pool: %v\n", err)
        os.Exit(1)
    }
    defer pool.Close()

    q := db.New(pool)
    // Use q the same way as with single connections
}
```

---

### SQL Schema Alteration Example

Source: https://docs.sqlc.dev/en/latest/howto/verify

An example of altering the `users` table to add a `created_at` timestamp column. This change, when applied, can cause issues with queries that reference columns with the same name.

```sql
ALTER TABLE users ADD COLUMN created_at TIMESTAMP;

```

---

### Go - Count Authors using SQLC

Source: https://docs.sqlc.dev/en/latest/_sources/howto/query_count

Implements a Go function to execute the SQL 'CountAuthors' query using SQLC. It takes a context and a DBTX interface, returning the total count of authors as an integer.

```go
package db

import (
	"context"
	"database/sql"
)

type DBTX interface {
	QueryContext(context.Context, string, ...interface{}) (*sql.Rows, error)
	QueryRowContext(context.Context, string, ...interface{}) *sql.Row
}

func New(db DBTX) *Queries {
	return &Queries{db: db}
}

type Queries struct {
	db DBTX
}

const countAuthors = `-- name: CountAuthors :one
SELECT count(*) FROM authors
`

func (q *Queries) CountAuthors(ctx context.Context) (int, error) {
	row := q.db.QueryRowContext(ctx, countAuthors)
	var i int
	err := row.Scan(&i)
	return i, err
}
```

---

### Shell Output of Verification Failure

Source: https://docs.sqlc.dev/en/latest/_sources/howto/verify

Example output from 'sqlc verify' indicating a failure due to an ambiguous column reference in the 'GetUserActions' query.

```shell
$ sqlc verify
FAIL: app query.sql

=== Failed
=== FAIL: app query.sql GetUserActions
    ERROR: column reference "created_at" is ambiguous (SQLSTATE 42702)

```

---

### Atlas SQL Migration Example

Source: https://docs.sqlc.dev/en/latest/howto/ddl

An Atlas migration SQL script to create a 'post' table with an ID, title, and body. sqlc can parse this to generate corresponding Go code.

```sql
-- Create "post" table
CREATE TABLE "public"."post" ("id" integer NOT NULL, "title" text NULL, "body" text NULL, PRIMARY KEY ("id"));

```

---

### Add sqlc-gen-kotlin plugin configuration (JSON)

Source: https://docs.sqlc.dev/en/latest/_sources/guides/migrating-to-sqlc-gen-kotlin

Configuration for adding the sqlc-gen-kotlin plugin to your sqlc setup in JSON format. This includes the plugin name and its WASM URL and SHA256 checksum.

```json
{
  "version": "2",
  "plugins": [
    {
      "name": "kt",
      "wasm": {
        "url": "https://downloads.sqlc.dev/plugin/sqlc-gen-kotlin_1.0.0.wasm",
        "sha256": "7620dc5d462de41fdc90e2011232c842117b416c98fd5c163d27c5738431a45c"
      }
    }
  ]
}
```

---

### SQLC Configuration for Migrations

Source: https://docs.sqlc.dev/en/latest/howto/ddl

Example sqlc configuration snippet showing how to point to a database migration directory instead of individual schema files. This enables sqlc to parse migration scripts.

```yaml
version: "2"
sql:
  - engine: "postgresql"
    queries: "query.sql"
    schema: "db/migrations"
    gen:
      go:
        package: "tutorial"
        out: "tutorial"
```

---

### Go: Get Author QueryRow

Source: https://docs.sqlc.dev/en/latest/reference/query-annotations

Go function to query for a single author and return one record.

```go
func (q *Queries) GetAuthor(ctx context.Context, id int64) (Author, error) {
	row := q.db.QueryRowContext(ctx, getAuthor, id)
	// ...
}
```

---

### Get multiple columns 'bio' and 'birth_year' for an author

Source: https://docs.sqlc.dev/en/latest/_sources/howto/select

Generates Go code to retrieve 'bio' and 'birth_year' columns for a given author ID, returning a struct with these fields.

```sql
-- name: GetInfoForAuthor :one
SELECT bio, birth_year FROM authors
WHERE id = $1;
```

```go
package db

import (
	"context"
	"database/sql"
)

type DBTX interface {
	QueryRowContext(context.Context, string, ...interface{}) *sql.Row
}

func New(db DBTX) *Queries {
	return &Queries{db: db}
}

type Queries struct {
	db DBTX
}

const getInfoForAuthor = `-- name: GetInfoForAuthor :one
SELECT bio, birth_year FROM authors
WHERE id = $1
`

type GetInfoForAuthorRow struct {
	Bio       string
	BirthYear int
}

func (q *Queries) GetInfoForAuthor(ctx context.Context, id int) (GetInfoForAuthorRow, error) {
	row := q.db.QueryRowContext(ctx, getInfoForAuthor, id)
	var i GetInfoForAuthorRow
	err := row.Scan(&i.Bio, &i.BirthYear)
	return i, err
}
```

---

### Go - Count Authors By Town using SQLC

Source: https://docs.sqlc.dev/en/latest/_sources/howto/query_count

Provides a Go function to execute the SQL 'CountAuthorsByTown' query using SQLC. It fetches a list of hometowns and their corresponding author counts, returning a slice of a custom struct.

```go
package db

import (
	"context"
	"database/sql"
)

type DBTX interface {
	QueryContext(context.Context, string, ...interface{}) (*sql.Rows, error)
	QueryRowContext(context.Context, string, ...interface{}) *sql.Row
}

func New(db DBTX) *Queries {
	return &Queries{db: db}
}

type Queries struct {
	db DBTX
}

const countAuthorsByTown = `-- name: CountAuthorsByTown :many
SELECT hometown, count(*) FROM authors
GROUP BY 1
ORDER BY 1
`

type CountAuthorsByTownRow struct {
	Hometown string
	Count    int
}

func (q *Queries) CountAuthorsByTown(ctx context.Context) ([]CountAuthorsByTownRow, error) {
	rows, err := q.db.QueryContext(ctx, countAuthorsByTown)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []CountAuthorsByTownRow{}
	for rows.Next() {
		var i CountAuthorsByTownRow
		if err := rows.Scan(&i.Hometown, &i.Count); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
```

---

### SQL Query Fix Example

Source: https://docs.sqlc.dev/en/latest/howto/verify

The corrected version of the `GetUserActions` query, specifying the table alias `u` for the `created_at` column to resolve ambiguity introduced by the schema alteration.

```sql
-- name: GetUserActions :many
SELECT * FROM users u
JOIN user_actions ua ON u.id = ua.user_id
ORDER BY u.created_at;

```

---

### SQL: Get Author Query Annotation

Source: https://docs.sqlc.dev/en/latest/reference/query-annotations

Annotates a SQL query to retrieve a single author by ID.

```sql
-- name: GetAuthor :one
SELECT * FROM authors
WHERE id = $1 LIMIT 1;
```

---

### Get single column 'bio' for an author

Source: https://docs.sqlc.dev/en/latest/_sources/howto/select

Generates Go code to retrieve only the 'bio' column for a given author ID. Returns a string and an error.

```sql
-- name: GetBioForAuthor :one
SELECT bio FROM authors
WHERE id = $1;
```

```go
package db

import (
	"context"
	"database/sql"
)

type DBTX interface {
	QueryRowContext(context.Context, string, ...interface{}) *sql.Row
}

func New(db DBTX) *Queries {
	return &Queries{db: db}
}

type Queries struct {
	db DBTX
}

const getBioForAuthor = `-- name: GetBioForAuthor :one
SELECT bio FROM authors
WHERE id = $1
`

func (q *Queries) GetBioForAuthor(ctx context.Context, id int) (string, error) {
	row := q.db.QueryRowContext(ctx, getBioForAuthor, id)
	var i string
	err := row.Scan(&i)
	return i, err
}
```

---

### Get Author using :one

Source: https://docs.sqlc.dev/en/latest/_sources/reference/query-annotations

Generates a Go method that returns a single record via QueryRowContext. Use for SELECT statements expected to return at most one row.

```sql
-- name: GetAuthor :one
SELECT * FROM authors
WHERE id = $1 LIMIT 1;
```

```go
func (q *Queries) GetAuthor(ctx context.Context, id int64) (Author, error) {
	row := q.db.QueryRowContext(ctx, getAuthor, id)
	// ...
}
```

---

### Get single author row using MySQL/SQLite syntax

Source: https://docs.sqlc.dev/en/latest/_sources/howto/select

Generates Go code to retrieve a single author row from the database using MySQL and SQLite parameter syntax (?).

```sql
-- name: GetAuthor :one
SELECT * FROM authors
WHERE id = ?;
```

```go
const getAuthor = `-- name: GetAuthor :one
SELECT id, bio, birth_year FROM authors
WHERE id = $1
`

func (q *Queries) GetAuthor(ctx context.Context, id int) (Author, error) {
	row := q.db.QueryRowContext(ctx, getAuthor, id)
	var i Author
	err := row.Scan(&i.ID, &i.Bio, &i.BirthYear)
	return i, err
}
```

---

### tern SQL Migration Example

Source: https://docs.sqlc.dev/en/latest/howto/ddl

A tern migration script showing a 'create table' statement and a 'drop table' statement separated by '---- create above / drop below ----'. sqlc parses the create statement.

```sql
CREATE TABLE comment (id int NOT NULL, text text NOT NULL);
---- create above / drop below ----
DROP TABLE comment;

```

---

### Get single author row using PostgreSQL syntax

Source: https://docs.sqlc.dev/en/latest/_sources/howto/select

Generates Go code to retrieve a single author row from the database using PostgreSQL parameter syntax ($1).

```sql
-- name: GetAuthor :one
SELECT * FROM authors
WHERE id = $1;
```

```go
const getAuthor = `-- name: GetAuthor :one
SELECT id, bio, birth_year FROM authors
WHERE id = $1
`

func (q *Queries) GetAuthor(ctx context.Context, id int) (Author, error) {
	row := q.db.QueryRowContext(ctx, getAuthor, id)
	var i Author
	err := row.Scan(&i.ID, &i.Bio, &i.BirthYear)
	return i, err
}
```

---

### CAST support for MySQL (SQL)

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Illustrates how sqlc handles `CAST` operations in MySQL queries. This example shows casting a BOOLEAN column to BIGINT.

```sql
CREATE TABLE foo (bar BOOLEAN NOT NULL);

-- name: SelectColumnCast :many
SELECT CAST(bar AS BIGINT) FROM foo;
```

---

### Define CEL Lint Rules for SQL Queries

Source: https://docs.sqlc.dev/en/latest/_sources/howto/vet

This example shows how to define lint rules in a sqlc configuration file. It specifies rules for checking the database engine, disallowing DELETE statements, enforcing a single parameter, and preventing the use of EXEC commands.

```yaml
version: 2
sql:
  - schema: "query.sql"
    queries: "query.sql"
    engine: "postgresql"
    gen:
      go:
        package: "authors"
        out: "db"
    rules:
      - no-pg
      - no-delete
      - only-one-param
      - no-exec
rules:
  - name: no-pg
    message: "invalid engine: postgresql"
    rule: |
      config.engine == "postgresql"
  - name: no-delete
    message: "don't use delete statements"
    rule: |
      query.sql.contains("DELETE")
  - name: only-one-param
    message: "too many parameters"
    rule: |
      query.params.size() > 1
  - name: no-exec
    message: "don't use exec"
    rule: |
      query.cmd == "exec"
```

---

### Create Author Table with Returning

Source: https://docs.sqlc.dev/en/latest/howto/insert

Defines the 'authors' table schema with 'id', 'name', and 'bio' columns, using BIGSERIAL for the primary key. This schema is used for examples involving the RETURNING clause.

```sql
-- Example queries for sqlc
CREATE TABLE authors (
  id   BIGSERIAL PRIMARY KEY,
  name text      NOT NULL,
  bio  text
);

```

---

### SQL - Count Authors and By Town

Source: https://docs.sqlc.dev/en/latest/_sources/howto/query_count

Defines SQL statements to count all rows in the 'authors' table and to count authors grouped by their hometown. The first query returns a single count, while the second returns a list of hometowns and their respective counts.

```sql
CREATE TABLE authors (
  id       SERIAL PRIMARY KEY,
  hometown text   NOT NULL
);

-- name: CountAuthors :one
SELECT count(*) FROM authors;

-- name: CountAuthorsByTown :many
SELECT hometown, count(*) FROM authors
GROUP BY 1
ORDER BY 1;
```

---

### sqlc CLI Usage and Commands

Source: https://docs.sqlc.dev/en/latest/_sources/reference/cli

This snippet outlines the general usage of the sqlc CLI and lists all available commands. It details the purpose of each command, such as 'compile' for syntax checking and 'generate' for code creation. Flags like '--file' and '--no-database' are also presented.

```sh
Usage:
  sqlc [command]

Available Commands:
  compile     Statically check SQL for syntax and type errors
  completion  Generate the autocompletion script for the specified shell
  createdb    Create an ephemeral database
  diff        Compare the generated files to the existing files
  generate    Generate source code from SQL
  help        Help about any command
  init        Create an empty sqlc.yaml settings file
  push        Push the schema, queries, and configuration for this project
  verify      Verify schema, queries, and configuration for this project
  version     Print the sqlc version number
  vet         Vet examines queries

Flags:
  -f, --file string    specify an alternate config file (default: sqlc.yaml)
  -h, --help           help for sqlc
      --no-database    disable database connections (default: false)
      --no-remote      disable remote execution (default: false)

Use "sqlc [command] --help" for more information about a command.
```

---

### Nullable Parameter Handling with sqlc.narg() - sqlc

Source: https://docs.sqlc.dev/en/latest/howto/named_parameters

Demonstrates how to use `sqlc.narg()` to explicitly define nullable parameters in SQL, overriding sqlc's inferred nullability. This example updates an author's name and bio, allowing either or both to be updated.

```sql
-- name: UpdateAuthor :one
UPDATE author
SET
 name = coalesce(sqlc.narg('name'), name),
 bio = coalesce(sqlc.narg('bio'), bio)
WHERE id = sqlc.arg('id')
RETURNING *;
```

```go
type UpdateAuthorParams struct {
	Name sql.NullString
	Bio  sql.NullString
	ID   int64
}
```

---

### Configure sqlc for go-geos with pgx/v5

Source: https://docs.sqlc.dev/en/latest/reference/datatypes

This snippet shows how to configure sqlc to use the go-geos package for geometry types when using pgx/v5. It includes an example SQL schema and query, the sqlc configuration file to override the default 'geometry' DB type to 'github.com/twpayne/go-geos.Geom', and Go code demonstrating how to register the geos context with a pgx connection.

```sql
-- Multipolygons in British National Grid (epsg:27700)
create table shapes(
  id serial,
  name varchar,
  geom geometry(Multipolygon, 27700)
);

-- name: GetCentroids :many
SELECT id, name, ST_Centroid(geom)::geometry FROM shapes;

```

```json
{
  "version": 2,
  "gen": {
    "go": {
      "overrides": [
        {
          "db_type": "geometry",
          "go_type": {
            "import": "github.com/twpayne/go-geos",
            "package": "geos",
            "pointer": true,
            "type": "Geom"
          },
          "nullable": true
        }
      ]
    }
  }
}
```

```go
import (
    "github.com/twpayne/go-geos"
    pgxgeos "github.com/twpayne/pgx-geos"
)

// ...

config.AfterConnect = func(ctx context.Context, conn *pgx.Conn) error {
    if err := pgxgeos.Register(ctx, conn, geos.NewContext()); err != nil {
        return err
    }
    return nil
}

```

---

### SQL Schema Definitions for Tables

Source: https://docs.sqlc.dev/en/latest/_sources/howto/rename

These SQL statements define two tables: 'authors' and 'book_publishers', including their primary keys and text fields. These are used as examples for struct generation.

```sql
CREATE TABLE authors (
  id   BIGSERIAL PRIMARY KEY,
  name text      NOT NULL,
  bio  text
);

CREATE TABLE book_publishers (
  id   BIGSERIAL PRIMARY KEY,
  name text      NOT NULL
);
```

---

### Default Parameter Naming in SQL - sqlc

Source: https://docs.sqlc.dev/en/latest/howto/named_parameters

Demonstrates how sqlc might generate less descriptive names for positional parameters when it lacks sufficient context. This example shows a SQL UPDATE statement with positional parameters and the resulting Go struct with auto-generated field names.

```sql
-- name: UpsertAuthorName :one
UPDATE author
SET
  name = CASE WHEN $1::bool
    THEN $2::text
    ELSE name
    END
RETURNING *;
```

```go
type UpdateAuthorNameParams struct {
	Column1   bool   `json:""`
	Column2_2 string `json:"_2"`
}
```

---

### Named Parameter Naming with sqlc.arg() - sqlc

Source: https://docs.sqlc.dev/en/latest/howto/named_parameters

Shows how to use the `sqlc.arg()` function in SQL to explicitly name parameters, providing better control over the generated Go struct field names. This example updates an author's name based on a boolean flag.

```sql
-- name: UpsertAuthorName :one
UPDATE author
SET
  name = CASE WHEN sqlc.arg(set_name)::bool
    THEN sqlc.arg(name)::text
    ELSE name
    END
RETURNING *;
```

```go
type UpdateAuthorNameParams struct {
	SetName bool   `json:"set_name"`
	Name    string `json:"name"`
}
```

---

### Run sqlc vet in GitHub Actions

Source: https://docs.sqlc.dev/en/latest/_sources/howto/ci-cd

A GitHub Actions workflow to execute `sqlc vet` on every push. This workflow includes setting up a PostgreSQL server if lint rules require a database connection.

````yaml
```yaml
name: sqlc
on: [push]
jobs:
  vet:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: sqlc-dev/setup-sqlc@v3
      with:
        sqlc-version: '1.30.0'
    # Start a PostgreSQL server
    - uses: sqlc-dev/action-setup-postgres@master
      with:
        postgres-version: "16"
      id: postgres
    - run: sqlc vet
      env:
        POSTGRESQL_SERVER_URI: ${{ steps.postgres.outputs.connection-uri }}?sslmode=disable
````

````

--------------------------------

### Configure Global Go Type Overrides for UUIDs (sqlc v1)

Source: https://docs.sqlc.dev/en/latest/howto/overrides

This example demonstrates how to set global type overrides for UUIDs in sqlc version 1. It specifies that the `uuid` database type should be mapped to the Go type `github.com/gofrs/uuid.UUID`.

```yaml
version: "1"
packages: [...]
overrides:
  - db_type: "uuid"
    go_type: "github.com/gofrs/uuid.UUID"

````

---

### Disable all lint rules for a SQL query - sqlc

Source: https://docs.sqlc.dev/en/latest/howto/vet

This example demonstrates how to disable all lint rules for a specific SQL query by using the `@sqlc-vet-disable` annotation without any parameters. This is useful for queries where you are certain no linting is required.

```sql
/* name: GetAuthor :one */
/* @sqlc-vet-disable */
SELECT * FROM authors
WHERE id = ? LIMIT 1;

```

---

### Enable database preparation rule in sqlc

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

This YAML configuration demonstrates how to enable the `sqlc/db-prepare` rule, which validates queries against a live database. It includes a `database.uri` setting to connect to a PostgreSQL instance, allowing `sqlc vet` to prepare queries and report any preparation failures.

```yaml
version: 2sql:
  - schema: "query.sql"
    queries: "query.sql"
    engine: "postgresql"
    gen:
      go:
        package: "authors"
        out: "db"
    database:
      uri: "postgresql://postgres:password@localhost:5432/postgres"
    rules:
      - sqlc/db-prepare
```

---

### Advanced Go Type Mapping with Import and Package

Source: https://docs.sqlc.dev/en/latest/_sources/howto/overrides

This example illustrates an advanced `go_type` override configuration in `sqlc.yaml`. It maps a `uuid` database type to a custom Go type `MyType` from a specific import path `a/b/v2`, using a pointer.

```yaml
version: "2"sql:
- schema: "postgresql/schema.sql"
  queries: "postgresql/query.sql"
  engine: "postgresql"
  gen:
    go:
      package: "authors"
      out: "db"
      sql_package: "pgx/v5"
      overrides:
        - db_type: "uuid"
          go_type:
            import: "a/b/v2"
            package: "b"
            type: "MyType"
            pointer: true
```

---

### SQLC CLI Usage

Source: https://docs.sqlc.dev/en/latest/reference/cli

Provides the general usage syntax for the sqlc command-line interface, including a list of available commands and their brief descriptions. It also lists global flags that can be applied to any command.

```bash
Usage:
  sqlc [command]

Available Commands:
  compile     Statically check SQL for syntax and type errors
  completion  Generate the autocompletion script for the specified shell
  createdb    Create an ephemeral database
  diff        Compare the generated files to the existing files
  generate    Generate source code from SQL
  help        Help about any command
  init        Create an empty sqlc.yaml settings file
  push        Push the schema, queries, and configuration for this project
  verify      Verify schema, queries, and configuration for this project
  version     Print the sqlc version number
  vet         Vet examines queries

Flags:
  -f, --file string    specify an alternate config file (default: sqlc.yaml)
  -h, --help           help for sqlc
      --no-database    disable database connections (default: false)
      --no-remote      disable remote execution (default: false)

Use "sqlc [command] --help" for more information about a command.

```

---

### Configure sqlc for go-geom

Source: https://docs.sqlc.dev/en/latest/reference/datatypes

This snippet demonstrates how to configure sqlc to use the go-geom package for PostGIS geometry types. It includes an example SQL schema and query, and a sqlc configuration file specifying overrides for the 'geometry' DB type to use 'github.com/twpayne/go-geom.MultiPolygon'.

```sql
-- Multipolygons in British National Grid (epsg:27700)
create table shapes(
  id serial,
  name varchar,
  geom geometry(Multipolygon, 27700)
);

-- name: GetShapes :many
SELECT * FROM shapes;

```

```json
{
  "version": "1",
  "packages": [
    {
      "path": "db",
      "engine": "postgresql",
      "schema": "query.sql",
      "queries": "query.sql"
    }
  ],
  "overrides": [
    {
      "db_type": "geometry",
      "go_type": "github.com/twpayne/go-geom.MultiPolygon"
    },
    {
      "db_type": "geometry",
      "go_type": "github.com/twpayne/go-geom.MultiPolygon",
      "nullable": true
    }
  ]
}
```

---

### Perform atomic record update within a transaction (lib/pq)

Source: https://docs.sqlc.dev/en/latest/_sources/howto/transactions

Illustrates how to update a record atomically using a transaction with the `github.lib/pq` driver. It begins a transaction, defers rollback, uses `WithTx` to get transaction-bound queries, fetches a record, updates it, and commits the transaction.

```go
// Using `github/lib/pq` as the driver.
func bumpCounter(ctx context.Context, db *sql.DB, queries *tutorial.Queries, id int32) error {
	tx, err := db.Begin()
	if err != nil {
		return err
	}
	defer tx.Rollback()
	qtx := queries.WithTx(tx)
	r, err := qtx.GetRecord(ctx, id)
	if err != nil {
		return err
	}
	if err := qtx.UpdateRecord(ctx, tutorial.UpdateRecordParams{
		ID:      r.ID,
		Counter: r.Counter + 1,
	}); err != nil {
		return err
	}
	return tx.Commit()
}

```

---

### GitHub Actions: Run sqlc diff

Source: https://docs.sqlc.dev/en/latest/howto/ci-cd

A GitHub Actions workflow configuration that runs `sqlc diff` on every push. It uses the `actions/checkout` and `sqlc-dev/setup-sqlc` actions to set up the environment.

```yaml
name: sqlc
on: [push]
jobs:
  diff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: sqlc-dev/setup-sqlc@v3
        with:
          sqlc-version: "1.30.0"
      - run: sqlc diff
```

---

### Custom SQL Validation Rules

Source: https://docs.sqlc.dev/en/latest/reference/config

Defines custom validation rules for SQL queries using the Common Expression Language (CEL). Examples include preventing specific SQL keywords or limiting the number of parameters.

```yaml
version: "2"
sql:
  - schema: "query.sql"
    queries: "query.sql"
    engine: "postgresql"
    gen:
      go:
        package: "authors"
        out: "db"
    rules:
      - no-pg
      - no-delete
      - only-one-param
      - no-exec
rules:
  - name: no-pg
    message: "invalid engine: postgresql"
    rule: |
      config.engine == "postgresql"
  - name: no-delete
    message: "don't use delete statements"
    rule: |
      query.sql.contains("DELETE")
  - name: only-one-param
    message: "too many parameters"
    rule: |
      query.params.size() > 1
  - name: no-exec
    message: "don't use exec"
    rule: |
      query.cmd == "exec"
```

---

### MySQL EXPLAIN rules for sqlc

Source: https://docs.sqlc.dev/en/latest/howto/vet

Defines rules in sqlc configuration for MySQL that analyze EXPLAIN output. It demonstrates checks for query cost and primary key usage.

```yaml
rules:
  - name: mysql-query-too-costly
    message: "Query cost estimate is too high"
    rule: "has(mysql.explain.query_block.cost_info) && double(mysql.explain.query_block.cost_info.query_cost) > 2.0"
  - name: mysql-must-use-primary-key
    message: "Query plan doesn't use primary key"
    rule: "has(mysql.explain.query_block.table.key) && mysql.explain.query_block.table.key != 'PRIMARY'"
```

---

### Debugging EXPLAIN Output in SQLC

Source: https://docs.sqlc.dev/en/latest/_sources/howto/vet

Shows how to configure sqlc to print the raw EXPLAIN output from the database by setting the SQLCDEBUG environment variable to 'dumpexplain=1'. This is useful for understanding the data structure when writing custom rules.

```yaml
version: 2
sql:
  - schema: "query.sql"
    queries: "query.sql"
    engine: "postgresql"
    database:
      uri: "postgresql://postgres:postgres@localhost:5432/postgres"
    gen:
      go:
        package: "db"
        out: "db"
    rules:
      - debug
rules:
  - name: debug
    rule: "!has(postgresql.explain)" # A dummy rule to trigger explain
```

---

### PostgreSQL EXPLAIN rules for sqlc

Source: https://docs.sqlc.dev/en/latest/howto/vet

Defines rules in sqlc configuration for PostgreSQL that analyze EXPLAIN output. It shows how to check for query cost and sequential scans.

```yaml
rules:
  - name: postgresql-query-too-costly
    message: "Query cost estimate is too high"
    rule: "postgresql.explain.plan.total_cost > 1.0"
  - name: postgresql-no-seq-scan
    message: "Query plan results in a sequential scan"
    rule: "postgresql.explain.plan.node_type == 'Seq Scan'"
```

---

### Configure sqlc vet with database connectivity (YAML)

Source: https://docs.sqlc.dev/en/latest/reference/changelog

Sets up sqlc vet to connect to a live PostgreSQL database to validate queries using the 'sqlc/db-prepare' rule. Requires a database URI in the configuration.

```yaml
version: 2sql:
  - schema: "query.sql"
    queries: "query.sql"
    engine: "postgresql"
    gen:
      go:
        package: "authors"
        out: "db"
    database:
      uri: "postgresql://postgres:password@localhost:5432/postgres"
    rules:
      - sqlc/db-prepare

```

---

### Build sqlc-gen-json with Go

Source: https://docs.sqlc.dev/en/latest/_sources/guides/development

Builds the sqlc-gen-json executable and places it in the go bin directory. Required for test success.

```bash
go build -o ~/go/bin/sqlc-gen-json ./cmd/sqlc-gen-json
```

---

### Disable specific lint rules in SQL query - sqlc

Source: https://docs.sqlc.dev/en/latest/howto/vet

This example demonstrates how to disable specific lint rules like 'sqlc/db-prepare' and 'no-pg' for a single SQL query using the `@sqlc-vet-disable` annotation. It's useful when you need to bypass certain checks for a particular query.

```sql
/* name: GetAuthor :one */
/* @sqlc-vet-disable sqlc/db-prepare no-pg */
SELECT * FROM authors
WHERE id = ? LIMIT 1;

```

---

### SQLC Configuration: Complex Go type override with import, package, and pointer

Source: https://docs.sqlc.dev/en/latest/howto/overrides

This example shows a more complex `db_type` override where the Go type mapping requires specifying an import path, package name, type name, and indicating that the type should be a pointer. This is useful when the desired Go type is not directly in the main package or requires specific import aliasing.

```yaml
version: "2"sql:
- schema: "postgresql/schema.sql"
  queries: "postgresql/query.sql"
  engine: "postgresql"
  gen:
    go:
      package: "authors"
      out: "db"
      sql_package: "pgx/v5"
      overrides:
        - db_type: "uuid"
          go_type:
            import: "a/b/v2"
            package: "b"
            type: "MyType"
            pointer: true

```

---

### Disable lint rules across multiple lines in SQL query - sqlc

Source: https://docs.sqlc.dev/en/latest/howto/vet

This example shows how to disable lint rules by splitting the `@sqlc-vet-disable` annotation across multiple lines. This approach can improve readability when disabling several rules for a query.

```sql
/* name: GetAuthor :one */
/* @sqlc-vet-disable sqlc/db-prepare */
/* @sqlc-vet-disable no-pg */
SELECT * FROM authors
WHERE id = ? LIMIT 1;

```

---

### GitHub Actions: Run sqlc vet

Source: https://docs.sqlc.dev/en/latest/howto/ci-cd

This GitHub Actions workflow executes `sqlc vet` on every push. It requires a PostgreSQL connection if lint rules depend on database access, like `sqlc/db-prepare`.

```yaml
name: sqlc
on: [push]
jobs:
  vet:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: sqlc-dev/setup-sqlc@v3
        with:
          sqlc-version: "1.30.0"
      # Start a PostgreSQL server
      - uses: sqlc-dev/action-setup-postgres@master
        with:
          postgres-version: "16"
        id: postgres
      - run: sqlc vet
        env:
          POSTGRESQL_SERVER_URI: ${{ steps.postgres.outputs.connection-uri }}?sslmode=disable
```

---

### SQL Query to Get Student's Highest Score (Embedded)

Source: https://docs.sqlc.dev/en/latest/reference/changelog

An SQL query using `sqlc.embed` to retrieve student records and their highest test score. This version embeds the entire 'students' table row into the result struct, simplifying Go code.

```sql
-- name: HighScoreEmbed :many
WITH high_scores AS (
  SELECT student_id, max(score) as high_score
  FROM test_scores
  GROUP BY 1
)
SELECT sqlc.embed(students), high_score::integer
FROM students
JOIN high_scores ON high_scores.student_id = students.id;
```

---

### Generate Go Code with sqlc

Source: https://docs.sqlc.dev/en/latest/_sources/guides/using-go-and-pgx

This bash command initiates the code generation process using sqlc. It reads the configuration from sqlc.yaml and the SQL queries to produce Go database access methods.

```bash
sqlc generate
```

---

### Go Code for Prepared Queries with SQLC

Source: https://docs.sqlc.dev/en/latest/_sources/howto/prepared_query

Generates Go code for database interactions using prepared statements. It includes a `DBTX` interface for dependency injection, functions to prepare queries, and methods to execute them within or outside transactions. The `getRecord` constant holds the SQL query string.

```go
package db

import (
	"context"
	"database/sql"
	"fmt"
)

type Record struct {
	ID int32
}

type DBTX interface {
	PrepareContext(context.Context, string) (*sql.Stmt, error)
	QueryRowContext(context.Context, string, ...interface{}) *sql.Row
}

func New(db DBTX) *Queries {
	return &Queries{db: db}
}

func Prepare(ctx context.Context, db DBTX) (*Queries, error) {
	q := Queries{db: db}
	var err error
	if q.getRecordStmt, err = db.PrepareContext(ctx, getRecord); err != nil {
		return nil, fmt.Errorf("error preparing query GetRecord: %w", err)
	}
	return &q, nil
}

func (q *Queries) queryRow(ctx context.Context, stmt *sql.Stmt, query string, args ...interface{}) *sql.Row {
	switch {
	case stmt != nil && q.tx != nil:
		return q.tx.StmtContext(ctx, stmt).QueryRowContext(ctx, args...)
	case stmt != nil:
		return stmt.QueryRowContext(ctx, args...)
	default:
		return q.db.QueryRowContext(ctx, query, args...)
	}
}

type Queries struct {
	db            DBTX
	tx            *sql.Tx
	getRecordStmt *sql.Stmt
}

func (q *Queries) WithTx(tx *sql.Tx) *Queries {
	return &Queries{
		db:            tx,
		tx:            tx,
		getRecordStmt: q.getRecordStmt,
	}
}

const getRecord = `-- name: GetRecord :one
SELECT id FROM records
WHERE id = $1
`

func (q *Queries) GetRecord(ctx context.Context, id int32) (int32, error) {
	row := q.queryRow(ctx, q.getRecordStmt, getRecord, id)
	err := row.Scan(&id)
	return id, err
}
```

---

### SQL Query to Get Student's Highest Score (Standard)

Source: https://docs.sqlc.dev/en/latest/reference/changelog

A standard SQL query to retrieve student records along with their highest test score. It uses a Common Table Expression (CTE) to find the max score per student and then joins it with the students table.

```sql
-- name: HighScore :many
WITH high_scores AS (
  SELECT student_id, max(score) as high_score
  FROM test_scores
  GROUP BY 1
)
SELECT students.*, high_score::integer
FROM students
JOIN high_scores ON high_scores.student_id = students.id;
```

---

### Use pgx connection for database operations in Go

Source: https://docs.sqlc.dev/en/latest/guides/using-go-and-pgx

This Go code demonstrates how to establish a connection to a PostgreSQL database using `pgx.Connect` and then use the generated `db` package to interact with the database. It retrieves an author by ID and prints their name. Error handling for connection and query execution is included.

```go
package main

import (
    "context"
    "fmt"
    "os"

    "github.com/jackc/pgx/v5"

    "example.com/sqlc-tutorial/db"
)

func main() {
    // urlExample := "postgres://username:password@localhost:5432/database_name"
    conn, err := pgx.Connect(context.Background(), os.Getenv("DATABASE_URL"))
    if err != nil {
            fmt.Fprintf(os.Stderr, "Unable to connect to database: %v\n", err)
            os.Exit(1)
    }
    defer conn.Close(context.Background())

    q := db.New(conn)

    author, err := q.GetAuthor(context.Background(), 1)
    if err != nil {
            fmt.Fprintf(os.Stderr, "GetAuthor failed: %v\n", err)
            os.Exit(1)
    }

    fmt.Println(author.Name)
}
```

---

### Run sqlc verify and push in GitHub Actions

Source: https://docs.sqlc.dev/en/latest/_sources/howto/ci-cd

A comprehensive GitHub Actions workflow that includes running `sqlc verify` to check schema changes against existing queries and `sqlc push` for main branch merges. It sets up PostgreSQL and uses `SQLC_AUTH_TOKEN`.

````yaml
```yaml
name: sqlc
on: [push]
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: sqlc-dev/setup-sqlc@v3
      with:
        sqlc-version: '1.30.0'
    - uses: sqlc-dev/action-setup-postgres@master
      with:
        postgres-version: "16"
      id: postgres
    - run: sqlc verify
      env:
        POSTGRESQL_SERVER_URI: ${{ steps.postgres.outputs.connection-uri }}?sslmode=disable
        SQLC_AUTH_TOKEN: ${{ secrets.SQLC_AUTH_TOKEN }}
  push:
    runs-on: ubuntu-latest
    if: ${{ github.ref == 'refs/heads/main' }}
    steps:
    - uses: sqlc-dev/setup-sqlc@v3
      with:
        sqlc-version: '1.30.0'
    - run: sqlc push
      env:
        SQLC_AUTH_TOKEN: ${{ secrets.SQLC_AUTH_TOKEN }}
````

````

--------------------------------

### Basic sqlc.yaml Configuration for PostgreSQL

Source: https://docs.sqlc.dev/en/latest/_sources/howto/generate

This configuration defines the `sqlc` version, the database engine (PostgreSQL), paths to SQL query and schema files, and the output configuration for Go code generation. It specifies the Go package name and output directory.

```yaml
version: "2"
sql:
  - engine: "postgresql"
    queries: "query.sql"
    schema: "schema.sql"
    gen:
      go:
        package: "tutorial"
        out: "tutorial"
        sql_package: "pgx/v5"
````

---

### Configure sqlc linting rules

Source: https://docs.sqlc.dev/en/latest/_sources/howto/vet

This YAML configuration snippet shows how to define and enable specific lint rules for a sqlc project. It specifies the schema, queries, engine, Go generation settings, and the rules to be applied. Dependencies include the sqlc configuration file format.

```yaml
version: 2
sql:
  - schema: "query.sql"
    queries: "query.sql"
    engine: "postgresql"
    gen:
      go:
        package: "authors"
        out: "db"
    rules:
      - no-delete
rules:
  - name: no-pg
    message: "invalid engine: postgresql"
    rule: |
      config.engine == "postgresql"
  - name: no-delete
    message: "don't use delete statements"
    rule: |
      query.sql.contains("DELETE")
```

---

### Proto Definitions for SQL Configuration and Queries

Source: https://docs.sqlc.dev/en/latest/_sources/howto/vet

These Protocol Buffer messages define the structure for sqlc configuration and query information. They include details like version, engine, schema files, query SQL, name, command type, and parameters, which are accessible in CEL expressions.

```proto
message Config
{
  string version = 1;
  string engine = 2 ;
  repeated string schema = 3;
  repeated string queries = 4;
}

message Query
{
  // SQL body
  string sql = 1;
  // Name of the query
  string name = 2;
  // One of "many", "one", "exec", etc.
  string cmd = 3;
  // Query parameters, if any
  repeated Parameter params = 4;
}

message Parameter
{
  int32 number = 1;
}
```

---

### MySQL EXPLAIN query format

Source: https://docs.sqlc.dev/en/latest/_sources/howto/vet

Demonstrates the SQL command used by sqlc to retrieve EXPLAIN output for MySQL, utilizing the JSON format for detailed query plan information. The output is parsed into a MySQLExplain proto message.

```sql
EXPLAIN FORMAT=JSON ...
```

---

### Use `sqlc createdb` command

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

The `sqlc createdb` command allows spinning up ephemeral databases for cloud projects. It outputs the connection string to standard output, facilitating integration with other tools.

```bash
# Example of using createdb
# sqlc create --project=myproject --database=mydb
# export DATABASE_URL=$(sqlc createdb --project=myproject --database=mydb)
# sqlc generate

```

---

### GitHub Actions: Run sqlc push

Source: https://docs.sqlc.dev/en/latest/howto/ci-cd

A GitHub Actions workflow that runs `sqlc push` on commits to the 'main' branch. This pushes schema, queries, and configuration to sqlc Cloud for verification purposes. Requires an SQLC_AUTH_TOKEN.

```yaml
name: sqlc
on: [push]
jobs:
  push:
    runs-on: ubuntu-latest
    if: ${{ github.ref == 'refs/heads/main' }}
    steps:
      - uses: actions/checkout@v3
      - uses: sqlc-dev/setup-sqlc@v3
        with:
          sqlc-version: "1.30.0"
      - run: sqlc push
        env:
          SQLC_AUTH_TOKEN: ${{ secrets.SQLC_AUTH_TOKEN }}
```

---

### Configure Plugins for sqlc (WASM and CLI)

Source: https://docs.sqlc.dev/en/latest/_sources/reference/config

Demonstrates how to define plugins for sqlc, supporting both WebAssembly modules (with URL and SHA256 checksum) and command-line executables, including environment variables and expected output format.

```yaml
version: "2"
plugins:
  - name: "py"
    wasm:
      url: "https://github.com/sqlc-dev/sqlc-gen-python/releases/download/v0.16.0-alpha/sqlc-gen-python.wasm"
      sha256: "428476c7408fd4c032da4ec74e8a7344f4fa75e0f98a5a3302f238283b9b95f2"
  - name: "js"
    env:
      - PATH
    process:
      cmd: "sqlc-gen-json"
```

---

### Connect to PostgreSQL using pgx in Go

Source: https://docs.sqlc.dev/en/latest/_sources/guides/using-go-and-pgx

This Go code demonstrates establishing a connection to a PostgreSQL database using the pgx driver and executing a query to retrieve an author. It handles potential connection errors and query execution failures.

```go
package main

import (
	"context"
	"fmt"
	"os"

	"github.com/jackc/pgx/v5"

	"example.com/sqlc-tutorial/db"
)

func main() {
	// urlExample := "postgres://username:password@localhost:5432/database_name"
	conn, err := pgx.Connect(context.Background(), os.Getenv("DATABASE_URL"))
	if err != nil {
		fmt.Fprintf(os.Stderr, "Unable to connect to database: %v\n", err)
		os.Exit(1)
	}
	defer conn.Close(context.Background())

	q := db.New(conn)

	author, err := q.GetAuthor(context.Background(), 1)
	if err != nil {
		fmt.Fprintf(os.Stderr, "GetAuthor failed: %v\n", err)
		os.Exit(1)
	}

	fmt.Println(author.Name)
}
```

---

### Configure WASM Plugin for Greeter Output

Source: https://docs.sqlc.dev/en/latest/_sources/guides/plugins

This YAML configuration sets up a WASM plugin named 'greeter' for sqlc. It specifies the plugin's WASM URL and SHA256 hash, and configures the codegen process to output files to the 'gen' directory using the 'greeter' plugin with a 'lang' option set to 'en-US'.

```yaml
version: "2"
plugins:
  - name: greeter
    wasm:
      url: https://github.com/sqlc-dev/sqlc-gen-greeter/releases/download/v0.1.0/sqlc-gen-greeter.wasm
      sha256: afc486dac2068d741d7a4110146559d12a013fd0286f42a2fc7dcd802424ad07
sql:
  - schema: schema.sql
    queries: query.sql
    engine: postgresql
    codegen:
      - out: gen
        plugin: greeter
        options:
          lang: en-US
```

---

### Push Project with sqlc push

Source: https://docs.sqlc.dev/en/latest/howto/push

This command uploads your project to sqlc Cloud. Ensure your configuration and authentication are set up correctly before running.

```bash
$ sqlc push

```

---

### Push Project to sqlc Cloud

Source: https://docs.sqlc.dev/en/latest/_sources/howto/push

This is the command to execute after configuring and verifying your project. Removing the `--dry-run` flag will perform the actual upload of your project to sqlc Cloud.

```shell
$ sqlc push
```

---

### Configure WASM Plugin with Environment Variables

Source: https://docs.sqlc.dev/en/latest/_sources/guides/plugins

This configuration demonstrates how to set up a WASM plugin in sqlc. It includes defining the plugin, specifying its WASM URL and SHA256 hash, and granting access to the 'PATH' environment variable. The 'codegen' section specifies the output directory and the plugin to use.

```yaml
version: "2"
sql:
  - schema: schema.sql
    queries: query.sql
    engine: postgresql
    codegen:
      - out: gen
        plugin: test
plugins:
  - name: test
    env:
      - PATH
    wasm:
      url: https://github.com/sqlc-dev/sqlc-gen-test/releases/download/v0.1.0/sqlc-gen-test.wasm
      sha256: 138220eae508d4b65a5a8cea555edd155eb2290daf576b7a8b96949acfeb3790
```

---

### Basic sqlc.yaml Configuration for PostgreSQL

Source: https://docs.sqlc.dev/en/latest/howto/generate

This configuration defines the PostgreSQL engine, points to SQL query and schema files, and specifies Go code generation settings, including package name and output directory.

```yaml
version: "2"sql:
  - engine: "postgresql"
    queries: "query.sql"
    schema: "schema.sql"
    gen:
      go:
        package: "tutorial"
        out: "tutorial"
        sql_package: "pgx/v5"

```

---

### sqlc Database Connection URI

Source: https://docs.sqlc.dev/en/latest/_sources/reference/config

Sets up a PostgreSQL database connection for sqlc using a URI that includes an environment variable for the password.

```yaml
version: "2"
sql:
  - schema: schema.sql
    queries: query.sql
    engine: postgresql
    database:
      uri: postgresql://postgres:${PG_PASSWORD}@localhost:5432/authors
    gen:
      go:
        package: authors
        out: postgresql
```

---

### Configure WASM plugin for code generation

Source: https://docs.sqlc.dev/en/latest/guides/plugins

This configuration defines a WASM plugin named 'greeter' that generates code in the 'gen' directory. It specifies the plugin's WebAssembly module URL and SHA256 hash for security. The plugin receives 'en-US' as a language option for code generation.

```yaml
version: "2"
plugins:
  - name: greeter
    wasm:
      url: https://github.com/sqlc-dev/sqlc-gen-greeter/releases/download/v0.1.0/sqlc-gen-greeter.wasm
      sha256: afc486dac2068d741d7a4110146559d12a013fd0286f42a2fc7dcd802424ad07
sql:
  - schema: schema.sql
    queries: query.sql
    engine: postgresql
    codegen:
      - out: gen
        plugin: greeter
        options:
          lang: en-US
```

---

### sqlc Configuration File (YAML)

Source: https://docs.sqlc.dev/en/latest/reference/config

This snippet shows the structure of a typical sqlc configuration file (sqlc.yaml) for version 1. It defines package settings like output paths, query and schema locations, database engine, and various code generation options.

```yaml
version: "1"
packages:
  - name: "db"
    path: "internal/db"
    queries: "./sql/query/"
    schema: "./sql/schema/"
    engine: "postgresql"
    emit_db_tags: false
    emit_prepared_queries: true
    emit_interface: false
    emit_exact_table_names: false
    emit_empty_slices: false
    emit_exported_queries: false
    emit_json_tags: true
    emit_result_struct_pointers: false
    emit_params_struct_pointers: false
    emit_methods_with_db_argument: false
    emit_pointers_for_null_types: false
    emit_enum_valid_method: false
    emit_all_enum_values: false
    build_tags: "some_tag"
    json_tags_case_style: "camel"
    omit_unused_structs: false
    output_batch_file_name: "batch.go"
    output_db_file_name: "db.go"
    output_models_file_name: "models.go"
    output_querier_file_name: "querier.go"
    output_copyfrom_file_name: "copyfrom.go"
    query_parameter_limit: 1
```

---

### Configure sqlc for pgx/v5

Source: https://docs.sqlc.dev/en/latest/_sources/guides/using-go-and-pgx

This YAML configuration tells sqlc to use the pgx/v5 driver for generating Go code. Ensure your sqlc.yaml file includes the 'sql_package' setting.

```yaml
version: "2"sql:
  - engine: "postgresql"
    queries: "query.sql"
    schema: "query.sql"
    gen:
      go:
        package: "db"
        sql_package: "pgx/v5"
        out: "db"
```

---

### Go: Create Book Batch Insert

Source: https://docs.sqlc.dev/en/latest/reference/query-annotations

Go struct, params, and methods for batch inserting books using ':batchone'. Supports PostgreSQL with pgx.

```go
type CreateBookBatchResults struct {
	br  pgx.BatchResults
	ind int
}
type CreateBookParams struct {
	AuthorID int32  `json:"author_id"`
	Isbn     string `json:"isbn"`
}

func (q *Queries) CreateBook(ctx context.Context, arg []CreateBookParams) *CreateBookBatchResults {
	//...
}
func (b *CreateBookBatchResults) QueryRow(f func(int, Book, error)) {
	//...
}
func (b *CreateBookBatchResults) Close() error {
	//...
}
```

---

### Configure process-based plugin for code generation

Source: https://docs.sqlc.dev/en/latest/guides/plugins

This configuration sets up a process-based plugin named 'jsonb' that executes the 'sqlc-gen-json' command to generate code. It directs the output to the 'gen' directory and passes 'indent: " "' and 'filename: codegen.json' as options to the plugin.

```yaml
version: "2"
plugins:
  - name: jsonb
    process:
      cmd: sqlc-gen-json
sql:
  - schema: schema.sql
    queries: query.sql
    engine: postgresql
    codegen:
      - out: gen
        plugin: jsonb
        options:
          indent: "  "
          filename: codegen.json
```

---

### sqlc Configuration - Package Settings (YAML)

Source: https://docs.sqlc.dev/en/latest/_sources/reference/config

This snippet defines a sqlc package configuration in YAML format. It specifies the output path for generated code, directories for SQL queries and schema files, the database engine to use (PostgreSQL), and numerous options for customizing the generated Go code, such as emitting DB tags, prepared queries, and JSON tags.

```yaml
version: "1"
packages:
  - name: "db"
    path: "internal/db"
    queries: "./sql/query/"
    schema: "./sql/schema/"
    engine: "postgresql"
    emit_db_tags: false
    emit_prepared_queries: true
    emit_interface: false
    emit_exact_table_names: false
    emit_empty_slices: false
    emit_exported_queries: false
    emit_json_tags: true
    emit_result_struct_pointers: false
    emit_params_struct_pointers: false
    emit_methods_with_db_argument: false
    emit_pointers_for_null_types: false
    emit_enum_valid_method: false
    emit_all_enum_values: false
    build_tags: "some_tag"
    json_tags_case_style: "camel"
    omit_unused_structs: false
    output_batch_file_name: "batch.go"
    output_db_file_name: "db.go"
    output_models_file_name: "models.go"
    output_querier_file_name: "querier.go"
    output_copyfrom_file_name: "copyfrom.go"
    query_parameter_limit: 1
```

---

### sqlc Configuration (Version 2)

Source: https://docs.sqlc.dev/en/latest/_sources/reference/config

Defines the sqlc configuration for PostgreSQL and MySQL databases, including schema and query paths, generation settings for Go, and database management options.

```yaml
version: "2"
cloud:
  project: "<PROJECT_ID>"
sql:
  - schema: "postgresql/schema.sql"
    queries: "postgresql/query.sql"
    engine: "postgresql"
    gen:
      go:
        package: "authors"
        out: "postgresql"
    database:
      managed: true
    rules:
      - sqlc/db-prepare
  - schema: "mysql/schema.sql"
    queries: "mysql/query.sql"
    engine: "mysql"
    gen:
      go:
        package: "authors"
        out: "mysql"
```

---

### Go Code for Prepared Statements with sqlc

Source: https://docs.sqlc.dev/en/latest/howto/prepared_query

Provides Go code generated by sqlc for interacting with a database using prepared statements. It includes data structures, database connection interfaces, and methods for executing queries.

```go
package db

import (
	"context"
	"database/sql"
	"fmt"
)

type Record struct {
	ID int32
}

type DBTX interface {
	PrepareContext(context.Context, string) (*sql.Stmt, error)
	QueryRowContext(context.Context, string, ...interface{}) *sql.Row
}

func New(db DBTX) *Queries {
	return &Queries{db: db}
}

func Prepare(ctx context.Context, db DBTX) (*Queries, error) {
	q := Queries{db: db}
	var err error
	if q.getRecordStmt, err = db.PrepareContext(ctx, getRecord); err != nil {
		return nil, fmt.Errorf("error preparing query GetRecord: %w", err)
	}
	return &q, nil
}

func (q *Queries) queryRow(ctx context.Context, stmt *sql.Stmt, query string, args ...interface{}) *sql.Row {
	switch {
	case stmt != nil && q.tx != nil:
		return q.tx.StmtContext(ctx, stmt).QueryRowContext(ctx, args...)
	case stmt != nil:
		return stmt.QueryRowContext(ctx, args...)
	default:
		return q.db.QueryRowContext(ctx, query, args...)
	}
}

type Queries struct {
	db            DBTX
	tx            *sql.Tx
	getRecordStmt *sql.Stmt
}

func (q *Queries) WithTx(tx *sql.Tx) *Queries {
	return &Queries{
		db:            tx,
		tx:            tx,
		getRecordStmt: q.getRecordStmt,
	}
}

const getRecord = `-- name: GetRecord :one
SELECT id FROM records
WHERE id = $1
`

func (q *Queries) GetRecord(ctx context.Context, id int32) (int32, error) {
	row := q.queryRow(ctx, q.getRecordStmt, getRecord, id)
	err := row.Scan(&id)
	return id, err
}

```

---

### Python Code Generation Options

Source: https://docs.sqlc.dev/en/latest/reference/config

Configuration options for generating Python code with sqlc. This includes specifying the package name, output directory, and settings for exact table names, synchronous/asynchronous queriers, and Pydantic model generation.

```yaml
package: "your_package_name"
out: "generated/python"
emit_exact_table_names: true
emit_sync_querier: true
emit_async_querier: true
emit_pydantic_models: true
```

---

### sqlc Database URI Configuration

Source: https://docs.sqlc.dev/en/latest/reference/config

Demonstrates configuring a PostgreSQL database connection in sqlc using a connection URI that includes an environment variable for the password.

```yaml
version: "2"
sql:
  - schema: schema.sql
    queries: query.sql
    engine: postgresql
    database:
      uri: postgresql://postgres:${PG_PASSWORD}@localhost:5432/authors
    gen:
      go:
        package: authors
        out: postgresql
```

---

### Environment Variables for Push

Source: https://docs.sqlc.dev/en/latest/reference/changelog

Lists common environment variables automatically included with the `push` command (formerly `upload`) for contextual information.

```bash
GITHUB_REPOSITORY
GITHUB_REF
GITHUB_REF_NAME
GITHUB_REF_TYPE
GITHUB_SHA

```

---

### Analyzing sqlc trace output with Go tool

Source: https://docs.sqlc.dev/en/latest/_sources/reference/environment-variables

Demonstrates how to use the Go tool to analyze the trace output file generated by sqlc when the 'trace' debug mode is enabled. This allows for detailed inspection of execution times and goroutine activity.

```bash
go tool trace trace.out
```

---

### Go sqlc Generated Queries with Transaction Support

Source: https://docs.sqlc.dev/en/latest/howto/transactions

Illustrates the Go code generated by sqlc, including the DBTX interface and the Queries struct with the WithTx method for transaction management.

```go
package tutorial

import (
	"context"
	"database/sql"
)

type DBTX interface {
	ExecContext(context.Context, string, ...interface{}) (sql.Result, error)
	PrepareContext(context.Context, string) (*sql.Stmt, error)
	QueryContext(context.Context, string, ...interface{}) (*sql.Rows, error)
	QueryRowContext(context.Context, string, ...interface{}) *sql.Row
}

func New(db DBTX) *Queries {
	return &Queries{db: db}
}

type Queries struct {
	db DBTX
}

func (q *Queries) WithTx(tx *sql.Tx) *Queries {
	return &Queries{
		db: tx,
	}
}

```

---

### SQL Queries for PostgreSQL

Source: https://docs.sqlc.dev/en/latest/_sources/guides/using-go-and-pgx

A set of SQL statements defining database schema and query operations. These include creating an 'authors' table and defining functions for retrieving, listing, creating, and deleting authors.

```sql
CREATE TABLE authors (
  id   BIGSERIAL PRIMARY KEY,
  name text      NOT NULL,
  bio  text
);

-- name: GetAuthor :one
SELECT * FROM authors
WHERE id = $1 LIMIT 1;

-- name: ListAuthors :many
SELECT * FROM authors
ORDER BY name;

-- name: CreateAuthor :one
INSERT INTO authors (
  name, bio
) VALUES (
  $1, $2
)
RETURNING *;

-- name: DeleteAuthor :exec
DELETE FROM authors
WHERE id = $1;
```

---

### Shell Command for Local Verification

Source: https://docs.sqlc.dev/en/latest/_sources/howto/verify

Runs the 'sqlc verify' command locally to check existing queries against the most recently pushed schema, using 'main' as the reference tag.

```shell
$ sqlc verify --against main
```

---

### SQLC Configuration for db-prepare Rule (Managed PostgreSQL)

Source: https://docs.sqlc.dev/en/latest/_sources/howto/vet

Sets up sqlc to use the 'sqlc/db-prepare' rule with a managed PostgreSQL database. This allows sqlc to automatically create and manage an ephemeral database with the correct schema for query preparation and validation.

```yaml
version: 2
cloud:
  project: "<PROJECT_ID>"
sql:
  - schema: "schema.sql"
    queries: "query.sql"
    engine: "postgresql"
    gen:
      go:
        package: "authors"
        out: "db"
    database:
      managed: true
    rules:
      - sqlc/db-prepare
```

---

### Run sqlc push in GitHub Actions

Source: https://docs.sqlc.dev/en/latest/_sources/howto/ci-cd

A GitHub Actions workflow that runs `sqlc push` on pushes to the main branch. It requires an `SQLC_AUTH_TOKEN` environment variable for authentication with sqlc Cloud.

````yaml
```yaml
name: sqlc
on: [push]
jobs:
  push:
    runs-on: ubuntu-latest
    if: ${{ github.ref == 'refs/heads/main' }}
    steps:
    - uses: actions/checkout@v3
    - uses: sqlc-dev/setup-sqlc@v3
      with:
        sqlc-version: '1.30.0'
    - run: sqlc push
      env:
        SQLC_AUTH_TOKEN: ${{ secrets.SQLC_AUTH_TOKEN }}
````

````

--------------------------------

### SQLC Configuration for db-prepare Rule (PostgreSQL URI)

Source: https://docs.sqlc.dev/en/latest/_sources/howto/vet

Configures sqlc to use the built-in 'sqlc/db-prepare' rule, which validates SQL queries by attempting to prepare them against a PostgreSQL database specified via a URI. Requires an up-to-date schema in the database.

```yaml
version: 2
sql:
  - schema: "schema.sql"
    queries: "query.sql"
    engine: "postgresql"
    gen:
      go:
        package: "authors"
        out: "db"
    database:
      uri: "postgresql://postgres:password@localhost:5432/postgres"
    rules:
      - sqlc/db-prepare
````

---

### Proto Messages for sqlc vet Linting

Source: https://docs.sqlc.dev/en/latest/howto/vet

Defines the protobuf messages used by sqlc vet for accessing query and configuration information within CEL expressions. These include details about the configuration, individual queries, and query parameters.

```proto
message Config
{
  string version = 1;
  string engine = 2 ;
  repeated string schema = 3;
  repeated string queries = 4;
}

message Query
{
  // SQL body
  string sql = 1;
  // Name of the query
  string name = 2;
  // One of "many", "one", "exec", etc.
  string cmd = 3;
  // Query parameters, if any
  repeated Parameter params = 4;
}

message Parameter
{
  int32 number = 1;
}

```

---

### sqlc Migration and Release Notes

Source: https://docs.sqlc.dev/en/latest/reference/changelog

Information regarding the sqlc repository migration and the release notes for version 1.20.0.

````APIDOC
## Release v1.20.0

**Released:** 2023-07-31

### Repository Migration

The `kyleconroy/sqlc` repository has been migrated to `sqlc-dev/sqlc`. All existing links and installation instructions remain functional. For Go tool installations, use the new import path for v1.20.0 and future versions.

**Incorrect Installation:**
```bash
go install github.com/kyleconroy/sqlc/cmd/sqlc@v1.20.0
````

**Correct Installation:**

```bash
go install github.com/sqlc-dev/sqlc/cmd/sqlc@v1.20.0
```

Users encountering issues during the upgrade are encouraged to file bug reports on GitHub.

### Linting with `EXPLAIN` Output

The `sqlc vet` command now supports using `EXPLAIN` output within lint rules. This allows for more sophisticated query analysis, such as verifying index usage.

**Example Lint Rule Configuration:**

```yaml
version: 2
sql:
  - schema: "query.sql"
    queries: "query.sql"
    engine: "postgresql"
    database:
      uri: "postgresql://postgres:postgres@localhost:5432/postgres"
    gen:
      go:
        package: "db"
        out: "db"
    rules:
      - has-index
rules:
  - name: has-index
    rule: >
      query.sql.startsWith("SELECT") &&
      !(postgresql.explain.plan.plans.all(p, has(p.index_name) || p.plans.all(p, has(p.index_name))))
```

**Available `EXPLAIN` Data:**

- For PostgreSQL: `postgresql.explain` (parsed into `PostgreSQLExplain` proto message)
- For MySQL: `mysql.explain` (parsed into `MySQLExplain` proto message)

**`EXPLAIN` Command Execution:**

- PostgreSQL: `EXPLAIN (ANALYZE false, VERBOSE, COSTS, SETTINGS, BUFFERS, FORMAT JSON) ...`
- MySQL: `EXPLAIN FORMAT=JSON ...`

**Debug Mode:**
Set `SQLCDEBUG=dumpexplain=1` environment variable to view the raw `EXPLAIN` JSON output for debugging lint rules.

````

--------------------------------

### Multi-Engine SQL Configuration with Overrides

Source: https://docs.sqlc.dev/en/latest/_sources/reference/config

Demonstrates SQL generation configuration for multiple database engines (PostgreSQL and MySQL) with language-specific generation paths and global overrides.

```APIDOC
## Multi-Engine SQL Configuration

### Description
Configure SQL generation for different database engines, specifying distinct schema, query files, and code generation output directories.

### Structure
Multiple `sql` entries can be defined for different engines:

- `schema` (string): Path to the SQL schema file for the specific engine.
- `queries` (string): Path to the SQL queries file for the specific engine.
- `engine` (string): The database engine (e.g., `postgresql`, `mysql`).
- `gen.go` (mapping): Go code generation settings.
    - `package` (string): Go package name.
    - `out` (string): Output directory for Go code.

### Example Usage
```yaml
version: "2"
sql:
- schema: "postgresql/schema.sql"
  queries: "postgresql/query.sql"
  engine: "postgresql"
  gen:
    go:
      package: "authors"
      out: "postgresql"
- schema: "mysql/schema.sql"
  queries: "mysql/query.sql"
  engine: "mysql"
  gen:
    go:
      package: "authors"
      out: "mysql"
````

````

--------------------------------

### GitHub Actions: Run sqlc verify and push

Source: https://docs.sqlc.dev/en/latest/howto/ci-cd

This workflow runs `sqlc verify` to check schema changes against existing queries and `sqlc push` to upload project details to sqlc Cloud. It's configured to run on every push, with `sqlc push` conditional on the 'main' branch.

```yaml
name: sqlc
on: [push]
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: sqlc-dev/setup-sqlc@v3
      with:
        sqlc-version: '1.30.0'
    - uses: sqlc-dev/action-setup-postgres@master
      with:
        postgres-version: "16"
      id: postgres
    - run: sqlc verify
      env:
        POSTGRESQL_SERVER_URI: ${{ steps.postgres.outputs.connection-uri }}?sslmode=disable
        SQLC_AUTH_TOKEN: ${{ secrets.SQLC_AUTH_TOKEN }}
  push:
    runs-on: ubuntu-latest
    if: ${{ github.ref == 'refs/heads/main' }}
    steps:
    - uses: sqlc-dev/setup-sqlc@v3
      with:
        sqlc-version: '1.30.0'
    - run: sqlc push
      env:
        SQLC_AUTH_TOKEN: ${{ secrets.SQLC_AUTH_TOKEN }}


````

---

### SQL-migrate Up and Down Migration SQL

Source: https://docs.sqlc.dev/en/latest/_sources/howto/ddl

SQL statements for sql-migrate, showing the 'Up' section to create a 'people' table and the 'Down' section to drop it.

```sql
-- +migrate Up
-- SQL in section 'Up' is executed when this migration is applied
CREATE TABLE people (id int);


-- +migrate Down
-- SQL section 'Down' is executed when this migration is rolled back
DROP TABLE people;
```

---

### Add Tags to Push with sqlc

Source: https://docs.sqlc.dev/en/latest/reference/changelog

Demonstrates how to add a tag to a schema or query push to sqlc Cloud. Tags function similarly to Git tags and can be used to reference specific versions for verification.

```bash
$ sqlc push --tag v1.0.0

```

---

### Set Go build tags with `emit_build_tags`

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

The `emit_build_tags` configuration parameter allows users to specify build tags to be added at the beginning of generated Go source files.

```yaml
# sqlc.yaml
version: "1"
schema: "schema.sql"
言語:
  go:
    emit_build_tags: true
    package: "main"
    out: "./generated"
```

---

### Update dependency: github.com/jackc/pgx/v5

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Updates the `github.com/jackc/pgx/v5` dependency from version `5.4.3` to `5.5.0`.

```go
require (
	...
	github.com/jackc/pgx/v5 v5.5.0
	...
)

```

---

### Plugin Configuration

Source: https://docs.sqlc.dev/en/latest/_sources/reference/config

Defines how to configure and execute external plugins within SQLC, specifying their names, environment variables, execution commands, and WASM details.

````APIDOC
## Plugin Configuration

### Description
Configure external plugins to extend SQLC's code generation capabilities.

### Structure
Each plugin configuration is a mapping with the following keys:

- `name` (string): The unique name for this plugin. Required.
- `env` (array of strings): A list of environment variables to pass to the plugin process. Defaults to no environment variables.
- `process` (mapping): Defines how to execute the plugin using a command.
    - `cmd` (string): The executable command to run for this plugin. Required.
    - `format` (string): The expected input/output format for the plugin. Supports `json` and `protobuf`. Defaults to `protobuf`.
- `wasm` (mapping): Specifies details for running a WebAssembly plugin.
    - `url` (string): The URL to fetch the WASM file. Supports `https://` and `file://` schemes. Required.
    - `sha256` (string): The SHA256 checksum of the downloaded WASM file for verification. Required.

### Example Usage
```yaml
version: "2"
plugins:
- name: "py"
  wasm:
    url: "https://github.com/sqlc-dev/sqlc-gen-python/releases/download/v0.16.0-alpha/sqlc-gen-python.wasm"
    sha256: "428476c7408fd4c032da4ec74e8a7344f4fa75e0f98a5a3302f238283b9b95f2"
- name: "js"
  env:
  - PATH
  process:
    cmd: "sqlc-gen-json"
````

````

--------------------------------

### Add script to mirror code to sqlc-gen-go

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Includes a new script designed to mirror code changes to the `sqlc-gen-go` repository, facilitating synchronization between projects.

```bash
# Example script content (conceptual)
# #!/bin/bash
#
# SOURCE_REPO="git@github.com:sqlc-dev/sqlc.git"
# TARGET_REPO="git@github.com:sqlc-dev/sqlc-gen-go.git"
#
# git clone $SOURCE_REPO sqlc-dev
# git clone $TARGET_REPO sqlc-gen-go-dev
#
# # Copy relevant Go code from sqlc-dev to sqlc-gen-go-dev
# cp -r sqlc-dev/internal/compiler/golang sqlc-gen-go-dev/golang
#
# cd sqlc-gen-go-dev
# git add .
# git commit -m "Mirror Go code from sqlc"
# git push origin main

````

---

### Configure Process Plugin for JSON Output

Source: https://docs.sqlc.dev/en/latest/_sources/guides/plugins

This configuration illustrates how to use a process-based plugin for serializing sqlc output to JSON. It defines a plugin named 'jsonb' that executes 'sqlc-gen-json'. Options for indentation and filename are provided, and the 'codegen' section specifies the output directory.

```yaml
version: "2"
plugins:
  - name: jsonb
    process:
      cmd: sqlc-gen-json
sql:
  - schema: schema.sql
    queries: query.sql
    engine: postgresql
    codegen:
      - out: gen
        plugin: jsonb
        options:
          indent: "  "
          filename: codegen.json
```

---

### Generated Go Struct for dbmate Foo

Source: https://docs.sqlc.dev/en/latest/_sources/howto/ddl

A Go struct generated by sqlc for the 'foo' table from a dbmate migration.

```go
package db

type Foo struct {
	Bar int32
}
```

---

### Create Author and Return All Columns (PostgreSQL)

Source: https://docs.sqlc.dev/en/latest/_sources/howto/insert

Inserts a new author and returns all columns of the newly inserted row using PostgreSQL's RETURNING \* syntax. It takes context, name, and bio as input.

```go
package db

import (
	"context"
	"database/sql"
)

const createAuthor = `-- name: CreateAuthor :one
INSERT INTO authors (
  name, bio
) VALUES (
  $1, $2
)
RETURNING *
`

type CreateAuthorParams struct {
	Name string
	Bio  sql.NullString
}

func (q *Queries) CreateAuthor(ctx context.Context, arg CreateAuthorParams) (Author, error) {
	row := q.db.QueryRowContext(ctx, createAuthor, arg.Name, arg.Bio)
	var i Author
	err := row.Scan(&i.ID, &i.Name, &i.Bio)
	return i, err
}

```

---

### JSON Output Configuration

Source: https://docs.sqlc.dev/en/latest/reference/config

Settings for generating JSON output with sqlc, including the output directory, filename, and indentation string.

```yaml
out: "output/json"
filename: "schema.json"
indent: "  "
```

---

### sqlc Lint Rule with EXPLAIN (PostgreSQL)

Source: https://docs.sqlc.dev/en/latest/reference/changelog

A sqlc configuration file snippet defining a lint rule that checks for index usage in PostgreSQL SELECT queries using EXPLAIN output.

```yaml
version: 2
sql:
  - schema: "query.sql"
    queries: "query.sql"
    engine: "postgresql"
    database:
      uri: "postgresql://postgres:postgres@localhost:5432/postgres"
    gen:
      go:
        package: "db"
        out: "db"
    rules:
      - has-index
rules:
  - name: has-index
    rule: >
      query.sql.startsWith("SELECT") &&
      !(postgresql.explain.plan.plans.all(p, has(p.index_name) || p.plans.all(p, has(p.index_name))))
```

---

### Tern Migration SQL

Source: https://docs.sqlc.dev/en/latest/_sources/howto/ddl

SQL statements for Tern migrations, creating a 'comment' table and then dropping it, separated by '---- create above / drop below ----'.

```sql
CREATE TABLE comment (id int NOT NULL, text text NOT NULL);
----
DROP TABLE comment;
```

---

### Enable dumpexplain Output with SQLCDEBUG

Source: https://docs.sqlc.dev/en/latest/_sources/reference/environment-variables

Enables the dumpexplain functionality to print JSON-formatted EXPLAIN results when running sqlc vet. This is useful for debugging and understanding query execution plans within SQLC rules.

```shell
SQLCDEBUG=dumpexplain=1
```

---

### Update dependency: github.com/google/cel-go

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Updates the `github.com/google/cel-go` dependency from version `0.18.1` to `0.18.2`.

```go
require (
	...
	github.com/google/cel-go v0.18.2
	...
)

```

---

### SQL Schema with Prepared Query

Source: https://docs.sqlc.dev/en/latest/_sources/howto/prepared_query

Defines a simple 'records' table and a SQL query named 'GetRecord' intended for use with prepared statements. The query selects a record by its ID.

```sql
CREATE TABLE records (
  id SERIAL PRIMARY KEY
);

-- name: GetRecord :one
SELECT * FROM records
WHERE id = $1;
```

---

### Shell Command for Verification with Specific Tag

Source: https://docs.sqlc.dev/en/latest/_sources/howto/verify

Runs 'sqlc verify' against a specific tag, allowing analysis of queries against a particular version of the schema.

```shell
$ sqlc verify --against [tag]
```

---

### Run Go Tests

Source: https://docs.sqlc.dev/en/latest/_sources/guides/development

Executes all Go tests within the project. Assumes tests are written in Go.

```bash
go test ./...
```

---

### Verify Changes Against a Tag with sqlc

Source: https://docs.sqlc.dev/en/latest/reference/changelog

Shows how to verify changes by comparing the current schema against a previously pushed tag. This is useful for tracking and validating schema evolution.

```bash
$ sqlc verify --against v1.0.0

```

---

### Push Project with a Tag

Source: https://docs.sqlc.dev/en/latest/_sources/howto/push

You can associate a tag with a `push` operation using the `--tag` flag. This tag can be used later with `sqlc verify` for referencing specific pushes. Providing an existing tag will overwrite the previous reference.

```shell
$ sqlc push --tag main
```

---

### Add sqlc-gen-python plugin configuration (JSON)

Source: https://docs.sqlc.dev/en/latest/_sources/guides/migrating-to-sqlc-gen-python

Configuration to add the sqlc-gen-python plugin using its WASM URL and SHA256 hash. This is added to the main sqlc configuration file.

```json
{
  "version": "2",
  "plugins": [
    {
      "name": "py",
      "wasm": {
        "url": "https://downloads.sqlc.dev/plugin/sqlc-gen-python_1.0.0.wasm",
        "sha256": "aca83e1f59f8ffdc604774c2f6f9eb321a2b23e07dc83fc12289d25305fa065b"
      }
    }
  ]
}
```

---

### sqlc.yaml for Enhanced Analysis with Managed PostgreSQL Database

Source: https://docs.sqlc.dev/en/latest/_sources/howto/generate

This configuration enables enhanced query analysis by automatically creating and using a managed ephemeral PostgreSQL database. It includes server configuration with a database URI and specifies that the database should be managed for analysis purposes. The Go code generation settings remain similar.

```yaml
version: "2"
servers:
  - engine: postgresql
    uri: "postgres://locahost:5432/postgres?sslmode=disable"
sql:
  - engine: "postgresql"
    queries: "query.sql"
    schema: "schema.sql"
    database:
      managed: true
    gen:
      go:
        out: "db"
        sql_package: "pgx/v5"
```

---

### List all author rows using PostgreSQL syntax

Source: https://docs.sqlc.dev/en/latest/_sources/howto/select

Generates Go code to retrieve all author rows from the database, ordered by ID.

```sql
-- name: ListAuthors :many
SELECT * FROM authors
ORDER BY id;
```

```go
const listAuthors = `-- name: ListAuthors :many
SELECT id, bio, birth_year FROM authors
ORDER BY id
`

func (q *Queries) ListAuthors(ctx context.Context) ([]Author, error) {
	rows, err := q.db.QueryContext(ctx, listAuthors)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Author
	for rows.Next() {
		var i Author
		if err := rows.Scan(&i.ID, &i.Bio, &i.BirthYear); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
```

---

### Update dependency: golang.org/x/sync

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Updates the `golang.org/x/sync` dependency from version `0.4.0` to `0.5.0`.

```go
require (
	...
	golang.org/x/sync v0.5.0
	...
)

```

---

### Lint Queries with Managed Databases - sqlc

Source: https://docs.sqlc.dev/en/latest/howto/managed-databases

This configuration enables managed databases in sqlc for linting queries, specifically for rules that require database connections like 'EXPLAIN ...'. It includes the 'sqlc/db-prepare' rule.

```yaml
version: "2"
servers:
  - engine: postgresql
    uri: "postgres://localhost:5432/postgres?sslmode=disable"
sql:
  - schema: schema.sql
    queries: query.sql
    engine: postgresql
    database:
      managed: true
    rules:
      - sqlc/db-prepare
```

---

### Add support for MySQL

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Introduces native support for MySQL databases, expanding sqlc's compatibility and allowing analysis of MySQL-specific SQL.

```go
// Conceptual configuration for MySQL
// type Config struct {
// 	// ...
// 	database:
// 		type: "mysql"
// 		// ... connection details for MySQL
// 	// ...
// }

```

---

### Python Code Generation Settings

Source: https://docs.sqlc.dev/en/latest/_sources/reference/config

Configuration options for generating Python code with SQLC, including package naming, output directories, and specific emission behaviors like sync/async queriers and Pydantic models.

```APIDOC
## Python Code Generation

### Description
Configure the generation of Python code from your SQL schema and queries.

### Parameters
#### Configuration Options
- `package` (string) - The package name to use for the generated Python code.
- `out` (string) - The output directory for the generated Python files.
- `emit_exact_table_names` (boolean) - If true, uses the exact table name for generated models. Defaults to `false`.
- `emit_sync_querier` (boolean) - If true, generates a class with synchronous query methods. Defaults to `false`.
- `emit_async_querier` (boolean) - If true, generates a class with asynchronous query methods. Defaults to `false`.
- `emit_pydantic_models` (boolean) - If true, generates Pydantic models. Otherwise, uses dataclasses. Defaults to `false`.
```

---

### Add Project ID to sqlc Configuration

Source: https://docs.sqlc.dev/en/latest/howto/push

This snippet shows how to add your project ID to the sqlc configuration file (`sqlc.yaml`) to enable project uploads.

```yaml
version: "2"
cloud:
  project: "<PROJECT_ID>"
```

---

### PostgreSQL: List Authors by IDs using ANY operator

Source: https://docs.sqlc.dev/en/latest/_sources/howto/select

Demonstrates how SQLC generates Go code to query PostgreSQL authors by a slice of IDs using the ANY operator. It uses the 'pq' package for array type handling.

```sql
CREATE TABLE authors (
  id         SERIAL PRIMARY KEY,
  bio        text   NOT NULL,
  birth_year int    NOT NULL
);

-- name: ListAuthorsByIDs :many
SELECT * FROM authors
WHERE id = ANY($1::int[]);

```

```go
package db

import (
	"context"
	"database/sql"

	"github.com/lib/pq"
)

type Author struct {
	ID        int
	Bio       string
	BirthYear int
}

type DBTX interface {
	QueryContext(context.Context, string, ...interface{}) (*sql.Rows, error)
	QueryRowContext(context.Context, string, ...interface{}) *sql.Row
}

func New(db DBTX) *Queries {
	return &Queries{db: db}
}

type Queries struct {
	db DBTX
}

const listAuthors = `-- name: ListAuthorsByIDs :many
SELECT id, bio, birth_year FROM authors
WHERE id = ANY($1::int[])
`

func (q *Queries) ListAuthorsByIDs(ctx context.Context, ids []int) ([]Author, error) {
	rows, err := q.db.QueryContext(ctx, listAuthors, pq.Array(ids))
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Author
	for rows.Next() {
		var i Author
		if err := rows.Scan(&i.ID, &i.Bio, &i.BirthYear); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

```

---

### Go: Count rows using sqlc

Source: https://docs.sqlc.dev/en/latest/howto/query_count

Provides Go code for interacting with a database using sqlc-generated code. It includes functions to count all authors and to count authors by town, demonstrating typical usage patterns for database queries.

```go
package db

import (
	"context"
	"database/sql"
)

type DBTX interface {
	QueryContext(context.Context, string, ...interface{}) (*sql.Rows, error)
	QueryRowContext(context.Context, string, ...interface{}) *sql.Row
}

func New(db DBTX) *Queries {
	return &Queries{db: db}
}

type Queries struct {
	db DBTX
}

const countAuthors = `-- name: CountAuthors :one
SELECT count(*) FROM authors
`

func (q *Queries) CountAuthors(ctx context.Context) (int, error) {
	row := q.db.QueryRowContext(ctx, countAuthors)
	var i int
	err := row.Scan(&i)
	return i, err
}

const countAuthorsByTown = `-- name: CountAuthorsByTown :many
SELECT hometown, count(*) FROM authors
GROUP BY 1
ORDER BY 1
`

type CountAuthorsByTownRow struct {
	Hometown string
	Count    int
}

func (q *Queries) CountAuthorsByTown(ctx context.Context) ([]CountAuthorsByTownRow, error) {
	rows, err := q.db.QueryContext(ctx, countAuthorsByTown)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []CountAuthorsByTownRow{}
	for rows.Next() {
		var i CountAuthorsByTownRow
		if err := rows.Scan(&i.Hometown, &i.Count); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

```

---

### Verify Queries Against a Tag

Source: https://docs.sqlc.dev/en/latest/howto/verify

Command to verify existing queries against a specific tag (e.g., 'main') in sqlc Cloud. This is useful for checking query compatibility in pull requests or locally.

```bash
$ sqlc verify --against main

```

---

### Apply Global Type and Field Overrides for Go in sqlc

Source: https://docs.sqlc.dev/en/latest/_sources/reference/config

Demonstrates global configuration for Go code generation, including renaming fields (e.g., 'id' to 'Identifier') and overriding database types (e.g., 'pg_catalog.timestamptz' to 'null.Time'), with specific handling for multiple database engines.

```yaml
version: "2"
overrides:
  go:
    rename:
      id: "Identifier"
    overrides:
      - db_type: "pg_catalog.timestamptz"
        nullable: true
        engine: "postgresql"
        go_type:
          import: "gopkg.in/guregu/null.v4"
          package: "null"
          type: "Time"
sql:
  - schema: "postgresql/schema.sql"
    queries: "postgresql/query.sql"
    engine: "postgresql"
    gen:
      go:
        package: "authors"
        out: "postgresql"
  - schema: "mysql/schema.sql"
    queries: "mysql/query.sql"
    engine: "mysql"
    gen:
      go:
        package: "authors"
        out: "mysql"
```

---

### Set SQLC Authentication Token

Source: https://docs.sqlc.dev/en/latest/_sources/howto/push

This command sets the `SQLC_AUTH_TOKEN` environment variable, which is required for authentication when using sqlc Cloud services like `push`. Ensure you replace `sqlc_xxxxxxxx` with your actual auth token.

```shell
export SQLC_AUTH_TOKEN=sqlc_xxxxxxxx
```

---

### Verify Queries Against Most Recent Push

Source: https://docs.sqlc.dev/en/latest/howto/verify

Command to run `sqlc verify` without an `against` argument, which defaults to using the most recently pushed queries for analysis against the current schema.

```bash
$ sqlc verify --against [tag]

```

---

### MySQL/SQLite: List Authors by IDs using sqlc.slice()

Source: https://docs.sqlc.dev/en/latest/_sources/howto/select

Illustrates how SQLC generates Go code for MySQL and SQLite to query authors by a slice of IDs using the IN operator and the `sqlc.slice()` meta-function for dynamic placeholder generation.

```sql
CREATE TABLE authors (
  id         SERIAL PRIMARY KEY,
  bio        text   NOT NULL,
  birth_year int    NOT NULL
);

-- name: ListAuthorsByIDs :many
SELECT * FROM authors
WHERE id IN (sqlc.slice('ids'));

```

```go
package db

import (
	"context"
	"database/sql"
	"fmt"
	"strings"
)

type Author struct {
	ID        int
	Bio       string
	BirthYear int
}

type DBTX interface {
	ExecContext(context.Context, string, ...interface{}) (sql.Result, error)
	PrepareContext(context.Context, string) (*sql.Stmt, error)
	QueryContext(context.Context, string, ...interface{}) (*sql.Rows, error)
	QueryRowContext(context.Context, string, ...interface{}) *sql.Row
}

func New(db DBTX) *Queries {
	return &Queries{db: db}
}

type Queries struct {
	db DBTX
}

func (q *Queries) WithTx(tx *sql.Tx) *Queries {
	return &Queries{
		db: tx,
	}
}

const listAuthorsByIDs = `-- name: ListAuthorsByIDs :many
SELECT id, bio, birth_year FROM authors
WHERE id IN (/*SLICE:ids*/?)
`

func (q *Queries) ListAuthorsByIDs(ctx context.Context, ids []int64) ([]Author, error) {
	sql := listAuthorsByIDs
	var queryParams []interface{}
	if len(ids) == 0 {
		return nil, fmt.Errorf("slice ids must have at least one element")
	}
	for _, v := range ids {
		queryParams = append(queryParams, v)
	}
	sql = strings.Replace(sql, "/*SLICE:ids*/?", strings.Repeat(",?", len(ids))[1:], 1)
	rows, err := q.db.QueryContext(ctx, sql, queryParams...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Author
	for rows.Next() {
		var i Author
		if err := rows.Scan(&i.ID, &i.Bio, &i.BirthYear); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

```

---

### sqlc.yaml for Enhanced Analysis with User's PostgreSQL

Source: https://docs.sqlc.dev/en/latest/howto/generate

This configuration allows sqlc to use your existing PostgreSQL database for enhanced analysis. It requires a database URI, potentially including environment variables for credentials, and Go code generation settings.

```yaml
version: "2"sql:
  - engine: "postgresql"
    queries: "query.sql"
    schema: "schema.sql"
    database:
      uri: "postgres://postgres:${PG_PASSWORD}@localhost:5432/postgres"
    gen:
      go:
        out: "db"
        sql_package: "pgx/v5"

```

---

### Dry Run Project Upload with sqlc push

Source: https://docs.sqlc.dev/en/latest/howto/push

This command performs a dry run of the `sqlc push` operation, showing which files and data would be uploaded without actually sending them. It's useful for verifying the upload content.

```bash
$ sqlc push --dry-run

```

---

### Bulk insert for MySQL (SQL)

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Shows how to use the `:copyfrom` annotation with MySQL for efficient bulk inserts using the LOAD DATA command. It includes a table definition and the insert statement.

```sql
CREATE TABLE foo (a text, b integer, c DATETIME, d DATE);

-- name: InsertValues :copyfrom
INSERT INTO foo (a, b, c, d) VALUES (?, ?, ?, ?);
```

---

### Add `verify` command for queries and migrations

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Introduces a new `verify` command that allows users to check the validity of their SQL queries and database migration files.

```bash
# Example usage of the verify command
# sqlc verify --schema=schema.sql --queries=queries.sql
# sqlc verify --migrations=migrations/

```

---

### Go Structs After Renaming

Source: https://docs.sqlc.dev/en/latest/_sources/howto/rename

This Go code illustrates the structs ('Writer', 'Publisher') after applying the renaming configurations to the default generated struct names.

```go
package db

import (
	"database/sql"
)

type Writer struct {
	ID   int64
	Name string
	Bio  sql.NullString
}

type Publisher struct {
	ID   int64
	Name string
}
```

---

### New workflow for sqlc-gen-python

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Establishes a new continuous integration workflow specifically for the `sqlc-gen-python` project, ensuring its build and test integrity.

```yaml
# .github/workflows/python-build.yml
# name: Python CI
#
# on: [push]
#
# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#     - uses: actions/checkout@v3
#     - name: Set up Python
#       uses: actions/setup-python@v3
#       with:
#         python-version: '3.x'
#     - name: Install dependencies
#       run:
#         python -m pip install --upgrade pip
#         pip install -r requirements.txt
#     - name: Run tests
#       run:
#         pytest
```

---

### Trace SQL execution with go tool trace

Source: https://docs.sqlc.dev/en/latest/reference/environment-variables

Enables tracing of sqlc execution by setting SQLCDEBUG to 'trace=1' or a specific output file like 'trace.out'. The execution trace can then be viewed using the 'go tool trace' command for performance analysis.

```shell
SQLCDEBUG=trace=1
go tool trace trace.out
```

```shell
SQLCDEBUG=trace=name.out
```

---

### SQL Schema Definitions

Source: https://docs.sqlc.dev/en/latest/_sources/howto/verify

Defines two tables, 'users' and 'user_actions', with their respective columns and primary keys. This represents the initial database schema.

```sql
CREATE TABLE users (
  id UUID PRIMARY KEY
);

CREATE TABLE user_actions (
  id UUID PRIMARY KEY,
  user_id UUID NOT NULL,
  action TEXT,
  created_at TIMESTAMP
);
```

---

### Go: Books By Title Year Batch Query

Source: https://docs.sqlc.dev/en/latest/reference/query-annotations

Go struct, params, and methods for batch querying books by title and year using ':batchmany'. Supports PostgreSQL with pgx.

```go
type BooksByTitleYearBatchResults struct {
	br  pgx.BatchResults
	ind int
}
type BooksByTitleYearParams struct {
	Title string `json:"title"`
	Year  int32  `json:"year"`
}

func (q *Queries) BooksByTitleYear(ctx context.Context, arg []BooksByTitleYearParams) *BooksByTitleYearBatchResults {
	//...
}
func (b *BooksByTitleYearBatchResults) Query(f func(int, []Book, error)) {
	//...
}
func (b *BooksByTitleYearBatchResults) Close() error {
	//...
}
```

---

### Configure sqlc vet with custom rules (YAML)

Source: https://docs.sqlc.dev/en/latest/reference/changelog

Defines custom linting rules for sqlc vet using a YAML configuration file. It specifies rules for engine usage, DELETE statements, parameter count, and command types.

```yaml
version: 2sql:
  - schema: "query.sql"
    queries: "query.sql"
    engine: "postgresql"
    gen:
      go:
        package: "authors"
        out: "db"
    rules:
      - no-pg
      - no-delete
      - only-one-param
      - no-exec
rules:
  - name: no-pg
    message: "invalid engine: postgresql"
    rule: |
      config.engine == "postgresql"
  - name: no-delete
    message: "don't use delete statements"
    rule: |
      query.sql.contains("DELETE")
  - name: only-one-param
    message: "too many parameters"
    rule: |
      query.params.size() > 1
  - name: no-exec
    message: "don't use exec"
    rule: |
      query.cmd == "exec"

```

---

### Add nil pointer check to prevent `db/prepare` panic

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Implements a nil pointer check in the `vet` component to prevent a panic that could occur during `db/prepare` operations.

```go
// Example illustrating the need for nil check
// func prepare(db *sql.DB) error {
// 	if if db == nil {
// 		return errors.New("database connection is nil")
// 	}
// 	// ... prepare statement logic
// 	return nil
// }

```

---

### Batch Query Books by Title and Year using :batchmany (PostgreSQL)

Source: https://docs.sqlc.dev/en/latest/_sources/reference/query-annotations

Generates a Go method for batch querying using pgx drivers on PostgreSQL. Returns a batch object with Query and Close methods for retrieving multiple records.

```sql
-- name: BooksByTitleYear :batchmany
SELECT * FROM books
WHERE title = $1 AND year = $2;
```

```go
type BooksByTitleYearBatchResults struct {
	br  pgx.BatchResults
	ind int
}
type BooksByTitleYearParams struct {
	Title string `json:"title"`
	Year  int32  `json:"year"`
}

func (q *Queries) BooksByTitleYear(ctx context.Context, arg []BooksByTitleYearParams) *BooksByTitleYearBatchResults {
	//...
}
func (b *BooksByTitleYearBatchResults) Query(f func(int, []Book, error)) {
	//...
}
func (b *BooksByTitleYearBatchResults) Close() error {
	//...
}
```

---

### Atlas Migration Schema Creation

Source: https://docs.sqlc.dev/en/latest/_sources/howto/ddl

An SQL statement for creating a 'post' table with 'id', 'title', and 'body' columns, as used in Atlas migrations.

```sql
-- Create "post" table
CREATE TABLE "public"."post" ("id" integer NOT NULL, "title" text NULL, "body" text NULL, PRIMARY KEY ("id"));
```

---

### Configure Plugin Environment Variables (YAML)

Source: https://docs.sqlc.dev/en/latest/reference/changelog

This configuration allows sqlc plugins to access specified environment variables. The `SQLC_VERSION` is always provided to the plugin. This is useful for plugins that need external configuration or access to system paths.

```yaml
version: '2'sql:
- schema: schema.sql
  queries: query.sql
  engine: postgresql
  codegen:
  - out: gen
    plugin: test
plugins:
- name: test
  env:
  - PATH
  wasm:
    url: https://github.com/sqlc-dev/sqlc-gen-test/releases/download/v0.1.0/sqlc-gen-test.wasm
    sha256: 138220eae508d4b65a5a8cea555edd155eb2290daf576b7a8b96949acfeb3790

```

---

### SQL Query for User Actions

Source: https://docs.sqlc.dev/en/latest/_sources/howto/verify

A SQL query that selects all columns from 'users' and 'user_actions', joining them on 'user_id' and ordering by 'created_at'. This query is used to test schema changes.

```sql
-- name: GetUserActions :many
SELECT * FROM users u
JOIN user_actions ua ON u.id = ua.user_id
ORDER BY created_at;
```

---

### Add sqlc-gen-python plugin configuration (YAML)

Source: https://docs.sqlc.dev/en/latest/_sources/guides/migrating-to-sqlc-gen-python

Configuration to add the sqlc-gen-python plugin using its WASM URL and SHA256 hash in YAML format. This is added to the main sqlc configuration file.

```yaml
version: "2"
plugins:
  - name: "py"
    wasm:
      url: "https://downloads.sqlc.dev/plugin/sqlc-gen-python_1.0.0.wasm"
      sha256: "aca83e1f59f8ffdc604774c2f6f9eb321a2b23e07dc83fc12289d25305fa065b"
```

---

### UUIDs to github.com/google/uuid

Source: https://docs.sqlc.dev/en/latest/_sources/reference/datatypes

Maps PostgreSQL UUID types to `github.com/google/uuid.UUID`. Requires the `github.com/google/uuid` package.

```sql
CREATE TABLE records (
  id   uuid PRIMARY KEY
);
```

```go
package db

import (
	"github.com/google/uuid"
)

type Author struct {
	ID uuid.UUID
}
```

---

### Create Author and Return All Columns (SQLite)

Source: https://docs.sqlc.dev/en/latest/_sources/howto/insert

Inserts a new author and returns all columns using SQLite's RETURNING \* syntax. This function is suitable for SQLite databases that support the RETURNING clause.

```go
package db

import (
	"context"
	"database/sql"
)

const createAuthor = `-- name: CreateAuthor :one
INSERT INTO authors (
  name, bio
) VALUES (
  ?, ?
)
RETURNING *
`

type CreateAuthorParams struct {
	Name string
	Bio  sql.NullString
}

func (q *Queries) CreateAuthor(ctx context.Context, arg CreateAuthorParams) (Author, error) {
	row := q.db.QueryRowContext(ctx, createAuthor, arg.Name, arg.Bio)
	var i Author
	err := row.Scan(&i.ID, &i.Name, &i.Bio)
	return i, err
}

```

---

### Set SQLC Auth Token Environment Variable

Source: https://docs.sqlc.dev/en/latest/howto/push

This command sets the `SQLC_AUTH_TOKEN` environment variable, which is required for authenticating with sqlc Cloud during project pushes.

```bash
export SQLC_AUTH_TOKEN=sqlc_xxxxxxxx

```

---

### Update dependency: github.com/spf13/cobra

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Updates the `github.com/spf13/cobra` dependency from version `1.7.0` to `1.8.0`.

```go
require (
	...
	github.com/spf13/cobra v1.8.0
	...
)

```

---

### SQL Schema and Query for Preparing Statements

Source: https://docs.sqlc.dev/en/latest/howto/prepared_query

Defines a simple 'records' table and a 'GetRecord' SQL query to retrieve a record by its ID. This is used in conjunction with sqlc's prepared statement generation.

```sql
CREATE TABLE records (
  id SERIAL PRIMARY KEY
);

-- name: GetRecord :one
SELECT * FROM records
WHERE id = $1;

```

---

### Batch Delete Books using :batchexec (PostgreSQL)

Source: https://docs.sqlc.dev/en/latest/_sources/reference/query-annotations

Generates a Go method for batch deletion using pgx drivers on PostgreSQL. Returns a batch object with Exec and Close methods.

```sql
-- name: DeleteBook :batchexec
DELETE FROM books
WHERE book_id = $1;
```

```go
type DeleteBookBatchResults struct {
	br  pgx.BatchResults
	ind int
}

func (q *Queries) DeleteBook(ctx context.Context, bookID []int32) *DeleteBookBatchResults {
	//...
}
func (b *DeleteBookBatchResults) Exec(f func(int, error)) {
	//...
}
func (b *DeleteBookBatchResults) Close() error {
	//...
}
```

---

### PostgreSQL COPY FROM SQLC Configuration

Source: https://docs.sqlc.dev/en/latest/howto/insert

SQLC configuration file snippet specifying PostgreSQL as the engine and 'pgx/v5' as the SQL package for COPY FROM operations.

```yaml
version: "2"
sql:
  - engine: "postgresql"
    queries: "query.sql"
    schema: "query.sql"
    gen:
      go:
        package: "db"
        sql_package: "pgx/v5"
        out: "db"
```

---

### Go: List Authors Query

Source: https://docs.sqlc.dev/en/latest/reference/query-annotations

Go function to query and return a slice of Author records.

```go
func (q *Queries) ListAuthors(ctx context.Context) ([]Author, error) {
	rows, err := q.db.QueryContext(ctx, listAuthors)
	// ...
}
```

---

### Handle simplified `CASE` statements in MySQL

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Improves the MySQL engine's ability to parse and understand simplified `CASE` statement syntax.

```go
-- Example of a simplified CASE statement in MySQL
/*
SELECT
  CASE status
    WHEN 1 THEN 'Active'
    WHEN 0 THEN 'Inactive'
    ELSE 'Unknown'
  END as status_text
FROM orders;
*/

```

---

### Search `SELECT` and `UPDATE` the same way

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Standardizes the search mechanism for `SELECT` and `UPDATE` statements within the compiler, ensuring consistent behavior.

```go
// Conceptual search logic
// func findStatement(sql string, statementType string) Statement {
// 	// ... logic to find SELECT or UPDATE statements
// 	// Ensure the logic treats both statement types similarly
// }

```

---

### Dry Run sqlc Push

Source: https://docs.sqlc.dev/en/latest/_sources/howto/push

The `--dry-run` flag for the `sqlc push` command allows you to see what files and data sqlc would upload without actually sending the data. This is useful for verifying the contents of your project upload.

```shell
$ sqlc push --dry-run
2023/11/21 10:39:51 INFO config file=sqlc.yaml bytes=912
2023/11/21 10:39:51 INFO codegen_request queryset=app file=codegen_request.pb
2023/11/21 10:39:51 INFO schema queryset=app file=migrations/00001_initial.sql bytes=3033
2023/11/21 10:39:51 INFO query queryset=app file=queries/app.sql bytes=1150
```

---

### Generate Go Code for PostgreSQL Batch Inserts (:batchone)

Source: https://docs.sqlc.dev/en/latest/_sources/reference/query-annotations

Generates Go code for batch inserting data into PostgreSQL using the pgx/v4 or pgx/v5 drivers. The generated methods allow for querying results and closing batch operations.

```sql
-- name: CreateBook :batchone
INSERT INTO books (
    author_id,
    isbn
) VALUES (
    $1,
    $2
)
RETURNING book_id, author_id, isbn

```

```go
type CreateBookBatchResults struct {
	br  pgx.BatchResults
	ind int
}
type CreateBookParams struct {
	AuthorID int32  `json:"author_id"`
	Isbn     string `json:"isbn"`
}

func (q *Queries) CreateBook(ctx context.Context, arg []CreateBookParams) *CreateBookBatchResults {
	//...
}
func (b *CreateBookBatchResults) QueryRow(f func(int, Book, error)) {
	//...
}
func (b *CreateBookBatchResults) Close() error {
	//...
}

```

---

### Bump Go version in CI and Dockerfile

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Updates the Go version used in continuous integration workflows and the project's Dockerfile to `1.21.4`.

```yaml
# .github/workflows/main.yml
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-go@v4
        with:
          go-version: "1.21.4"
```

```dockerfile
# Dockerfile
FROM golang:1.21.4-alpine

WORKDIR /app

COPY go.mod go.sum ./
RUN go mod download

COPY *.go ./
RUN go build -o main .

CMD ["./main"]

```

---

### Configure Per-Package Overrides (sqlc v1)

Source: https://docs.sqlc.dev/en/latest/howto/overrides

Illustrates how per-package configurations are nested within the `packages` list in sqlc version 1. This allows for specific override settings for individual packages, such as database connection details and query paths.

```yaml
version: "1"
packages:
  - name: "db"
    path: "internal/db"
    queries: "./sql/query/"
    schema: "./sql/schema/"
    engine: "postgresql"
    overrides: [...]
```

---

### Migrate Python package config to sqlc.yaml

Source: https://docs.sqlc.dev/en/latest/guides/migrating-to-sqlc-gen-python

Shows the transformation of the Python generation configuration within sqlc.yaml from the old 'gen' format to the new 'codegen' format for the sqlc-gen-python plugin.

```yaml
sql:
  - schema: "schema.sql"
    queries: "query.sql"
    engine: "postgresql"
    gen:
      python:
        out: "src"
        package: "foo"
        emit_sync_querier: true
        emit_async_querier: true
        query_parameter_limit: 5
```

```yaml
sql:
  - schema: "schema.sql"
    queries: "query.sql"
    engine: "postgresql"
    codegen:
      - plugin: "py"
        out: "src"
        options:
          package: "foo"
          emit_sync_querier: true
          emit_async_querier: true
          query_parameter_limit: 5
```

---

### Go Struct for Named Parameters

Source: https://docs.sqlc.dev/en/latest/_sources/howto/named_parameters

Presents the Go struct generated by sqlc when using named parameters (`sqlc.arg()`). The field names (`SetName`, `Name`) are directly derived from the SQL arguments, making them more descriptive.

```go
type UpdateAuthorNameParams struct {
	SetName bool   `json:"set_name"`
	Name    string `json:"name"`
}
```

---

### sqlc.yaml for Enhanced Analysis with Managed PostgreSQL

Source: https://docs.sqlc.dev/en/latest/howto/generate

This configuration enables enhanced analysis by using a managed ephemeral PostgreSQL database. It specifies the database engine and connection URI, alongside Go code generation settings.

```yaml
version: "2"
servers:
- engine: postgresql
  uri: "postgres://locahost:5432/postgres?sslmode=disable"sql:
  - engine: "postgresql"
    queries: "query.sql"
    schema: "schema.sql"
    database:
      managed: true
    gen:
      go:
        out: "db"
        sql_package: "pgx/v5"

```

---

### Migrate Python package config to sqlc.json

Source: https://docs.sqlc.dev/en/latest/guides/migrating-to-sqlc-gen-python

Shows the transformation of the Python generation configuration within sqlc.json from the old 'gen' format to the new 'codegen' format for the sqlc-gen-python plugin.

```json
  "sql": [
    {
      "schema": "schema.sql",
      "queries": "query.sql",
      "engine": "postgresql",
      "gen": {
        "python": {
          "out": "src",
          "package": "foo",
          "emit_sync_querier": true,
          "emit_async_querier": true,
          "query_parameter_limit": 5
        }
      }
    }
  ]

```

```json
  "sql": [
    {
      "schema": "schema.sql",
      "queries": "query.sql",
      "engine": "postgresql",
      "codegen": [
        {
          "out": "src",
          "plugin": "py",
          "options": {
            "package": "authors",
            "emit_sync_querier": true,
            "emit_async_querier": true,
            "query_parameter_limit": 5
          }
        }
      ]
    }
  ]

```

---

### Go: Delete Book Batch Execution

Source: https://docs.sqlc.dev/en/latest/reference/query-annotations

Go struct and method for batch deleting books using ':batchexec'. Supports PostgreSQL with pgx.

```go
type DeleteBookBatchResults struct {
	br  pgx.BatchResults
	ind int
}

func (q *Queries) DeleteBook(ctx context.Context, bookID []int32) *DeleteBookBatchResults {
	//...
}
func (b *DeleteBookBatchResults) Exec(f func(int, error)) {
	//...
}
func (b *DeleteBookBatchResults) Close() error {
	//...
}
```

---

### Generated Go Struct for Atlas Post

Source: https://docs.sqlc.dev/en/latest/_sources/howto/ddl

A Go struct generated by sqlc for the 'post' table from an Atlas migration, including nullable string types.

```go
package db

type Post struct {
	ID    int
	Title sql.NullString
	Body  sql.NullString
}
```

---

### Select specific columns for an author (Go)

Source: https://docs.sqlc.dev/en/latest/howto/select

Demonstrates how to select specific columns ('bio' or 'bio', 'birth_year') from the 'authors' table using sqlc. It includes the Go code for methods that return a single string (bio) or a struct containing multiple fields.

```sql
-- name: GetBioForAuthor :one
SELECT bio FROM authors
WHERE id = $1;
```

```sql
-- name: GetInfoForAuthor :one
SELECT bio, birth_year FROM authors
WHERE id = $1;
```

```Go
package db

import (
	"context"
	"database/sql"
)

type DBTX interface {
	QueryRowContext(context.Context, string, ...interface{}) *sql.Row
}

func New(db DBTX) *Queries {
	return &Queries{db: db}
}

type Queries struct {
	db DBTX
}

const getBioForAuthor = `-- name: GetBioForAuthor :one
SELECT bio FROM authors
WHERE id = $1
`

func (q *Queries) GetBioForAuthor(ctx context.Context, id int) (string, error) {
	row := q.db.QueryRowContext(ctx, getBioForAuthor, id)
	var i string
	err := row.Scan(&i)
	return i, err
}

const getInfoForAuthor = `-- name: GetInfoForAuthor :one
SELECT bio, birth_year FROM authors
WHERE id = $1
`

type GetInfoForAuthorRow struct {
	Bio       string
	BirthYear int
}

func (q *Queries) GetInfoForAuthor(ctx context.Context, id int) (GetInfoForAuthorRow, error) {
	row := q.db.QueryRowContext(ctx, getInfoForAuthor, id)
	var i GetInfoForAuthorRow
	err := row.Scan(&i.Bio, &i.BirthYear)
	return i, err
}

```

---

### Create Author and Return ID (SQLite)

Source: https://docs.sqlc.dev/en/latest/_sources/howto/insert

Inserts a new author into the 'authors' table and returns the generated ID using SQLite's RETURNING id syntax. It requires context and author details.

```go
package db

import (
	"context"
	"database/sql"
)

const createAuthorAndReturnId = `-- name: CreateAuthorAndReturnId :one
INSERT INTO authors (
  name, bio
) VALUES (
  ?, ?
)
RETURNING id
`

type CreateAuthorAndReturnIdParams struct {
	Name string
	Bio  sql.NullString
}

func (q *Queries) CreateAuthorAndReturnId(ctx context.Context, arg CreateAuthorAndReturnIdParams) (int64, error) {
	row := q.db.QueryRowContext(ctx, createAuthorAndReturnId, arg.Name, arg.Bio)
	var id int64
	err := row.Scan(&id)
	return id, err
}

```

---

### sqlc Dependency Updates

Source: https://docs.sqlc.dev/en/latest/reference/changelog

This section details the dependency updates and version bumps for the sqlc project during its development cycle.

```APIDOC
## Dependency Updates

This section lists recent dependency updates for the sqlc project.

### Key Updates:
*   Bumped `wasmtime` from v8.0.0 to v11.0.0
*   Updated `golang` from 1.20.6 to 1.21.0
*   Updated `chardet` in `/docs` from 5.1.0 to 5.2.0
*   Updated `github.com/pganalyze/pg_query_go/v4` to a newer version
*   Updated `github.com/jackc/pgx/v5` from 5.4.2 to 5.4.3
*   Updated `pygments` in `/docs` from 2.15.1 to 2.16.1
*   Updated `sphinxcontrib-applehelp` in `/docs` from 1.0.4 to 1.0.7
*   Updated `sphinxcontrib-qthelp` in `/docs` from 1.0.3 to 1.0.6
*   Updated `github.com/google/cel-go` from 0.17.1 to 0.18.0
*   Updated `sphinxcontrib-serializinghtml` in `/docs`
*   Upgraded Go toolchain to 1.21
*   Updated `sphinxcontrib-devhelp` in `/docs` from 1.0.2 to 1.0.5
*   Updated `github.com/bytecodealliance/wasmtime-go` from v11.0.0 to v12.0.0
*   Updated `sphinx-rtd-theme` in `/docs` from 1.2.2 to 1.3.0
*   Updated `sphinxcontrib-htmlhelp` in `/docs` from 2.0.1 to 2.0.4
*   Updated `pytz` in `/docs` from 2023.3 to 2023.3.post1
*   Updated `actions/checkout` from 3 to 4
```

---

### Go struct generation with JSON tags

Source: https://docs.sqlc.dev/en/latest/_sources/howto/structs

Generates a Go 'Author' struct with JSON tags corresponding to the database column names. Requires the 'time' package for timestamp handling.

```go
package db

import (
	"time"
)

type Author struct {
	ID        int       `json:"id"`
	CreatedAt time.Time `json:"created_at"`
}
```

---

### SQL: Create Book Batch Insert Annotation

Source: https://docs.sqlc.dev/en/latest/reference/query-annotations

Annotates a SQL INSERT query with RETURNING clause for batch insertion using ':batchone'. Requires PostgreSQL with pgx driver.

```sql
-- name: CreateBook :batchone
INSERT INTO books (
    author_id,
    isbn
) VALUES (
    $1,
    $2
)
RETURNING book_id, author_id, isbn
```

---

### Pull opts into its own package

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Organizes the Go code generation component by extracting option-related logic into a separate, dedicated package.

```go
// Conceptual structure with a separate options package
// package main
//
// import (
// 	"github.com/sqlc-dev/sqlc/internal/opts"
// 	// ...
// )
//
// func main() {
// 	// ... use options from opts package
// 	cfg := opts.LoadConfig()
// 	// ...
// }

```

---

### Update dependency: github.com/mattn/go-sqlite3

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Updates the `github.com/mattn/go-sqlite3` dependency from version `1.14.17` to `1.14.18`.

```go
require (
	...
	github.com/mattn/go-sqlite3 v1.14.18
	...
)

```

---

### MySQL LOAD DATA SQLC Configuration

Source: https://docs.sqlc.dev/en/latest/howto/insert

SQLC configuration file snippet specifying MySQL as the engine and the 'go-sql-driver/mysql' as the SQL driver for LOAD DATA operations.

```yaml
version: "2"
sql:
  - engine: "mysql"
    queries: "query.sql"
    schema: "query.sql"
    gen:
      go:
        package: "db"
        sql_package: "database/sql"
        sql_driver: "github.com/go-sql-driver/mysql"
        out: "db"
```

---

### Add Project ID to sqlc Configuration

Source: https://docs.sqlc.dev/en/latest/_sources/howto/push

This snippet shows how to add your project ID to the sqlc configuration file (`sqlc.yaml`). This is a necessary step after creating a project to associate it with your sqlc Cloud project.

```yaml
version: "2"
cloud:
  project: "<PROJECT_ID>"
```

---

### JSON Output Settings

Source: https://docs.sqlc.dev/en/latest/_sources/reference/config

Options for configuring the output of codegen requests as JSON files, including the output directory, filename, and indentation.

```APIDOC
## JSON Output Configuration

### Description
Customize the generation of codegen request information into JSON format.

### Parameters
#### Configuration Options
- `out` (string) - The output directory for the generated JSON file.
- `filename` (string) - The name of the generated JSON file. Defaults to `codegen_request.json`.
- `indent` (string) - The string to use for indentation in the JSON document. Defaults to two spaces (`  `).
```

---

### Add glob pattern tests to sqlpath.Glob

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Enhances the `sqlpath.Glob` functionality by adding test cases that specifically cover glob pattern matching for file paths.

```go
// Conceptual test for glob pattern matching
// import (
// 	"testing"
// 	"github.com/sqlc-dev/sqlc/internal/sqlpath"
// )
//
// func TestGlob(t *testing.T) {
// 	matches, err := sqlpath.Glob("*.sql")
// 	if err != nil {
// 		t // handle error
// 	}
// 	// Assert that matches contains expected .sql files
// }

```

---

### List Authors using :many

Source: https://docs.sqlc.dev/en/latest/_sources/reference/query-annotations

Generates a Go method that returns a slice of records via QueryContext. Suitable for SELECT statements that are expected to return multiple rows.

```sql
-- name: ListAuthors :many
SELECT * FROM authors
ORDER BY name;
```

```go
func (q *Queries) ListAuthors(ctx context.Context) ([]Author, error) {

ows, err := q.db.QueryContext(ctx, listAuthors)
	// ...
}
```

---

### Add sqlc-gen-python plugin to sqlc.yaml

Source: https://docs.sqlc.dev/en/latest/guides/migrating-to-sqlc-gen-python

Configuration to add the sqlc-gen-python plugin to your sqlc.yaml file. This involves specifying the plugin name and its WASM URL and SHA256 hash.

```yaml
version: "2"
plugins:
  - name: "py"
    wasm:
      url: "https://downloads.sqlc.dev/plugin/sqlc-gen-python_1.0.0.wasm"
      sha256: "aca83e1f59f8ffdc604774c2f6f9eb321a2b23e07dc83fc12289d25305fa065b
```

---

### Use hosted MySQL databases for tests

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Updates the testing infrastructure to utilize hosted MySQL databases, providing a more reliable and consistent environment for running tests.

```go
// Conceptual test setup using hosted MySQL
// import (
// 	"testing"
// 	"github.com/go-sql-driver/mysql"
// 	// ...
// )
//
// func TestMain(m *testing.M) {
// 	// Connect to hosted MySQL instance
// 	// db, err := sql.Open("mysql", "user:password@tcp(mysql.example.com:3306)/testdb")
// 	// setup schema, run tests, teardown
// }

```

---

### Bulk Insert Values (MySQL with LOAD DATA)

Source: https://docs.sqlc.dev/en/latest/_sources/howto/insert

Performs bulk inserts into the 'foo' table using MySQL's LOAD DATA feature. Errors and duplicate keys are treated as warnings, and insertions continue. Requires specific `sql_package` and `sql_driver` configuration.

```go
func (q *Queries) InsertValues(ctx context.Context, arg []InsertValuesParams) (int64, error) {
	...
}

```

---

### Configure Managed Databases with Environment Variable - sqlc

Source: https://docs.sqlc.dev/en/latest/howto/managed-databases

This configuration enables managed databases in sqlc using an environment variable for the database URI. It requires a running database server and is used for query analysis and linting.

```yaml
version: "2"
servers:
  - engine: postgresql
    uri: ${DATABASE_URI}
sql:
  - schema: schema.sql
    queries: query.sql
    engine: postgresql
    database:
      managed: true
```

---

### Go Struct for Positional Parameters

Source: https://docs.sqlc.dev/en/latest/_sources/howto/named_parameters

Shows the Go struct generated by sqlc for the SQL query using positional parameters. Note the less descriptive field names like `Column1` and `Column2_2`.

```go
type UpdateAuthorNameParams struct {
	Column1   bool   `json:""`
	Column2_2 string `json:"_2"`
}
```

---

### Configure SQL Generation with Rules in sqlc

Source: https://docs.sqlc.dev/en/latest/_sources/reference/config

Specifies SQL generation settings, including schema and query file paths, the database engine, and applies custom validation rules to the generated code.

```yaml
version: "2"
sql:
  - schema: "query.sql"
    queries: "query.sql"
    engine: "postgresql"
    gen:
      go:
        package: "authors"
        out: "db"
    rules:
      - no-pg
      - no-delete
      - only-one-param
      - no-exec
```

---

### Define Author struct and DBTX interface

Source: https://docs.sqlc.dev/en/latest/_sources/howto/select

Defines the Author struct for database rows and the DBTX interface for database operations, used by the generated Go code.

```go
package db

import (
	"context"
	"database/sql"
)

type Author struct {
	ID        int
	Bio       string
	BirthYear int
}

type DBTX interface {
	QueryContext(context.Context, string, ...interface{}) (*sql.Rows, error)
	QueryRowContext(context.Context, string, ...interface{}) *sql.Row
}

func New(db DBTX) *Queries {
	return &Queries{db: db}
}

type Queries struct {
	db DBTX
}
```

---

### SQLC Go Client for Returning Inserted Columns

Source: https://docs.sqlc.dev/en/latest/howto/insert

Go code for the sqlc generated client to execute insert statements that return data. It includes specific structs for parameters and handles scanning the returned row.

```go
package db

import (
	"context"
	"database/sql"
)

const createAuthor = `-- name: CreateAuthor :one
INSERT INTO authors (
  name, bio
) VALUES (
  $1, $2
)
RETURNING id, name, bio
`

type CreateAuthorParams struct {
	Name string
	Bio  sql.NullString
}

func (q *Queries) CreateAuthor(ctx context.Context, arg CreateAuthorParams) (Author, error) {
	row := q.db.QueryRowContext(ctx, createAuthor, arg.Name, arg.Bio)
	var i Author
	err := row.Scan(&i.ID, &i.Name, &i.Bio)
	return i, err
}

const createAuthorAndReturnId = `-- name: CreateAuthorAndReturnId :one
INSERT INTO authors (
  name, bio
) VALUES (
  $1, $2
)
RETURNING id
`

type CreateAuthorAndReturnIdParams struct {
	Name string
	Bio  sql.NullString
}

func (q *Queries) CreateAuthorAndReturnId(ctx context.Context, arg CreateAuthorAndReturnIdParams) (int64, error) {
	row := q.db.QueryRowContext(ctx, createAuthorAndReturnId, arg.Name, arg.Bio)
	var id int64
	err := row.Scan(&id)
	return id, err
}

```

---

### sqlc Code Generation with Plugins (Python)

Source: https://docs.sqlc.dev/en/latest/_sources/reference/config

Configures sqlc to use a WASM plugin for Python code generation, specifying output directory, package, and query emission options.

```yaml
version: "2"
plugins:
  - name: py
    wasm:
      url: https://github.com/sqlc-dev/sqlc-gen-python/releases/download/v0.16.0-alpha/sqlc-gen-python.wasm
      sha256: 428476c7408fd4c032da4ec74e8a7344f4fa75e0f98a5a3302f238283b9b95f2
sql:
  - schema: "schema.sql"
    queries: "query.sql"
    engine: postgresql
    codegen:
      - out: src/authors
        plugin: py
        options:
          package: authors
          emit_sync_querier: true
          emit_async_querier: true
          query_parameter_limit: 5
```

---

### Go struct generation using singular table name

Source: https://docs.sqlc.dev/en/latest/_sources/howto/structs

Generates a Go 'Author' struct from the 'authors' SQL table, using the singular form of the table name. No external dependencies are required for this basic generation.

```go
package db

// Struct names use the singular form of table names
type Author struct {
	ID   int
	Name string
}
```

---

### Generated Go Struct for Tern Comment

Source: https://docs.sqlc.dev/en/latest/_sources/howto/ddl

A Go struct generated by sqlc for the 'comment' table from a Tern migration.

```go
package db

type Comment struct {
	ID   int32
	Text string
}
```

---

### SQL: Books By Title Year Batch Query Annotation

Source: https://docs.sqlc.dev/en/latest/reference/query-annotations

Annotates a SQL SELECT query for batch retrieval of books by title and year using ':batchmany'. Requires PostgreSQL with pgx driver.

```sql
-- name: BooksByTitleYear :batchmany
SELECT * FROM books
WHERE title = $1 AND year = $2;
```

---

### SQLCDEBUG: trace command for performance analysis

Source: https://docs.sqlc.dev/en/latest/_sources/reference/environment-variables

Enables the 'trace' debug mode in sqlc to help diagnose performance issues. The trace output can be directed to a specific file (e.g., 'name.out') and analyzed using the Go 'trace' tool.

```bash
SQLCDEBUG=trace=1
```

```bash
SQLCDEBUG=trace=name.out
```

---

### Configure WASM plugin with environment variable access

Source: https://docs.sqlc.dev/en/latest/guides/plugins

This configuration enables a WASM plugin named 'test' and grants it access to the 'PATH' environment variable. The plugin is defined by its WebAssembly URL and SHA256 hash, specifying its sandboxed execution environment.

```yaml
version: "2"
sql:
  - schema: schema.sql
    queries: query.sql
    engine: postgresql
    codegen:
      - out: gen
        plugin: test
plugins:
  - name: test
    env:
      - PATH
    wasm:
      url: https://github.com/sqlc-dev/sqlc-gen-test/releases/download/v0.1.0/sqlc-gen-test.wasm
      sha256: 138220eae508d4b65a5a8cea555edd155eb2290daf576b7a8b96949acfeb3790
```

---

### Nullable TEXT to \*string with emit_pointers_for_null_types

Source: https://docs.sqlc.dev/en/latest/_sources/reference/datatypes

Explains how to map nullable TEXT columns to Go pointer types (`*string`) using the `emit_pointers_for_null_types` configuration option.

```go
// Configuration example using pointer: true override
// "overrides": [
//   {
//     "column": "*.text",
//     "go_type": "*string"
//   }
// ]
```

---

### Sqlc Verify Command Output

Source: https://docs.sqlc.dev/en/latest/howto/verify

The output from the `sqlc verify` command indicating a failure due to an ambiguous column reference in the `GetUserActions` query after a schema change.

```bash
$ sqlc verify
FAIL: app query.sql

=== Failed
=== FAIL: app query.sql GetUserActions
    ERROR: column reference "created_at" is ambiguous (SQLSTATE 42702)


```

---

### JSON to Go Struct with pgx/v5

Source: https://docs.sqlc.dev/en/latest/_sources/reference/datatypes

Demonstrates mapping JSONB columns to custom Go structs using `pgx/v5` and overrides for automatic marshaling/unmarshaling.

```go
package dto

type BookData struct {
	Genres    []string `json:"genres"`
	Title     string   `json:"title"`
	Published bool     `json:"published"`
}
```

```sql
CREATE TABLE books (
  data jsonb
);
```

```json
{
  "overrides": [
    {
      "column": "books.data",
      "go_type": {
        "import": "example.com/db",
        "package": "dto",
        "type": "BookData",
        "pointer": true
      }
    }
  ]
}
```

```go
package db

import (
	"example.com/db/dto"
)

type Book struct {
    Data *dto.BookData
}
```

---

### SQLCDEBUG: dumpast command for SQL AST

Source: https://docs.sqlc.dev/en/latest/_sources/reference/environment-variables

Enables the 'dumpast' debug mode in sqlc, which displays the generic SQL Abstract Syntax Tree (AST) generated by the parser. This is useful for understanding how sqlc interprets SQL queries before engine-specific processing.

```bash
SQLCDEBUG=dumpast=1
```

---

### Create Author and Return All Columns (PostgreSQL)

Source: https://docs.sqlc.dev/en/latest/howto/insert

SQL statement to insert an author and return all columns of the inserted row using PostgreSQL's RETURNING clause.

```sql
-- name: CreateAuthor :one
INSERT INTO authors (
  name, bio
) VALUES (
  $1, $2
)
RETURNING *;

```

---

### sqlc verify command output

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Demonstrates the command-line output of `sqlc verify` when it detects an ambiguous column reference in a query due to a schema change. It highlights the specific query and the error message.

```sh
$ sqlc verify
FAIL: app query.sql

=== Failed
=== FAIL: app query.sql GetUserActions
    ERROR: column reference "created_at" is ambiguous (SQLSTATE 42702) (SQLSTATE 42702)
```

---

### SQLCDEBUG: dumpcatalog command for database catalog

Source: https://docs.sqlc.dev/en/latest/_sources/reference/environment-variables

Enables the 'dumpcatalog' debug mode in sqlc to output the entire database catalog. This can be extensive for MySQL and PostgreSQL and is subject to change in future versions.

```bash
SQLCDEBUG=dumpcatalog=1
```

---

### Rely on go.mod for Go version

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Configures the CI environment to determine the Go version to use based on the `go.mod` file, ensuring consistency between local development and CI builds.

```yaml
# .github/workflows/main.yml
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-go@v4
        with:
          # This line ensures go.mod is used to determine the Go version
          discover: true
```

---

### Create Author and Return ID (PostgreSQL)

Source: https://docs.sqlc.dev/en/latest/_sources/howto/insert

Inserts a new author and returns only the ID of the newly inserted row using PostgreSQL's RETURNING id syntax. It accepts context and author details.

```go
package db

import (
	"context"
	"database/sql"
)

const createAuthorAndReturnId = `-- name: CreateAuthorAndReturnId :one
INSERT INTO authors (
  name, bio
) VALUES (
  $1, $2
)
RETURNING id
`

type CreateAuthorAndReturnIdParams struct {
	Name string
	Bio  sql.NullString
}

func (q *Queries) CreateAuthorAndReturnId(ctx context.Context, arg CreateAuthorAndReturnIdParams) (int64, error) {
	row := q.db.QueryRowContext(ctx, createAuthorAndReturnId, arg.Name, arg.Bio)
	var id int64
	err := row.Scan(&id)
	return id, err
}

```

---

### Validate `database` config in all cases

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Ensures that the `database` configuration parameter is validated correctly regardless of the specific configuration context.

```go
// Conceptual validation of database configuration
// type Config struct {
// 	database DatabaseConfig
// 	// ...
// }
//
// func ValidateConfig(cfg Config) error {
// 	if cfg.database.Type == "" {
// 		return errors.New("database type is required")
// 	}
// 	// ... other validation logic
// 	return nil
// }

```

---

### Nullable Text Fields to sql.NullString

Source: https://docs.sqlc.dev/en/latest/_sources/reference/datatypes

Illustrates mapping nullable text columns to `sql.NullString` for proper null value handling in Go.

```sql
CREATE TABLE authors (
  id   SERIAL PRIMARY KEY,
  name text   NOT NULL,
  bio  text
);
```

```go
package db

import (
	"database/sql"
)

type Author struct {
	ID   int
	Name string
	Bio  sql.NullString
}
```

---

### Generate Author Name Update Query with Named Parameters

Source: https://docs.sqlc.dev/en/latest/_sources/howto/named_parameters

Illustrates using `sqlc.arg()` for named parameters in SQL, allowing explicit control over the Go struct field names. This improves code readability and maintainability.

```sql
-- name: UpsertAuthorName :one
UPDATE author
SET
  name = CASE WHEN sqlc.arg(set_name)::bool
    THEN sqlc.arg(name)::text
    ELSE name
    END
RETURNING *;
```

---

### Move Go-specific config validation to plugin

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Refactors the Go code generation component by moving Go-specific configuration validation logic into the plugin itself.

```go
// Conceptual refactoring of validation logic
// package golang
//
// func ValidateConfig(cfg Config) error {
// 	// ... validation logic moved here from main compiler
// 	return nil
// }

```

---

### Configure Remote Code Generation with sqlc.json

Source: https://docs.sqlc.dev/en/latest/reference/changelog

Configures sqlc for remote code generation by specifying organization and project IDs in the sqlc.json file. This is used to bridge compatibility gaps where local engines might not function on certain operating systems.

```json
{
  "version": "2",
  "cloud": {
    "organization": "<org-id>",
    "project": "<project-id>",
  },
  ...
}
```

---

### Configure Managed Databases with URI - sqlc

Source: https://docs.sqlc.dev/en/latest/howto/managed-databases

This configuration enables managed databases in sqlc by setting 'managed: true' and providing a database URI within the 'servers' mapping. It requires a running database server and is used for query analysis and linting.

```yaml
version: "2"
servers:
  - engine: postgresql
    uri: "postgres://localhost:5432/postgres?sslmode=disable"
sql:
  - schema: schema.sql
    queries: query.sql
    engine: postgresql
    database:
      managed: true
```

---

### Generated Go Struct for SQL-migrate People

Source: https://docs.sqlc.dev/en/latest/_sources/howto/ddl

A Go struct generated by sqlc for the 'people' table from an sql-migrate migration.

```go
package db

type People struct {
	ID int32
}
```

---

### Configure Python Code Generation in sqlc

Source: https://docs.sqlc.dev/en/latest/_sources/reference/config

Defines settings for generating Python code with sqlc, including package name, output directory, and options for model generation (exact table names, synchronous/asynchronous queriers, Pydantic models).

```yaml
package: "your_package_name"
out: "generated_code"
emit_exact_table_names: true
emit_sync_querier: true
emit_async_querier: false
emit_pydantic_models: true
```

---

### MySQL/SQLite: List Authors by IDs using sqlc.slice()

Source: https://docs.sqlc.dev/en/latest/howto/select

Illustrates how to query MySQL and SQLite using the sqlc.slice() function for array parameters. The generated Go code dynamically constructs the SQL query with the correct number of placeholders.

```sql
CREATE TABLE authors (
  id         SERIAL PRIMARY KEY,
  bio        text   NOT NULL,
  birth_year int    NOT NULL
);

-- name: ListAuthorsByIDs :many
SELECT * FROM authors
WHERE id IN (sqlc.slice('ids'));

```

```go
package db

import (
	"context"
	"database/sql"
	"fmt"
	"strings"
)

type Author struct {
	ID        int
	Bio       string
	BirthYear int
}

type DBTX interface {
	ExecContext(context.Context, string, ...interface{}) (sql.Result, error)
	PrepareContext(context.Context, string) (*sql.Stmt, error)
	QueryContext(context.Context, string, ...interface{}) (*sql.Rows, error)
	QueryRowContext(context.Context, string, ...interface{}) *sql.Row
}

func New(db DBTX) *Queries {
	return &Queries{db: db}
}

type Queries struct {
	db DBTX
}

func (q *Queries) WithTx(tx *sql.Tx) *Queries {
	return &Queries{
		db: tx,
	}
}

const listAuthorsByIDs = `-- name: ListAuthorsByIDs :many
SELECT id, bio, birth_year FROM authors
WHERE id IN (/*SLICE:ids*/?)
`

func (q *Queries) ListAuthorsByIDs(ctx context.Context, ids []int64) ([]Author, error) {
	sql := listAuthorsByIDs
	var queryParams []interface{}
	if len(ids) == 0 {
		return nil, fmt.Errorf("slice ids must have at least one element")
	}
	for _, v := range ids {
		queryParams = append(queryParams, v)
	}
	sql = strings.Replace(sql, "/*SLICE:ids*/?", strings.Repeat(",?", len(ids))[1:], 1)

ows, err := q.db.QueryContext(ctx, sql, queryParams...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Author
	for rows.Next() {
		var i Author
		if err := rows.Scan(&i.ID, &i.Bio, &i.BirthYear); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

```

---

### Bulk Insert Authors (PostgreSQL with COPY FROM)

Source: https://docs.sqlc.dev/en/latest/_sources/howto/insert

Efficiently inserts multiple authors using PostgreSQL's COPY FROM protocol. This requires the 'pgx/v4' or 'pgx/v5' SQL package and is configured via the `sqlc.yaml`.

```go
type CreateAuthorsParams struct {
	Name string
	Bio  string
}

func (q *Queries) CreateAuthors(ctx context.Context, arg []CreateAuthorsParams) (int64, error) {
	...
}

```

---

### Create Author and Return All Columns (SQLite)

Source: https://docs.sqlc.dev/en/latest/howto/insert

SQL statement to insert an author and return all columns of the inserted row using SQLite's RETURNING clause.

```sql
-- name: CreateAuthor :one
INSERT INTO authors (
  name, bio
) VALUES (
  ?, ?
)
RETURNING *;

```

---

### Configure Managed Databases with Direct URI

Source: https://docs.sqlc.dev/en/latest/_sources/howto/managed-databases

This configuration sets up sqlc to use a managed database by disabling the 'uri' key and enabling 'managed: true'. It requires access to a running PostgreSQL server specified in the 'servers' mapping.

```yaml
version: "2"
servers:
  - engine: postgresql
    uri: "postgres://localhost:5432/postgres?sslmode=disable"
sql:
  - schema: schema.sql
    queries: query.sql
    engine: postgresql
    database:
      managed: true
```

---

### Go Struct for Nullable Parameters

Source: https://docs.sqlc.dev/en/latest/_sources/howto/named_parameters

Displays the Go struct generated by sqlc for a query that uses `sqlc.narg()`. The parameters `Name` and `Bio` are generated as `sql.NullString` to handle potential null values.

```go
type UpdateAuthorParams struct {
	Name sql.NullString
	Bio  sql.NullString
	ID   int64
}
```

---

### Update Author Query with Nullable Parameters

Source: https://docs.sqlc.dev/en/latest/_sources/howto/named_parameters

Demonstrates using `sqlc.narg()` to explicitly define nullable parameters in an SQL query. This allows for finer control over nullability than sqlc's default inference.

```sql
-- name: UpdateAuthor :one
UPDATE author
SET
 name = coalesce(sqlc.narg('name'), name),
 bio = coalesce(sqlc.narg('bio'), bio)
WHERE id = sqlc.arg('id')
RETURNING *;
```

---

### golang-migrate SQL Create Table Statement

Source: https://docs.sqlc.dev/en/latest/howto/ddl

A SQL script from a golang-migrate migration file (e.g., 20060102.up.sql) that creates a 'post' table. sqlc parses this to generate Go code.

```sql
CREATE TABLE post (
    id    int NOT NULL,
    title text,
    body  text,
    PRIMARY KEY(id)
);

```

---

### MySQL Bulk Insert Go Function

Source: https://docs.sqlc.dev/en/latest/reference/changelog

Generated Go function to handle bulk inserts for MySQL using the :copyfrom annotation. This function takes a context and a slice of parameters to insert.

```go
func (q *Queries) InsertValues(ctx context.Context, arg []InsertValuesParams) (int64, error) {
	// ... implementation details ...
}

```

---

### SQL Generation Configuration with Rules

Source: https://docs.sqlc.dev/en/latest/_sources/reference/config

Specifies the SQL schema, queries, engine, and code generation settings, including the application of custom validation rules.

````APIDOC
## SQL Generation Configuration

### Description
Configure SQL generation, specifying schema, queries, database engine, and code generation targets, along with custom validation rules.

### Structure
Each SQL configuration is a mapping within the `sql` collection:

- `schema` (string): Path to the SQL schema file. Required.
- `queries` (string): Path to the SQL queries file. Required.
- `engine` (string): The database engine to use (e.g., `postgresql`, `mysql`). Required.
- `gen` (mapping): Code generation settings for specific languages.
    - `go` (mapping): Go language specific generation settings.
        - `package` (string): The Go package name for the generated code. Required.
        - `out` (string): The output directory for the generated Go code. Required.
- `rules` (array of strings): A list of rule names to apply to this SQL configuration.

### Example Usage
```yaml
version: "2"
sql:
- schema: "query.sql"
  queries: "query.sql"
  engine: "postgresql"
  gen:
    go:
      package: "authors"
      out: "db"
  rules:
  - no-pg
  - no-delete
  - only-one-param
  - no-exec
````

````

--------------------------------

### Go Implementation for Deleting Author

Source: https://docs.sqlc.dev/en/latest/_sources/howto/delete

Provides a Go implementation for the 'DeleteAuthor' function using SQLC. It includes the necessary DBTX interface, Queries struct, and the ExecContext call to interact with the database.

```Go
package db

import (
	"context"
	"database/sql"
)

type DBTX interface {
	ExecContext(context.Context, string, ...interface{}) (sql.Result, error)
}

func New(db DBTX) *Queries {
	return &Queries{db: db}
}

type Queries struct {
	db DBTX
}

const deleteAuthor = `-- name: DeleteAuthor :exec
DELETE FROM authors WHERE id = $1
`

func (q *Queries) DeleteAuthor(ctx context.Context, id int) error {
	_, err := q.db.ExecContext(ctx, deleteAuthor, id)
	return err
}
````

---

### Set SQLC_AUTH_TOKEN Environment Variable

Source: https://docs.sqlc.dev/en/latest/reference/changelog

Sets the SQLC_AUTH_TOKEN environment variable, which is required for remote code generation. This token authenticates the user with the remote code generation service.

```shell
export SQLC_AUTH_TOKEN=<token>
```

---

### Verify changes against a specific tag

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Enables verification of schema changes against a previously tagged set of queries. This is useful for comparing current schema against a known baseline.

```bash
$ sqlc verify --against v1.0.0
```

---

### Configure Managed Databases with Environment Variable URI

Source: https://docs.sqlc.dev/en/latest/_sources/howto/managed-databases

This configuration enables sqlc's managed database feature using an environment variable for the database connection string. It's a flexible way to manage credentials and access to a PostgreSQL server.

```yaml
version: "2"
servers:
  - engine: postgresql
    uri: ${DATABASE_URI}
sql:
  - schema: schema.sql
    queries: query.sql
    engine: postgresql
    database:
      managed: true
```

---

### Update Python dependency: certifi

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Updates the `certifi` Python dependency from version `2023.7.22` to `2023.11.17` in the `/docs` directory.

```bash
# requirements.txt (in /docs)
# ...
# certifi==2023.7.22
# ...

# Updated requirements.txt (in /docs)
# ...
# certifi==2023.11.17
# ...

```

---

### Update Python dependency: urllib3

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Updates the `urllib3` Python dependency from version `2.0.7` to `2.1.0` in the `/docs` directory.

```bash
# requirements.txt (in /docs)
# ...
# urllib3==2.0.7
# ...

# Updated requirements.txt (in /docs)
# ...
# urllib3==2.1.0
# ...

```

---

### Configure Query Parameter Limit for Go

Source: https://docs.sqlc.dev/en/latest/reference/changelog

Sets the `query_parameter_limit` configuration option in sqlc. If set to 0, all generated Go methods will use an argument struct, regardless of the number of parameters. By default, methods with more than one parameter use a struct.

```json
{
  "query_parameter_limit": 0
}
```

---

### SQL Query with sqlc.slice for MySQL IN Clause

Source: https://docs.sqlc.dev/en/latest/reference/changelog

Demonstrates the use of `sqlc.slice` for generating dynamic queries with the IN operator, specifically for the MySQL Go driver which does not natively support passing slices. This avoids issues with prepared queries.

```sql
/* name: SelectStudents :many */
SELECT * FROM students
WHERE age IN (sqlc.slice("ages"))
```

---

### SQL table definition with timestamp

Source: https://docs.sqlc.dev/en/latest/_sources/howto/structs

Defines an 'authors' table with an 'id' as the primary key and a 'created_at' timestamp column.

```sql
CREATE TABLE authors (
  id         SERIAL    PRIMARY KEY,
  created_at timestamp NOT NULL
);
```

---

### SQL Schema Modifications (PostgreSQL)

Source: https://docs.sqlc.dev/en/latest/howto/ddl

Demonstrates common SQL commands for altering database schemas, including creating, altering, dropping, and renaming tables. These are parsed by sqlc to generate corresponding Go structs.

```sql
CREATE TABLE authors (
  id          SERIAL PRIMARY KEY,
  birth_year  int    NOT NULL
);

ALTER TABLE authors ADD COLUMN bio text NOT NULL;
ALTER TABLE authors DROP COLUMN birth_year;
ALTER TABLE authors RENAME TO writers;

```

---

### Alternative YAML for Renaming Go Structs

Source: https://docs.sqlc.dev/en/latest/_sources/howto/rename

This YAML configuration provides an alternative way to specify struct renaming for Go, using the 'overrides' section to target 'go' specific renames for 'author' and 'book_publisher'.

```yaml
version: "2"
sql:
  - engine: postgresql
    queries: query.sql
    schema: query.sql
overrides:
  go:
    rename:
      author: Writer
      book_publisher: Publisher
```

---

### Push schema with tags

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Allows users to add tags when pushing schema and queries to sqlc Cloud. Tags can be overwritten and are useful for associating pushes with relevant environment identifiers like git tags or branch names.

```bash
$ sqlc push --tag v1.0.0
```

---

### SQLC Query Annotations - :execresult

Source: https://docs.sqlc.dev/en/latest/reference/query-annotations

The :execresult annotation generates a Go function that returns the sql.Result from ExecContext.

````APIDOC
## GET /api/users/{id}

### Description
Retrieves the details of a specific user by their ID.

### Method
GET

### Endpoint
/api/users/{id}

### Parameters
#### Path Parameters
- **id** (string) - Required - The unique identifier of the user to retrieve.

### Response
#### Success Response (200)
- **id** (string) - The unique identifier for the user.
- **name** (string) - The name of the user.
- **email** (string) - The email address of the user.

#### Response Example
```json
{
  "id": "123e4567-e89b-12d3-a456-426614174000",
  "name": "Jane Doe",
  "email": "jane.doe@example.com"
}
````

````

--------------------------------

### Generate Author Name Update Query with Positional Parameters

Source: https://docs.sqlc.dev/en/latest/_sources/howto/named_parameters

Demonstrates an SQL query using positional parameters (`$1`, `$2`) where sqlc might generate less descriptive Go struct field names. This is the default behavior when parameter names are not explicitly provided.

```sql
-- name: UpsertAuthorName :one
UPDATE author
SET
  name = CASE WHEN $1::bool
    THEN $2::text
    ELSE name
    END
RETURNING *;
````

---

### Use gRPC interface for codegen plugin communication

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Improves the communication mechanism between sqlc and its code generation plugins by adopting the gRPC interface.

```go
// Conceptual gRPC client/server setup
// package plugin
//
// import (
// 	"google.golang.org/grpc"
// 	"path/to/your/proto"
// )
//
// func main() {
// 	conn, err := grpc.Dial("localhost:50051", grpc.WithInsecure())
// 	if err != nil {
// 		// handle error
// 	}
// 	defer conn.Close()
// 	client := proto.NewCodegenClient(conn)
// 	// ... make RPC calls
// }

```

---

### Make struct and field names less confusing

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Improves the clarity of the configuration system by renaming confusing struct and field names within the configuration handling code.

```go
// Conceptual example of renaming for clarity
// type OldConfig struct {
// 	DatabaseURL string
// 	SchemaPath  string
// }
//
// type Config struct {
// 	DBConnString string // Renamed from DatabaseURL
// 	SchemaFiles  []string // Renamed from SchemaPath
// }

```

---

### Configure JSON Output in sqlc

Source: https://docs.sqlc.dev/en/latest/_sources/reference/config

Specifies configuration for generating JSON output from sqlc, including the output directory, filename, and indentation settings.

```yaml
out: "json_output"
filename: "custom_codegen.json"
indent: "  "
```

---

### SQLC Query Annotations - :copyfrom

Source: https://docs.sqlc.dev/en/latest/reference/query-annotations

The :copyfrom annotation is used for efficient bulk insertion of rows, with specific driver and package implementations.

````APIDOC
## POST /api/books/bulk

### Description
Inserts multiple books efficiently using a bulk copy operation.

### Method
POST

### Endpoint
/api/books/bulk

### Parameters
#### Request Body
- **books_data** (string) - Required - Data for bulk insertion, format depends on the specific driver implementation (e.g., CSV).

### Request Example
```csv
author_id,isbn
123e4567-e89b-12d3-a456-426614174000,978-3-16-148410-0
87654321-e89b-12d3-a456-426614174000,978-1-23-456789-7
````

### Response

#### Success Response (200)

- **message** (string) - A confirmation message indicating the bulk insertion process completed.

#### Response Example

```json
{
  "message": "Bulk book insertion completed."
}
```

````

--------------------------------

### Improve Codegen with Managed Database

Source: https://docs.sqlc.dev/en/latest/_sources/howto/managed-databases

This configuration enhances sqlc's code generation by using a managed database. It includes the 'gen' block for Go output, directing the generated code to the 'db' directory.

```yaml
version: '2'
servers:
- engine: postgresql
  uri: "postgres://localhost:5432/postgres?sslmode=disable"
sql:
- schema: schema.sql
  queries: query.sql
  engine: postgresql
  database:
    managed: true
  gen:
    go:
      out: "db"
````

---

### Define DBTX interface and New Queries constructor

Source: https://docs.sqlc.dev/en/latest/_sources/howto/transactions

Defines the `DBTX` interface required by sqlc-generated code and the constructor for the `Queries` struct. The `DBTX` interface specifies methods for database operations that can be executed within a transaction.

```go
package tutorial

import (
	"context"
	"database/sql"
)

type DBTX interface {
	ExecContext(context.Context, string, ...interface{}) (sql.Result, error)
	PrepareContext(context.Context, string) (*sql.Stmt, error)
	QueryContext(context.Context, string, ...interface{}) (*sql.Rows, error)
	QueryRowContext(context.Context, string, ...interface{}) *sql.Row
}

func New(db DBTX) *Queries {
	return &Queries{db: db}
}

type Queries struct {
	db DBTX
}

```

---

### SQLC Query Annotations - :one

Source: https://docs.sqlc.dev/en/latest/reference/query-annotations

The :one annotation generates a Go function that returns a single record via QueryRowContext.

````APIDOC
## GET /api/books/{id}

### Description
Retrieves the details of a specific book by its ID.

### Method
GET

### Endpoint
/api/books/{id}

### Parameters
#### Path Parameters
- **id** (string) - Required - The unique identifier of the book to retrieve.

### Response
#### Success Response (200)
- **id** (string) - The unique identifier for the book.
- **title** (string) - The title of the book.
- **author_id** (string) - The ID of the book's author.
- **genre** (string) - The genre of the book.

#### Response Example
```json
{
  "id": "b0f8e5a7-7d2b-4c3e-8f1a-9a0a1b2c3d4e",
  "title": "The Great Novel",
  "author_id": "123e4567-e89b-12d3-a456-426614174000",
  "genre": "Fiction"
}
````

````

--------------------------------

### Standard SQL Query for Students and Scores

Source: https://docs.sqlc.dev/en/latest/howto/embedding

A standard SQL query to select all columns from both 'students' and 'test_scores' tables, joined by student ID. This is used as a baseline before applying embedding.

```SQL
-- name: ScoreAndTests :many
SELECT students.*, test_scores.*
FROM students
JOIN test_scores ON test_scores.student_id = students.id
WHERE students.id = $1;

````

---

### Insert Author Row (PostgreSQL)

Source: https://docs.sqlc.dev/en/latest/_sources/howto/insert

Inserts a new author into the 'authors' table using PostgreSQL's parameter syntax ($1). This function expects a context and the author's biography.

```go
package db

import (
	"context"
	"database/sql"
)

type DBTX interface {
	ExecContext(context.Context, string, ...interface{}) (sql.Result, error)
}

func New(db DBTX) *Queries {
	return &Queries{db: db}
}

type Queries struct {
	db DBTX
}

const createAuthor = `-- name: CreateAuthor :exec
INSERT INTO authors (bio) VALUES ($1)
`

func (q *Queries) CreateAuthor(ctx context.Context, bio string) error {
	_, err := q.db.ExecContext(ctx, createAuthor, bio)
	return err
}

```

---

### Default Go Structs Generated from SQL Tables

Source: https://docs.sqlc.dev/en/latest/_sources/howto/rename

This Go code shows the default struct types ('Author', 'Publisher') generated by sqlc from the 'authors' and 'book_publishers' SQL tables, respectively.

```go
package db

import (
	"database/sql"
)

type Author struct {
	ID   int64
	Name string
	Bio  sql.NullString
}

type Publisher struct {
	ID   int64
	Name string
}
```

---

### Global Overrides for Code Generation

Source: https://docs.sqlc.dev/en/latest/_sources/reference/config

Applies global overrides for type mapping and field renaming across different database engines and language targets, primarily for Go.

````APIDOC
## Global Overrides

### Description
Define global settings for type overrides and field renaming that apply across multiple SQL configurations and database engines. Primarily supported for Go.

### Structure

- `overrides` (mapping): Contains global overrides for specific languages.
    - `go` (mapping): Global overrides for Go code generation.
        - `rename` (mapping): Renames fields generated from database columns.
            - `original_name` (string): The original database column name.
            - `new_name` (string): The desired Go struct field name.
        - `overrides` (array of mappings): Defines type mappings for database types.
            - `db_type` (string): The database type (e.g., `pg_catalog.timestamptz`). Required.
            - `nullable` (boolean): Whether the database type is nullable. Required.
            - `engine` (string): The database engine this override applies to. Required for multi-engine configurations.
            - `go_type` (mapping): The target Go type definition.
                - `import` (string): The Go import path for the type.
                - `package` (string): The Go package name for the type.
                - `type` (string): The name of the Go type.

### Example Usage
```yaml
version: "2"
overrides:
  go:
    rename:
      id: "Identifier"
    overrides:
    - db_type: "pg_catalog.timestamptz"
      nullable: true
      engine: "postgresql"
      go_type:
        import: "gopkg.in/guregu/null.v4"
        package: "null"
        type: "Time"
````

**Note:** The `engine` field in global type overrides is crucial when defining mappings for multiple database engines. If only one engine is in use, this field defaults to the current engine.

````

--------------------------------

### SQL: Delete Book Batch Execute Annotation

Source: https://docs.sqlc.dev/en/latest/reference/query-annotations

Annotates a SQL DELETE query for batch execution using ':batchexec'. Requires PostgreSQL with pgx driver.

```sql
-- name: DeleteBook :batchexec
DELETE FROM books
WHERE book_id = $1;
````

---

### Configure Go Type Overrides in sqlc.yaml

Source: https://docs.sqlc.dev/en/latest/_sources/howto/overrides

This YAML configuration demonstrates how to set up `sqlc` to override default Go type mappings. It shows how to map a database `uuid` type to `github.com/google/uuid.UUID` and a specific column `users.birthday` to `time.Time`.

```yaml
version: "2"sql:
- schema: "postgresql/schema.sql"
  queries: "postgresql/query.sql"
  engine: "postgresql"
  gen:
    go:
      package: "authors"
      out: "db"
      sql_package: "pgx/v5"
      overrides:
        - db_type: "uuid"
          nullable: true
          go_type:
            import: "github.com/google/uuid"
            type: "UUID"
        - column: "users.birthday"
          go_type: "time.Time"
```

---

### Associate queries with a transaction using WithTx

Source: https://docs.sqlc.dev/en/latest/_sources/howto/transactions

Provides the `WithTx` method for the `Queries` struct, which allows creating a new `Queries` instance bound to a specific transaction. This is crucial for executing multiple operations within a single atomic transaction.

```go
func (q *Queries) WithTx(tx *sql.Tx) *Queries {
	return &Queries{
		db: tx,
	}
}

```

---

### Support `->` and `->>` operators in SQLite engine

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Introduces support for the `->` (json extract) and `->>` (json extract as text) operators in the SQLite engine.

```go
// Example of JSON extraction with -> and ->>
/*
SELECT
  data ->> 'name' AS user_name,
  data -> 'settings' ->> 'theme' AS theme
FROM user_data;
*/

```

---

### Calculate SHA256 if it does not exist

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Adds functionality to calculate the SHA256 hash for files if it is not already present, useful for integrity checks or versioning.

```go
// Conceptual function to calculate SHA256
// import (
// 	"crypto/sha256"
// 	"encoding/hex"
// 	"io/ioutil"
// 	"os"
// )
//
// func CalculateSHA256(filePath string) (string, error) {
// 	data, err := ioutil.ReadFile(filePath)
// 	if err != nil {
// 		return "", err
// 	}
//
// 	hash := sha256.Sum256(data)
// 	return hex.EncodeToString(hash[:]), nil
// }

```

---

### Generate Author Name Update Query with '@' Operator Shortcut

Source: https://docs.sqlc.dev/en/latest/_sources/howto/named_parameters

Shows a concise way to use named parameters in SQL by employing the `@` operator as a shortcut for `sqlc.arg()`. This syntax is not supported in MySQL.

```sql
-- name: UpsertAuthorName :one
UPDATE author
SET
  name = CASE WHEN @set_name::bool
    THEN @name::text
    ELSE name
    END
RETURNING *;
```

---

### Go: Insert Author Last ID

Source: https://docs.sqlc.dev/en/latest/reference/query-annotations

Go function to insert an author and return the generated ID.

```go
func (q *Queries) InsertAuthor(ctx context.Context, name string) (int64, error) {
	_, err := q.db.ExecContext(ctx, insertAuthor, name)
	// ...
}
```

---

### Insert Author Row (MySQL and SQLite)

Source: https://docs.sqlc.dev/en/latest/_sources/howto/insert

Inserts a new author into the 'authors' table using the placeholder '?' syntax common in MySQL and SQLite. This function takes a context and the author's biography.

```go
package db

import (
	"context"
	"database/sql"
)

type DBTX interface {
	ExecContext(context.Context, string, ...interface{}) (sql.Result, error)
}

func New(db DBTX) *Queries {
	return &Queries{db: db}
}

type Queries struct {
	db DBTX
}

const createAuthor = `-- name: CreateAuthor :exec
INSERT INTO authors (bio) VALUES (?)
`

func (q *Queries) CreateAuthor(ctx context.Context, bio string) error {
	_, err := q.db.ExecContext(ctx, createAuthor, bio)
	return err
}

```

---

### sqlc.yaml for Enhanced Analysis with Self-Managed PostgreSQL Database

Source: https://docs.sqlc.dev/en/latest/_sources/howto/generate

This configuration utilizes a self-managed PostgreSQL database for enhanced query analysis. It specifies the database connection string via a `uri`, which can include environment variables like `${PG_PASSWORD}` for credentials. The schema must be up-to-date in the provided database.

```yaml
version: "2"
sql:
  - engine: "postgresql"
    queries: "query.sql"
    schema: "schema.sql"
    database:
      uri: "postgres://postgres:${PG_PASSWORD}@localhost:5432/postgres"
    gen:
      go:
        out: "db"
        sql_package: "pgx/v5"
```

---

### SQLC rename mapping configuration

Source: https://docs.sqlc.dev/en/latest/_sources/reference/config

This YAML snippet demonstrates how to use the `rename` mapping in a SQLC configuration file to specify custom struct field names. It maps the database column `spotify_url` to the struct field name `SpotifyURL`, overriding the default naming convention.

```yaml
version: "1"
packages: [...]
rename:
  spotify_url: "SpotifyURL"
```

---

### SQLC Query Annotations - :batchone

Source: https://docs.sqlc.dev/en/latest/reference/query-annotations

The :batchone annotation is used for PostgreSQL with pgx drivers to execute batch queries that return a single row.

````APIDOC
## POST /api/books/batch/onerow

### Description
Inserts multiple books and returns the generated ID for each in a batch operation.

### Method
POST

### Endpoint
/api/books/batch/onerow

### Parameters
#### Request Body
- **books** (array) - Required - An array of book objects to insert.
  - **author_id** (string) - Required - The ID of the author.
  - **isbn** (string) - Required - The ISBN of the book.

### Request Example
```json
{
  "books": [
    {
      "author_id": "123e4567-e89b-12d3-a456-426614174000",
      "isbn": "978-3-16-148410-0"
    },
    {
      "author_id": "87654321-e89b-12d3-a456-426614174000",
      "isbn": "978-1-23-456789-7"
    }
  ]
}
````

### Response

#### Success Response (200)

- **results** (array) - An array of results, where each element corresponds to a book insertion.
  - **book_id** (string) - The generated ID of the inserted book.
  - **author_id** (string) - The ID of the author.
  - **isbn** (string) - The ISBN of the book.

#### Response Example

```json
{
  "results": [
    {
      "book_id": "a1b2c3d4-e5f6-7890-1234-567890abcdef",
      "author_id": "123e4567-e89b-12d3-a456-426614174000",
      "isbn": "978-3-16-148410-0"
    },
    {
      "book_id": "f0e1d2c3-b4a5-6789-0123-456789abcdef",
      "author_id": "87654321-e89b-12d3-a456-426614174000",
      "isbn": "978-1-23-456789-7"
    }
  ]
}
```

````

--------------------------------

### SQLC Query Annotations - :many

Source: https://docs.sqlc.dev/en/latest/reference/query-annotations

The :many annotation generates a Go function that returns a slice of records via QueryContext.

```APIDOC
## GET /api/books

### Description
Retrieves a list of all books in the collection.

### Method
GET

### Endpoint
/api/books

### Parameters
#### Query Parameters
- **author_id** (string) - Optional - Filters books by author ID.
- **genre** (string) - Optional - Filters books by genre.

### Response
#### Success Response (200)
- **books** (array) - An array of book objects.
  - **id** (string) - The unique identifier for the book.
  - **title** (string) - The title of the book.
  - **author_id** (string) - The ID of the book's author.
  - **genre** (string) - The genre of the book.

#### Response Example
```json
{
  "books": [
    {
      "id": "b0f8e5a7-7d2b-4c3e-8f1a-9a0a1b2c3d4e",
      "title": "The Great Novel",
      "author_id": "123e4567-e89b-12d3-a456-426614174000",
      "genre": "Fiction"
    },
    {
      "id": "c1a9f6b8-8e3c-4d4f-9f2b-0b1a2c3d4e5f",
      "title": "Science Explained",
      "author_id": "87654321-e89b-12d3-a456-426614174000",
      "genre": "Science"
    }
  ]
}
````

````

--------------------------------

### SQLCDEBUG: dumpvetenv command for vet rule variables

Source: https://docs.sqlc.dev/en/latest/_sources/reference/environment-variables

Enables the 'dumpvetenv' debug mode in sqlc, which prints all variables accessible to a 'sqlc vet' rule during its evaluation. This is helpful for debugging validation rules.

```bash
SQLCDEBUG=dumpvetenv=1
````

---

### Improve Codegen with Managed Databases - sqlc

Source: https://docs.sqlc.dev/en/latest/howto/managed-databases

This configuration enables managed databases in sqlc to improve code generation by providing a schema-aware database for analysis. It includes a 'go' output configuration.

```yaml
version: "2"
servers:
  - engine: postgresql
    uri: "postgres://localhost:5432/postgres?sslmode=disable"
sql:
  - schema: schema.sql
    queries: query.sql
    engine: postgresql
    database:
      managed: true
    gen:
      go:
        out: "db"
```

---

### SQLC Query Annotations - :batchexec

Source: https://docs.sqlc.dev/en/latest/reference/query-annotations

The :batchexec annotation is used for PostgreSQL with pgx drivers to execute multiple statements in a batch and return results.

````APIDOC
## POST /api/books/batch/exec

### Description
Executes multiple book deletion operations in a batch.

### Method
POST

### Endpoint
/api/books/batch/exec

### Parameters
#### Request Body
- **book_ids** (array) - Required - An array of book IDs to delete.
  - **item** (string) - The ID of a book to delete.

### Request Example
```json
{
  "book_ids": ["b0f8e5a7-7d2b-4c3e-8f1a-9a0a1b2c3d4e", "c1a9f6b8-8e3c-4d4f-9f2b-0b1a2c3d4e5f"]
}
````

### Response

#### Success Response (200)

- **message** (string) - A confirmation message indicating the batch deletion process has started.

#### Response Example

```json
{
  "message": "Batch book deletion initiated."
}
```

````

--------------------------------

### SQLC Query Annotations - :exec

Source: https://docs.sqlc.dev/en/latest/reference/query-annotations

The :exec annotation generates a Go function that returns the error from ExecContext.

```APIDOC
## POST /api/users

### Description
This endpoint allows for the creation of a new user.

### Method
POST

### Endpoint
/api/users

### Parameters
#### Request Body
- **name** (string) - Required - The name of the user.
- **email** (string) - Required - The email address of the user.

### Request Example
```json
{
  "name": "John Doe",
  "email": "john.doe@example.com"
}
````

### Response

#### Success Response (200)

- **id** (string) - The unique identifier for the newly created user.
- **name** (string) - The name of the user.
- **email** (string) - The email address of the user.

#### Response Example

```json
{
  "id": "123e4567-e89b-12d3-a456-426614174000",
  "name": "John Doe",
  "email": "john.doe@example.com"
}
```

````

--------------------------------

### MySQL CAST Support in Queries

Source: https://docs.sqlc.dev/en/latest/reference/changelog

Demonstrates the use of the CAST function in MySQL queries with sqlc. This feature allows for type casting of columns, providing flexibility in data manipulation.

```sql
CREATE TABLE foo (bar BOOLEAN NOT NULL);

-- name: SelectColumnCast :many
SELECT CAST(bar AS BIGINT) FROM foo;

````

---

### Lint Queries with Managed Database and db-prepare Rule

Source: https://docs.sqlc.dev/en/latest/_sources/howto/managed-databases

This configuration enables sqlc's linting capabilities with managed databases. It includes the 'sqlc/db-prepare' rule, which validates queries by preparing them against the managed database.

```yaml
version: "2"
servers:
  - engine: postgresql
    uri: "postgres://localhost:5432/postgres?sslmode=disable"
sql:
  - schema: schema.sql
    queries: query.sql
    engine: postgresql
    database:
      managed: true
    rules:
      - sqlc/db-prepare
```

---

### PostgreSQL Arrays to Go Slices

Source: https://docs.sqlc.dev/en/latest/_sources/reference/datatypes

Demonstrates mapping PostgreSQL TEXT arrays to Go string slices. Ensures proper handling of array data in Go applications.

```sql
CREATE TABLE places (
  name text   not null,
  tags text[]
);
```

```go
package db

type Place struct {
	Name string
	Tags []string
}
```

---

### Default Field Renaming Algorithm - sqlc

Source: https://docs.sqlc.dev/en/latest/howto/rename

Demonstrates the default algorithm used by sqlc to convert column names to Go struct field names. Underscores are used as delimiters, and each part is capitalized.

```text
account     -> Account
spotify_url -> SpotifyUrl
app_id      -> AppID
```

---

### Support more `UNION`s for MySQL

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Expands the support for `UNION` clauses in SQL statements when targeting MySQL, improving compatibility with complex queries.

```go
-- Example SQL with UNION clauses
/*
SELECT col1 FROM table1
UNION
SELECT col1 FROM table2
UNION ALL
SELECT col1 FROM table3;
*/

```

---

### SQL Query with Ambiguity Fix

Source: https://docs.sqlc.dev/en/latest/_sources/howto/verify

The updated SQL query to resolve the ambiguous 'created_at' column reference by specifying the table alias 'u.created_at'.

```sql
-- name: GetUserActions :many
SELECT * FROM users u
JOIN user_actions ua ON u.id = ua.user_id
ORDER BY u.created_at;
```

---

### Define sqlc linting rules

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

This YAML configuration defines custom linting rules for `sqlc`. It specifies rules for checking the database engine, disallowing DELETE statements, enforcing single parameters, and preventing the use of `exec`. Each rule includes a name, a descriptive message, and a CEL expression to evaluate against queries.

```yaml
version: 2sql:
  - schema: "query.sql"
    queries: "query.sql"
    engine: "postgresql"
    gen:
      go:
        package: "authors"
        out: "db"
    rules:
      - no-pg
      - no-delete
      - only-one-param
      - no-exec
rules:
  - name: no-pg
    message: "invalid engine: postgresql"
    rule: |
      config.engine == "postgresql"
  - name: no-delete
    message: "don't use delete statements"
    rule: |
      query.sql.contains("DELETE")
  - name: only-one-param
    message: "too many parameters"
    rule: |
      query.params.size() > 1
  - name: no-exec
    message: "don't use exec"
    rule: |
      query.cmd == "exec"
```

---

### Delete All Authors using :execresult

Source: https://docs.sqlc.dev/en/latest/_sources/reference/query-annotations

Generates a Go method that returns the sql.Result from ExecContext. Useful for operations where you need information about the execution, like affected rows.

```sql
-- name: DeleteAllAuthors :execresult
DELETE FROM authors;
```

```go
func (q *Queries) DeleteAllAuthors(ctx context.Context) (sql.Result, error) {
	return q.db.ExecContext(ctx, deleteAllAuthors)
}
```

---

### Go Struct for dbmate Foo Table

Source: https://docs.sqlc.dev/en/latest/howto/ddl

A Go struct generated by sqlc for the 'foo' table created by the dbmate migration. It maps the 'bar' column to an int32.

```go
package db

type Foo struct {
	Bar int32
}

```

---

### Support pgvector with pgx in Go

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

sqlc now generates code for the pgvector extension when using pgx in Go. This eliminates the need for custom overrides.

```go
import "github.com/pgvector/pgvector-go"

// Example usage with pgx
// conn, err := pgx.Connect(ctx, os.Getenv("DATABASE_URL"))
// ...
// _, err = conn.Exec(ctx, "CREATE TABLE items (id serial PRIMARY KEY, embedding vector(3));")
// _, err = conn.Exec(ctx, "INSERT INTO items (embedding) VALUES ($1)", []float32{1, 2, 3})

```

---

### Bulk insert for MySQL (Go)

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Provides the Go function signature generated for a bulk insert operation using `:copyfrom` in MySQL. This function handles inserting multiple records efficiently.

```go
func (q *Queries) InsertValues(ctx context.Context, arg []InsertValuesParams) (int64, error) {
	...
}
```

---

### SQL Schema Definitions

Source: https://docs.sqlc.dev/en/latest/howto/embedding

Defines the 'students' and 'test_scores' tables with their respective columns and constraints. This forms the basis for the subsequent SQL queries.

```SQL
CREATE TABLE students (
  id   bigserial PRIMARY KEY,
  name text NOT NULL,
  age  integer NOT NULL
);

CREATE TABLE test_scores (
  student_id bigint NOT NULL,
  score      integer NOT NULL,
  grade      text NOT NULL
);

```

---

### Update author bio with multiple parameters

Source: https://docs.sqlc.dev/en/latest/_sources/howto/update

Updates the 'bio' field for a specific author identified by 'id'. Shows how to handle multiple parameters and structure them in a Go struct.

```sql
-- name: UpdateAuthor :exec
UPDATE authors SET bio = $2
WHERE id = $1
```

```sql
-- name: UpdateAuthor :exec
UPDATE authors SET bio = ?
WHERE id = ?
```

```go
package db

import (
	"context"
	"database/sql"
)

type DBTX interface {
	ExecContext(context.Context, string, ...interface{}) (sql.Result, error)
}

func New(db DBTX) *Queries {
	return &Queries{db: db}
}

type Queries struct {
	db DBTX
}

const updateAuthor = "-- name: UpdateAuthor :exec\nUPDATE authors SET bio = $2\nWHERE id = $1\n"

type UpdateAuthorParams struct {
	ID  int32
	Bio string
}

func (q *Queries) UpdateAuthor(ctx context.Context, arg UpdateAuthorParams) error {
	_, err := q.db.ExecContext(ctx, updateAuthor, arg.ID, arg.Bio)
	return err
}
```

---

### Go Struct for Atlas Post Table

Source: https://docs.sqlc.dev/en/latest/howto/ddl

A Go struct representing the 'post' table as defined in the Atlas migration script. It includes fields for ID, Title, and Body, using sql.NullString for nullable columns.

```go
package db

type Post struct {
	ID    int
	Title sql.NullString
	Body  sql.NullString
}

```

---

### Correct column names in `:copyfrom`

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Fixes an issue in the code generation for `:copyfrom` directives where column names were being incorrectly generated.

```go
-- Example SQL with :copyfrom directive
/*
-- name: InsertBatchData :copyfrom
INSERT INTO my_table (col1, col2)
VALUES (:col1, :col2);
*/

```

---

### Update author bio with single parameter

Source: https://docs.sqlc.dev/en/latest/_sources/howto/update

Updates the 'bio' field for all authors. Demonstrates how to handle a single parameter in SQL queries and its mapping to a Go function.

```sql
-- name: UpdateAuthorBios :exec
UPDATE authors SET bio = $1
```

```sql
-- name: UpdateAuthorBios :exec
UPDATE authors SET bio = ?
```

```go
package db

import (
	"context"
	"database/sql"
)

type DBTX interface {
	ExecContext(context.Context, string, ...interface{}) (sql.Result, error)
}

func New(db DBTX) *Queries {
	return &Queries{db: db}
}

type Queries struct {
	db DBTX
}

const updateAuthorBios = "-- name: UpdateAuthorBios :exec\nUPDATE authors SET bio = $1\n"

func (q *Queries) UpdateAuthorBios(ctx context.Context, bio string) error {
	_, err := q.db.ExecContext(ctx, updateAuthorBios, bio)
	return err
}
```

---

### Use correct func signature for `CommentSyntax` on Windows

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Fixes an issue on Windows where the `CommentSyntax` function used an incorrect signature, potentially leading to errors in comment generation.

```go
// Conceptual signature for CommentSyntax
// // On Windows, paths might use backslashes, requiring specific handling.
// func CommentSyntax(path string) string {
// 	// ... ensure correct signature and handling for Windows paths
// 	return "-- " + path
// }

```

---

### SQL: List Authors Query Annotation

Source: https://docs.sqlc.dev/en/latest/reference/query-annotations

Annotates a SQL query to list all authors, returning multiple records.

```sql
-- name: ListAuthors :many
SELECT * FROM authors
ORDER BY name;
```

---

### Renaming SQL column 'spotify_url' to 'SpotifyURL' in Go

Source: https://docs.sqlc.dev/en/latest/_sources/howto/rename

This YAML configuration demonstrates how to use the 'rename' mapping within the Go generation settings to change a specific SQL column name ('spotify_url') to a desired Go struct field name ('SpotifyURL').

```yaml
version: "2"
sql:
  - schema: "postgresql/schema.sql"
    queries: "postgresql/query.sql"
    engine: "postgresql"
    gen:
      go:
        package: "authors"
        out: "postgresql"
        rename:
          spotify_url: "SpotifyURL"
```

---

### Define Author table and retrieve single/multiple authors (MySQL/SQLite)

Source: https://docs.sqlc.dev/en/latest/howto/select

Defines an 'authors' table and provides two sqlc-annotated queries for MySQL and SQLite: GetAuthor to retrieve a single author by ID and ListAuthors to retrieve all authors. It uses '?' as the parameter placeholder.

```sql
-- name: GetAuthor :one
SELECT * FROM authors
WHERE id = ?;
```

```sql
-- name: ListAuthors :many
SELECT * FROM authors
ORDER BY id;
```

---

### Define Author table and retrieve single/multiple authors (PostgreSQL)

Source: https://docs.sqlc.dev/en/latest/howto/select

Defines an 'authors' table and provides two sqlc-annotated queries for PostgreSQL: GetAuthor to retrieve a single author by ID and ListAuthors to retrieve all authors. It also includes the Go code for the Queries struct and the methods to execute these queries.

```sql
CREATE TABLE authors (
  id         SERIAL PRIMARY KEY,
  bio        text   NOT NULL,
  birth_year int    NOT NULL
);
```

```sql
-- name: GetAuthor :one
SELECT * FROM authors
WHERE id = $1;
```

```sql
-- name: ListAuthors :many
SELECT * FROM authors
ORDER BY id;
```

```Go
package db

import (
	"context"
	"database/sql"
)

type Author struct {
	ID        int
	Bio       string
	BirthYear int
}

type DBTX interface {
	QueryContext(context.Context, string, ...interface{}) (*sql.Rows, error)
	QueryRowContext(context.Context, string, ...interface{}) *sql.Row
}

func New(db DBTX) *Queries {
	return &Queries{db: db}
}

type Queries struct {
	db DBTX
}

const getAuthor = `-- name: GetAuthor :one
SELECT id, bio, birth_year FROM authors
WHERE id = $1
`

func (q *Queries) GetAuthor(ctx context.Context, id int) (Author, error) {
	row := q.db.QueryRowContext(ctx, getAuthor, id)
	var i Author
	err := row.Scan(&i.ID, &i.Bio, &i.BirthYear)
	return i, err
}

const listAuthors = `-- name: ListAuthors :many
SELECT id, bio, birth_year FROM authors
ORDER BY id
`

func (q *Queries) ListAuthors(ctx context.Context) ([]Author, error) {
	rows, err := q.db.QueryContext(ctx, listAuthors)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Author
	for rows.Next() {
		var i Author
		if err := rows.Scan(&i.ID, &i.Bio, &i.BirthYear); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

```

---

### Return error for duplicate Go names

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Enhances the generation process to return an error when duplicate Go names are detected, preventing naming conflicts and ensuring valid Go code.

```go
// Conceptual error handling for duplicate names
// func generateGoCode(schema Schema) ([]File, error) {
// 	// ... check for duplicate Go identifier names
// 	if hasDuplicateGoNames(schema) {
// 		return nil, errors.New("duplicate Go names found")
// 	}
// 	// ... generate code
// 	return files, nil
// }

```

---

### Prevent panic when compiler is nil

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Addresses a panic condition by ensuring the compiler is not nil before performing operations in the compiler component.

```go
// Conceptual check for nil compiler
// func compile(compiler *Compiler) error {
// 	if compiler == nil {
// 		return errors.New("compiler is nil")
// 	}
// 	// ... compilation logic
// 	return nil
// }

```

---

### Delete Author using :exec

Source: https://docs.sqlc.dev/en/latest/_sources/reference/query-annotations

Generates a Go method that returns an error from ExecContext. Used for DELETE, UPDATE, or INSERT statements that don't return values.

```sql
-- name: DeleteAuthor :exec
DELETE FROM authors
WHERE id = $1;
```

```go
func (q *Queries) DeleteAuthor(ctx context.Context, id int64) error {
	_, err := q.db.ExecContext(ctx, deleteAuthor, id)
	return err
}
```

---

### Configure Output File Name for Batch Operations

Source: https://docs.sqlc.dev/en/latest/reference/changelog

Allows configuration of the output file name for batch operations using the `output_batch_file_name` option. Previously, this was always generated in `batch.go`.

```json
{
  "output_batch_file_name": "my_batch.go"
}
```

---

### Modify Database Schema with SQL Statements

Source: https://docs.sqlc.dev/en/latest/_sources/howto/ddl

Demonstrates SQL statements for altering database table schemas, including adding columns, dropping columns, and renaming tables. These are parsed by sqlc.

```sql
CREATE TABLE authors (
  id          SERIAL PRIMARY KEY,
  birth_year  int    NOT NULL
);

ALTER TABLE authors ADD COLUMN bio text NOT NULL;
ALTER TABLE authors DROP COLUMN birth_year;
ALTER TABLE authors RENAME TO writers;
```

---

### Support functions with `OUT` parameters

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Enhances the compiler's ability to handle stored functions that utilize `OUT` parameters, improving support for database procedures.

```go
-- Example function with OUT parameter (PostgreSQL)
/*
CREATE OR REPLACE FUNCTION get_user_count(OUT user_count integer)
RETURNS integer AS $$
BEGIN
  SELECT count(*) INTO user_count FROM users;
END;
$$ LANGUAGE plpgsql;
*/

```

---

### Renaming Go Structs 'author' to 'Writer' and 'book_publisher' to 'Publisher'

Source: https://docs.sqlc.dev/en/latest/_sources/howto/rename

This YAML configuration demonstrates how to rename generated Go structs. It maps the default struct names derived from table names ('author', 'book_publisher') to custom names ('Writer', 'Publisher').

```yaml
version: "1"
packages:
  - path: db
    engine: postgresql
    schema: query.sql
    queries: query.sql
rename:
  author: Writer
  book_publisher: Publisher
```

---

### Opt-out of specific lint rules in SQL queries

Source: https://docs.sqlc.dev/en/latest/_sources/howto/vet

This SQL snippet demonstrates how to disable specific lint rules for a given query using the `@sqlc-vet-disable` annotation. It shows how to disable multiple rules and how they can be split across multiple lines. The input is a SQL query with annotations.

```sql
/* name: GetAuthor :one */
/* @sqlc-vet-disable sqlc/db-prepare no-pg */
SELECT * FROM authors
WHERE id = ? LIMIT 1;
```

```sql
/* name: GetAuthor :one */
/* @sqlc-vet-disable sqlc/db-prepare */
/* @sqlc-vet-disable no-pg */
SELECT * FROM authors
WHERE id = ? LIMIT 1;
```

---

### Account for parameters without parents

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Addresses an issue where parameters lacking a parent element in the SQL AST were not being handled correctly by the compiler.

```go
// Conceptual handling of parameters without parents
// func processParameters(params []Parameter) error {
// 	for _, p := range params {
// 		if p.Parent == nil {
// 			// Handle parameter without parent correctly
// 		}
// 		// ...
// 	}
// 	return nil
// }

```

---

### Support `CASE` expression in SQLite engine

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Adds support for `CASE` expressions within the SQLite engine, improving query analysis capabilities.

```go
// Example of a query with CASE expression
/*
SELECT
  CASE WHEN name = 'Alice' THEN 'Admin' ELSE 'User' END as role
FROM users;
*/

```

---

### Go: Delete All Authors Execution Result

Source: https://docs.sqlc.dev/en/latest/reference/query-annotations

Go function to execute a delete all authors operation and return the sql.Result.

```go
func (q *Queries) DeleteAllAuthors(ctx context.Context) (sql.Result, error) {
	return q.db.ExecContext(ctx, deleteAllAuthors)
}
```

---

### Configure temporary directory for WASM codegen with SQLCTMPDIR

Source: https://docs.sqlc.dev/en/latest/reference/environment-variables

The `SQLCTMPDIR` environment variable allows you to specify a base directory for temporary folders. This setting is relevant only when using WASM-based codegen plugins. If `SQLCTMPDIR` is not set, `os.MkdirTemp` is called with an empty string, using the system's default temporary directory.

```bash
export SQLCTMPDIR=/path/to/your/temp/dir
```

---

### PostgreSQL COPY FROM Statement

Source: https://docs.sqlc.dev/en/latest/howto/insert

SQL statement for bulk insertion into the 'authors' table using PostgreSQL's COPY FROM syntax. It specifies the columns to be inserted.

```sql
-- name: CreateAuthors :copyfrom
INSERT INTO authors (name, bio) VALUES ($1, $2);

```

---

### Go Function Signature for SelectStudents

Source: https://docs.sqlc.dev/en/latest/reference/changelog

The Go function signature generated by sqlc for the `SelectStudents` query. It accepts a slice of integers (`ages`) as input for the dynamic IN clause.

```go
func (q *Queries) SelectStudents(ctx context.Context, ages []int32) ([]Student, error) {
```

---

### Insert Author (MySQL/SQLite)

Source: https://docs.sqlc.dev/en/latest/howto/insert

SQL statement for inserting a bio into the authors table using MySQL and SQLite's placeholder syntax (?).

```sql
-- name: CreateAuthor :exec
INSERT INTO authors (bio) VALUES (?);

```

---

### Opt-out of all lint rules in SQL queries

Source: https://docs.sqlc.dev/en/latest/_sources/howto/vet

This SQL snippet illustrates how to disable all lint rules for a specific query by using the `@sqlc-vet-disable` annotation without any parameters. The input is a SQL query with the general disable annotation.

```sql
/* name: GetAuthor :one */
/* @sqlc-vet-disable */
SELECT * FROM authors
WHERE id = ? LIMIT 1;
```

---

### Create Author and Return ID (PostgreSQL)

Source: https://docs.sqlc.dev/en/latest/howto/insert

SQL statement to insert an author and return only the 'id' of the inserted row using PostgreSQL's RETURNING clause.

```sql
-- name: CreateAuthorAndReturnId :one
INSERT INTO authors (
  name, bio
) VALUES (
  $1, $2
)
RETURNING id;

```

---

### Configure SQLC with go-geos for PostGIS Geometry Types

Source: https://docs.sqlc.dev/en/latest/_sources/reference/datatypes

This snippet shows how to configure SQLC to use the github.com/twpayne/go-geos package for PostGIS geometry types. It involves overriding the default 'geometry' database type to use the 'geos.Geom' Go type and registering the geos context with the pgx connection after it's established.

```SQL
-- Multipolygons in British National Grid (epsg:27700)
create table shapes(
  id serial,
  name varchar,
  geom geometry(Multipolygon, 27700)
);

-- name: GetCentroids :many
SELECT id, name, ST_Centroid(geom)::geometry FROM shapes;
```

```JSON
{
  "version": 2,
  "gen": {
    "go": {
      "overrides": [
        {
          "db_type": "geometry",
          "go_type": {
            "import": "github.com/twpayne/go-geos",
            "package": "geos",
            "pointer": true,
            "type": "Geom"
          },
          "nullable": true
        }
      ]
    }
  }
}
```

```Go
import (
    "github.com/twpayne/go-geos"
    pgxgeos "github.com/twpayne/pgx-geos"
)

// ...

config.AfterConnect = func(ctx context.Context, conn *pgx.Conn) error {
    if err := pgxgeos.Register(ctx, conn, geos.NewContext()); err != nil {
        return err
    }
    return nil
}
```

---

### Update Author by ID (MySQL/SQLite)

Source: https://docs.sqlc.dev/en/latest/howto/update

Generates Go code to update an author's 'bio' based on their 'id' using MySQL and SQLite's '?' parameter syntax.

```SQL
-- name: UpdateAuthor :exec
UPDATE authors SET bio = ?
WHERE id = ?;

```

```Go
package db

import (
	"context"
	"database/sql"
)

type DBTX interface {
	ExecContext(context.Context, string, ...interface{}) (sql.Result, error)
}

func New(db DBTX) *Queries {
	return &Queries{db: db}
}

type Queries struct {
	db DBTX
}

const updateAuthor = `-- name: UpdateAuthor :exec
UPDATE authors SET bio = $2
WHERE id = $1
`

type UpdateAuthorParams struct {
	ID  int32
	Bio string
}

func (q *Queries) UpdateAuthor(ctx context.Context, arg UpdateAuthorParams) error {
	_, err := q.db.ExecContext(ctx, updateAuthor, arg.ID, arg.Bio)
	return err
}

```

---

### PostgreSQL COPY FROM Table Definition

Source: https://docs.sqlc.dev/en/latest/howto/insert

Defines the 'authors' table schema for use with PostgreSQL's COPY FROM command. It includes 'id', 'name', and 'bio' columns.

```sql
CREATE TABLE authors (
  id         SERIAL PRIMARY KEY,
  name       text   NOT NULL,
  bio        text   NOT NULL
);

```

---

### Timestamps to time.Time and sql.NullTime

Source: https://docs.sqlc.dev/en/latest/_sources/reference/datatypes

Maps PostgreSQL timestamp types to Go's `time.Time` and `sql.NullTime` for nullable values. Requires `parseTime=true` for MySQL's go-sql-driver.

```sql
CREATE TABLE authors (
  id         SERIAL    PRIMARY KEY,
  created_at timestamp NOT NULL DEFAULT NOW(),
  updated_at timestamp
);
```

```go
package db

import (
	"database/sql"
	"time"
)

type Author struct {
	ID        int
	CreatedAt time.Time
	UpdatedAt sql.NullTime
}
```

---

### Update Author Bio (MySQL/SQLite)

Source: https://docs.sqlc.dev/en/latest/howto/update

Generates Go code to update the 'bio' field for a single author using MySQL and SQLite's '?' parameter syntax.

```SQL
-- name: UpdateAuthorBios :exec
UPDATE authors SET bio = ?;

```

```Go
package db

import (
	"context"
	"database/sql"
)

type DBTX interface {
	ExecContext(context.Context, string, ...interface{}) (sql.Result, error)
}

func New(db DBTX) *Queries {
	return &Queries{db: db}
}

type Queries struct {
	db DBTX
}

const updateAuthorBios = `-- name: UpdateAuthorBios :exec
UPDATE authors SET bio = $1
`

func (q *Queries) UpdateAuthorBios(ctx context.Context, bio string) error {
	_, err := q.db.ExecContext(ctx, updateAuthorBios, bio)
	return err
}

```

---

### Error Handling for pgx Batch Closed

Source: https://docs.sqlc.dev/en/latest/reference/changelog

Shows how to check if a pgx batch has already been closed using `errors.Is` with the exported `ErrBatchAlreadyClosed` error from the generated package. This provides cleaner error handling compared to string comparison.

```go
errors.Is(err, generated_package.ErrBatchAlreadyClosed)
```

---

### Insert Author using :execlastid

Source: https://docs.sqlc.dev/en/latest/_sources/reference/query-annotations

Generates a Go method that returns the last inserted ID from ExecContext. Ideal for INSERT statements where the primary key is auto-generated.

```sql
-- name: InsertAuthor :execlastid
INSERT INTO authors (name) VALUES (?);
```

```go
func (q *Queries) InsertAuthor(ctx context.Context, name string) (int64, error) {
	_, err := q.db.ExecContext(ctx, insertAuthor, name)
	// ...
}
```

---

### Insert Author (PostgreSQL)

Source: https://docs.sqlc.dev/en/latest/howto/insert

SQL statement for inserting a bio into the authors table using PostgreSQL's placeholder syntax ($1).

```sql
-- name: CreateAuthor :exec
INSERT INTO authors (bio) VALUES ($1);

```

---

### SQL Schema Definition

Source: https://docs.sqlc.dev/en/latest/_sources/howto/embedding

Defines the 'students' and 'test_scores' tables with their respective columns.

```sql
CREATE TABLE students (
  id   bigserial PRIMARY KEY,
  name text NOT NULL,
  age  integer NOT NULL
);

CREATE TABLE test_scores (
  student_id bigint NOT NULL,
  score      integer NOT NULL,
  grade      text NOT NULL
);
```

---

### Create Author Table

Source: https://docs.sqlc.dev/en/latest/howto/insert

Defines the 'authors' table schema with 'id' and 'bio' columns. The 'id' is a serial primary key.

```sql
CREATE TABLE authors (
  id         SERIAL PRIMARY KEY,
  bio        text   NOT NULL
);

```

---

### Update Author by ID (PostgreSQL)

Source: https://docs.sqlc.dev/en/latest/howto/update

Generates Go code to update an author's 'bio' based on their 'id' using PostgreSQL's '$1' and '$2' parameter syntax.

```SQL
-- name: UpdateAuthor :exec
UPDATE authors SET bio = $2
WHERE id = $1;

```

```Go
package db

import (
	"context"
	"database/sql"
)

type DBTX interface {
	ExecContext(context.Context, string, ...interface{}) (sql.Result, error)
}

func New(db DBTX) *Queries {
	return &Queries{db: db}
}

type Queries struct {
	db DBTX
}

const updateAuthor = `-- name: UpdateAuthor :exec
UPDATE authors SET bio = $2
WHERE id = $1
`

type UpdateAuthorParams struct {
	ID  int32
	Bio string
}

func (q *Queries) UpdateAuthor(ctx context.Context, arg UpdateAuthorParams) error {
	_, err := q.db.ExecContext(ctx, updateAuthor, arg.ID, arg.Bio)
	return err
}

```

---

### Configure Global Go Type Override for UUID (sqlc v1)

Source: https://docs.sqlc.dev/en/latest/_sources/howto/overrides

This configuration demonstrates how to set a global override for the 'uuid' database type to use the 'github.com/gofrs/uuid.UUID' Go type within the older version 1 of the sqlc configuration format. Global overrides are placed under the top-level 'overrides' key.

```yaml
version: "1"
packages: [...]
overrides:
  - db_type: "uuid"
    go_type: "github.com/gofrs/uuid.UUID"
```

---

### SQLC Query Annotations - :batchmany

Source: https://docs.sqlc.dev/en/latest/reference/query-annotations

The :batchmany annotation is used for PostgreSQL with pgx drivers to execute batch queries that return multiple rows.

````APIDOC
## POST /api/books/batch/query

### Description
Retrieves multiple books based on title and year in a batch query.

### Method
POST

### Endpoint
/api/books/batch/query

### Parameters
#### Request Body
- **queries** (array) - Required - An array of query parameters.
  - **title** (string) - Required - The title of the book to search for.
  - **year** (integer) - Required - The publication year of the book.

### Request Example
```json
{
  "queries": [
    {
      "title": "The Great Novel",
      "year": 2020
    },
    {
      "title": "Science Explained",
      "year": 2019
    }
  ]
}
````

### Response

#### Success Response (200)

- **results** (array) - An array of results, where each element corresponds to a query in the batch.
  - **books** (array) - An array of book objects matching the query criteria.
    - **id** (string) - The unique identifier for the book.
    - **title** (string) - The title of the book.
    - **author_id** (string) - The ID of the book's author.
    - **genre** (string) - The genre of the book.

#### Response Example

```json
{
  "results": [
    {
      "books": [
        {
          "id": "b0f8e5a7-7d2b-4c3e-8f1a-9a0a1b2c3d4e",
          "title": "The Great Novel",
          "author_id": "123e4567-e89b-12d3-a456-426614174000",
          "genre": "Fiction"
        }
      ]
    },
    {
      "books": [
        {
          "id": "c1a9f6b8-8e3c-4d4f-9f2b-0b1a2c3d4e5f",
          "title": "Science Explained",
          "author_id": "87654321-e89b-12d3-a456-426614174000",
          "genre": "Science"
        }
      ]
    }
  ]
}
```

````

--------------------------------

### SQLCDEBUG: processplugins command to disable plugins

Source: https://docs.sqlc.dev/en/latest/_sources/reference/environment-variables

Disables process-based plugins in sqlc by setting 'processplugins=0'. If any process-based plugins are declared in the configuration, running any sqlc command will result in an error.

```bash
SQLCDEBUG=processplugins=0
````

---

### SQL Queries for Record Operations

Source: https://docs.sqlc.dev/en/latest/howto/transactions

Contains SQL queries for retrieving a single record by ID and updating a record's counter.

```sql
-- name: GetRecord :one
SELECT * FROM records
WHERE id = $1;

-- name: UpdateRecord :exec
UPDATE records SET counter = $2
WHERE id = $1;

```

---

### Configure Global Go Type Overrides for PostgreSQL Timestamps (sqlc v2)

Source: https://docs.sqlc.dev/en/latest/howto/overrides

This snippet shows how to configure global type overrides in sqlc version 2. It maps PostgreSQL's `pg_catalog.timestamptz` to Go's `null.Time` from the `gopkg.in/guregu/null.v4` package, making nullable timestamp types consistent across all generated Go code.

```yaml
version: "2"
overrides:
  go:
    overrides:
      - db_type: "pg_catalog.timestamptz"
        nullable: true
        engine: "postgresql"
        go_type:
          import: "gopkg.in/guregu/null.v4"
          package: "null"
          type: "Time"
sql:
  - schema: "service1/schema.sql"
    queries: "service1/query.sql"
    engine: "postgresql"
    gen:
      go:
        package: "service1"
        out: "service1"
  - schema: "service2/schema.sql"
    queries: "service2/query.sql"
    engine: "postgresql"
    gen:
      go:
        package: "service2"
        out: "service2"
```

---

### sqlc Lint Rule Annotations

Source: https://docs.sqlc.dev/en/latest/reference/changelog

Demonstrates how to disable lint rules for specific queries using annotations.

````APIDOC
## Lint Rule Disabling

### Query Annotation

You can prevent `sqlc vet` from evaluating lint rules for a specific query by adding the `@sqlc-vet-disable` annotation.

**Example Usage:**
```sql
/* name: GetAuthor :one */
/* @sqlc-vet-disable */
SELECT * FROM authors
WHERE id = ? LIMIT 1;
````

````

--------------------------------

### SQLC Query Annotations - :execrows

Source: https://docs.sqlc.dev/en/latest/reference/query-annotations

The :execrows annotation generates a Go function that returns the number of affected rows from ExecContext.

```APIDOC
## PUT /api/users/{id}

### Description
Updates an existing user's information. The request body should contain the fields to be updated.

### Method
PUT

### Endpoint
/api/users/{id}

### Parameters
#### Path Parameters
- **id** (string) - Required - The unique identifier of the user to update.
#### Request Body
- **name** (string) - Optional - The updated name of the user.
- **email** (string) - Optional - The updated email address of the user.

### Request Example
```json
{
  "email": "jane.doe.updated@example.com"
}
````

### Response

#### Success Response (200)

- **id** (string) - The unique identifier for the updated user.
- **name** (string) - The updated name of the user.
- **email** (string) - The updated email address of the user.

#### Response Example

```json
{
  "id": "123e4567-e89b-12d3-a456-426614174000",
  "name": "Jane Doe",
  "email": "jane.doe.updated@example.com"
}
```

````

--------------------------------

### sqlc.embed: Reuse Model Structs in Queries

Source: https://docs.sqlc.dev/en/latest/_sources/reference/macros

The `sqlc.embed` macro allows for the reuse of existing model structs in multiple queries, reducing manual serialization efforts. It enables returning nested structs directly from query results, simplifying data handling in the application.

```sql
-- name: GetStudentAndScore :one
SELECT sqlc.embed(students), sqlc.embed(test_scores)
FROM students
JOIN test_scores ON test_scores.student_id = students.id
WHERE students.id = $1;

-- >>> EXPANDS TO >>>

-- name: GetStudentAndScore :one
SELECT students.*, test_scores.*
FROM students
JOIN test_scores ON test_scores.student_id = students.id
WHERE students.id = $1;

````

```go
type GetStudentAndScoreRow struct {
	Student   Student
	TestScore TestScore
}

func (q *Queries) GetStudentAndScore(ctx context.Context, id int64) (GetStudentAndScoreRow, error) {
    // ...
}

```

---

### SQL table definition for authors

Source: https://docs.sqlc.dev/en/latest/_sources/howto/structs

Defines an 'authors' table with an auto-incrementing primary key 'id' and a non-nullable 'name' field.

```sql
CREATE TABLE authors (
  id   SERIAL PRIMARY KEY,
  name text   NOT NULL
);
```

---

### Update Author Bio (PostgreSQL)

Source: https://docs.sqlc.dev/en/latest/howto/update

Generates Go code to update the 'bio' field for a single author using PostgreSQL's '$1' parameter syntax.

```SQL
-- name: UpdateAuthorBios :exec
UPDATE authors SET bio = $1;

```

```Go
package db

import (
	"context"
	"database/sql"
)

type DBTX interface {
	ExecContext(context.Context, string, ...interface{}) (sql.Result, error)
}

func New(db DBTX) *Queries {
	return &Queries{db: db}
}

type Queries struct {
	db DBTX
}

const updateAuthorBios = `-- name: UpdateAuthorBios :exec
UPDATE authors SET bio = $1
`

func (q *Queries) UpdateAuthorBios(ctx context.Context, bio string) error {
	_, err := q.db.ExecContext(ctx, updateAuthorBios, bio)
	return err
}

```

---

### Create Author and Return ID (SQLite)

Source: https://docs.sqlc.dev/en/latest/howto/insert

SQL statement to insert an author and return only the 'id' of the inserted row using SQLite's RETURNING clause.

```sql
-- name: CreateAuthorAndReturnId :one
INSERT INTO authors (
  name, bio
) VALUES (
  ?, ?
)
RETURNING id;

```

---

### Go: Delete Author Execution

Source: https://docs.sqlc.dev/en/latest/reference/query-annotations

Go function to execute a delete operation on the 'authors' table using the 'deleteAuthor' SQL statement.

```go
func (q *Queries) DeleteAuthor(ctx context.Context, id int64) error {
	_, err := q.db.ExecContext(ctx, deleteAuthor, id)
	return err
}
```

---

### MySQL LOAD DATA Statement

Source: https://docs.sqlc.dev/en/latest/howto/insert

SQL statement for bulk insertion into the 'foo' table using MySQL's LOAD DATA syntax. It specifies the columns to be inserted.

```sql
CREATE TABLE foo (a text, b integer, c DATETIME, d DATE);

-- name: InsertValues :copyfrom
INSERT INTO foo (a, b, c, d) VALUES (?, ?, ?, ?);

```

---

### Never generate return structs for :exec

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Modifies the code generation process to prevent the creation of return structs when using the `:exec` command, simplifying output for execution-only statements.

```go
-- Example SQL with :exec directive
/*
-- name: CreateUser :exec
INSERT INTO users (name, email) VALUES (:name, :email);
*/

```

---

### Define Custom Validation Rules in sqlc

Source: https://docs.sqlc.dev/en/latest/_sources/reference/config

Shows how to define custom validation rules for SQL queries using Common Expression Language (CEL). Each rule includes a name, a CEL expression, and an optional message displayed when the rule evaluates to true.

```yaml
version: "2"
rules:
  - name: no-pg
    message: "invalid engine: postgresql"
    rule: |
      config.engine == "postgresql"
  - name: no-delete
    message: "don't use delete statements"
    rule: |
      query.sql.contains("DELETE")
  - name: only-one-param
    message: "too many parameters"
    rule: |
      query.params.size() > 1
  - name: no-exec
    message: "don't use exec"
    rule: |
      query.cmd == "exec"
```

---

### Remove Go specific overrides from codegen proto

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Enhances the code generation protocol by removing Go-specific override directives, making the protocol more language-agnostic.

```protobuf
// Conceptual proto definition without Go-specific overrides
// syntax = "proto3";
//
// message GenerateRequest {
// 	string schema = 1;
// 	map<string, string> options = 2;
// 	// Removed Go-specific override fields
// }

```

---

### No panic on fully-qualified column names

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Resolves an issue where fully-qualified column names could cause a panic in the compiler. This ensures robustness when dealing with complex schema definitions.

```go
// Example of a fully-qualified column name
/*
SELECT
  my_table.my_column
FROM my_table;
*/

```

---

### SQL Schema for Student Test Scores

Source: https://docs.sqlc.dev/en/latest/reference/changelog

Defines the database schema for students and their test scores, including tables for student information and their corresponding test results. This schema is used to demonstrate sqlc.embed.

```sql
CREATE TABLE students (
  id   bigserial PRIMARY KEY,
  name text,
  age  integer
)

CREATE TABLE test_scores (
  student_id bigint,
  score integer,
  grade text
)
```

---

### Generated Go Struct for Standard Query

Source: https://docs.sqlc.dev/en/latest/howto/embedding

The Go struct generated by sqlc for the standard query, showing a flattened list of all selected columns. This struct can become unwieldy with many columns or tables.

```Go
type ScoreAndTestsRow struct {
	ID        int64
	Name      string
	Age       int32
	StudentID int64
	Score     int32
	Grade     string
}

```

---

### Go Struct for tern Comment Table

Source: https://docs.sqlc.dev/en/latest/howto/ddl

A Go struct generated by sqlc for the 'comment' table defined in a tern migration script. It maps 'id' and 'text' columns to Go types.

```go
package db

type Comment struct {
	ID   int32
	Text string
}

```

---

### sqlc.arg: Name SQL Parameters

Source: https://docs.sqlc.dev/en/latest/_sources/reference/macros

The `sqlc.arg` macro attaches a name to a parameter in a SQL query, expanding to an engine-specific placeholder. This is useful for improving code readability and maintainability by clearly associating query parameters with their intended use.

```sql
-- name: GetAuthorByName :one
SELECT *
FROM authors
WHERE lower(name) = sqlc.arg(name);

-- >>> EXPANDS TO >>>

-- name: GetAuthorByName :one
SELECT *
FROM authors
WHERE lower(name) = ?;

```

---

### Generated Go Struct for Modified Schema

Source: https://docs.sqlc.dev/en/latest/_sources/howto/ddl

A Go struct generated by sqlc based on the modified database schema, specifically for a table renamed to 'writers'.

```go
package db

type Writer struct {
	ID  int
	Bio string
}
```

---

### Escape reserved keywords in Go

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Ensures that Go keywords used as identifiers in generated code are properly escaped to prevent syntax errors.

```go
// Example of escaping a reserved keyword like 'type'
// func GenerateUserStruct(user User) string {
// 	return fmt.Sprintf("type UserType struct {\n\tName string `json:\"name\"`\n\tType string `json:\"type\"`\n}\n", user.Name, user.Type)
// }

```

---

### Go Struct for Flattened Query Results

Source: https://docs.sqlc.dev/en/latest/_sources/howto/embedding

The Go struct generated by sqlc for the flattened query results, representing all selected columns as individual fields.

```go
type ScoreAndTestsRow struct {
	ID        int64
	Name      string
	Age       int32
	StudentID int64
	Score     int32
	Grade     string
}
```

---

### Map Nullable TEXT to Go \*string

Source: https://docs.sqlc.dev/en/latest/reference/datatypes

Nullable TEXT columns in PostgreSQL are mapped to pgtype.Text with pgx/v5. To map them to \*string, use the emit_pointers_for_null_types option or override the datatype with pointer: true.

```go
// Configuration using emit_pointers_for_null_types:
// {
//   "emit_pointers_for_null_types": true
// }

// Configuration using override:
// {
//   "overrides": [
//     {
//       "column": "*.text_column",
//       "go_type": {
//         "type": "*string"
//       }
//     }
//   ]
// }
```

---

### Pull in array information from analyzer

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Improves the compiler by incorporating array type information gathered during the analysis phase.

```go
-- Example SQL with array types (PostgreSQL)
/*
CREATE TABLE items (
  id serial primary key,
  tags text[]
);

INSERT INTO items (tags) VALUES (ARRAY['tag1', 'tag2']);
*/

```

---

### sqlc.slice: Dynamic Parameter Slicing for IN Clauses

Source: https://docs.sqlc.dev/en/latest/_sources/reference/macros

The `sqlc.slice` macro is designed for database drivers that do not natively support passing slices to SQL `IN` operators. It generates a dynamic query at runtime, adapting to the correct number of parameters required for the `IN` clause.

```sql
/* name: SelectStudents :many */
SELECT * FROM students
WHERE age IN (sqlc.slice("ages"))

-- >>> EXPANDS TO >>>

/* name: SelectStudents :many */
SELECT id, name, age FROM authors
WHERE age IN (/*SLICE:ages*/?)

```

---

### sqlc.narg: Mark SQL Parameters as Nullable

Source: https://docs.sqlc.dev/en/latest/_sources/reference/macros

The `sqlc.narg` macro functions identically to `sqlc.arg` but explicitly marks the parameter as nullable. This ensures that the generated code correctly handles potential null values for the associated parameter.

```sql
-- name: GetAuthorByName :one
SELECT *
FROM authors
WHERE lower(name) = sqlc.narg(name);

-- >>> EXPANDS TO >>>

-- name: GetAuthorByName :one
SELECT *
FROM authors
WHERE LOWER(name) = ?;

```

---

### Error on unexpanded star expression

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Introduces error handling for star expressions (`*`) that are not properly expanded in queries, ensuring all selected columns are explicitly accounted for.

```go
-- Example of an unexpanded star expression (if not handled)
/*
SELECT *
FROM users;
-- This might cause an error if the analyzer expects explicit column names.
*/

```

---

### Define Table and Delete Author SQL (MySQL/SQLite)

Source: https://docs.sqlc.dev/en/latest/_sources/howto/delete

Defines the 'authors' table and a SQL statement to delete an author by ID using MySQL and SQLite's '?' parameter syntax.

```SQL
CREATE TABLE authors (
  id         SERIAL PRIMARY KEY,
  bio        text   NOT NULL
);

-- name: DeleteAuthor :exec
DELETE FROM authors WHERE id = ?;
```

---

### Custom Rule Definition

Source: https://docs.sqlc.dev/en/latest/_sources/reference/config

Enables the creation of custom validation rules using the Common Expression Language (CEL) to enforce specific constraints on SQL queries.

````APIDOC
## Custom Rule Definition

### Description
Define custom validation rules for your SQL code using the Common Expression Language (CEL).

### Structure
Each rule definition is a mapping with the following keys:

- `name` (string): The unique name for this rule. Required.
- `rule` (string): A CEL expression that defines the validation logic. Required.
- `message` (string): An optional message to display when the rule evaluates to `true` (i.e., when the condition is met and potentially indicates an issue).

### Example Usage
```yaml
version: "2"
rules:
- name: "no-pg"
  message: "invalid engine: postgresql"
  rule: |
    config.engine == "postgresql"
- name: "no-delete"
  message: "don't use delete statements"
  rule: |
    query.sql.contains("DELETE")
````

Refer to the [vet documentation](../howto/vet.md) for a list of built-in rules and guidance on writing custom rules.

````

--------------------------------

### Add, drop, rename, and change `ENUM` values in MySQL

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Extends MySQL support to include operations for adding, dropping, renaming, and changing `ENUM` values within table definitions.

```go
-- Example of modifying ENUM values in MySQL
/*
-- Add a new ENUM value
ALTER TABLE tasks
MODIFY COLUMN priority ENUM('low', 'medium', 'high', 'urgent');

-- Drop an ENUM value (requires recreating the column definition)
-- ALTER TABLE tasks
-- MODIFY COLUMN priority ENUM('low', 'medium', 'urgent');
*/

````

---

### Goose SQL Migration Script

Source: https://docs.sqlc.dev/en/latest/howto/ddl

A Goose migration script with '-- +goose Up' and '-- +goose Down' directives. The 'Up' section creates a 'post' table, which sqlc parses.

```sql
-- +goose Up
CREATE TABLE post (
    id    int NOT NULL,
    title text,
    body  text,
    PRIMARY KEY(id)
);

-- +goose Down
DROP TABLE post;

```

---

### CAST support for MySQL (Go)

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Presents the Go code generated for a MySQL query involving a `CAST` operation. The function `SelectColumnCast` is defined to return a slice of `int64`.

```go
package querytest

import (
	"context"
)

const selectColumnCast = `-- name: SelectColumnCast :many
SELECT CAST(bar AS BIGINT) FROM foo
`

func (q *Queries) SelectColumnCast(ctx context.Context) ([]int64, error) {
  ...
}
```

---

### SQLC Query Annotations - :execlastid

Source: https://docs.sqlc.dev/en/latest/reference/query-annotations

The :execlastid annotation generates a Go function that returns the last inserted ID from ExecContext.

````APIDOC
## DELETE /api/users/{id}

### Description
Deletes a user specified by their unique identifier.

### Method
DELETE

### Endpoint
/api/users/{id}

### Parameters
#### Path Parameters
- **id** (string) - Required - The unique identifier of the user to delete.

### Response
#### Success Response (200)
- **message** (string) - A confirmation message indicating the user was deleted.

#### Response Example
```json
{
  "message": "User with ID 123e4567-e89b-12d3-a456-426614174000 deleted successfully."
}
````

````

--------------------------------

### SQL Schema for Records Table

Source: https://docs.sqlc.dev/en/latest/howto/transactions

Defines the schema for the 'records' table, including an auto-incrementing primary key 'id' and an integer 'counter' field.

```sql
CREATE TABLE records (
  id SERIAL PRIMARY KEY,
  counter INT NOT NULL
);

````

---

### SQL: Delete Author Query Annotation

Source: https://docs.sqlc.dev/en/latest/reference/query-annotations

Defines a SQL query for deleting an author by ID and annotates it with ':exec' for execution.

```sql
-- name: DeleteAuthor :exec
DELETE FROM authors
WHERE id = $1;
```

---

### Define Table and Delete Author SQL (PostgreSQL)

Source: https://docs.sqlc.dev/en/latest/_sources/howto/delete

Defines the 'authors' table and a SQL statement to delete an author by ID using PostgreSQL's '$1' parameter syntax.

```SQL
CREATE TABLE authors (
  id         SERIAL PRIMARY KEY,
  bio        text   NOT NULL
);

-- name: DeleteAuthor :exec
DELETE FROM authors WHERE id = $1;
```

---

### SQL: Count all rows and count rows by town

Source: https://docs.sqlc.dev/en/latest/howto/query_count

Defines SQL queries to count the total number of rows in the 'authors' table and to count authors grouped by their hometown. These queries are intended to be used with a SQL code generator like sqlc.

```sql
CREATE TABLE authors (
  id       SERIAL PRIMARY KEY,
  hometown text   NOT NULL
);

-- name: CountAuthors :one
SELECT count(*) FROM authors;

-- name: CountAuthorsByTown :many
SELECT hometown, count(*) FROM authors
GROUP BY 1
ORDER BY 1;

```

---

### Go Struct for Authors Table

Source: https://docs.sqlc.dev/en/latest/howto/ddl

A Go struct generated by sqlc based on a modified 'authors' table schema, renamed to 'writers'. It maps SQL columns to Go types.

```go
package db

type Writer struct {
	ID  int
	Bio string
}

```

---

### Go Struct for Standard High Score Query

Source: https://docs.sqlc.dev/en/latest/reference/changelog

The Go struct generated by sqlc for the standard `HighScore` query. It represents a flattened list of columns from the 'students' table and the calculated 'high_score'.

```go
type HighScoreRow struct {
	ID        int64
	Name      sql.NullString
	Age       sql.NullInt32
	HighScore int32
}
```

---

### PostgreSQL Enums to Aliased String Types

Source: https://docs.sqlc.dev/en/latest/_sources/reference/datatypes

Shows how PostgreSQL ENUM types are mapped to aliased Go string types, enabling type-safe enum usage.

```sql
CREATE TYPE status AS ENUM (
  'open',
  'closed'
);

CREATE TABLE stores (
  name   text    PRIMARY KEY,
  status status  NOT NULL
);
```

```go
package db

type Status string

const (
	StatusOpen   Status = "open"
	StatusClosed Status = "closed"
)

type Store struct {
	Name   string
	Status Status
}
```

---

### Embedded SQL Query for Students and Scores

Source: https://docs.sqlc.dev/en/latest/howto/embedding

An SQL query utilizing sqlc's 'embed' function to select and group columns into their respective model structs ('students' and 'test_scores'). This promotes cleaner data representation.

```SQL
-- name: ScoreAndTests :many
SELECT sqlc.embed(students), sqlc.embed(test_scores)
FROM students
JOIN test_scores ON test_scores.student_id = students.id
WHERE students.id = $1;

```

---

### Generated Go Struct for Embedded Query

Source: https://docs.sqlc.dev/en/latest/howto/embedding

The Go struct generated by sqlc for the embedded query. It contains nested structs ('Student' and 'TestScore') representing the embedded models, leading to more organized data.

```Go
type ScoreAndTestsRow struct {
	Student   Student
	TestScore TestScore
}

```

---

### Configure SQLC with go-geom for PostGIS Geometry Types

Source: https://docs.sqlc.dev/en/latest/_sources/reference/datatypes

This snippet demonstrates configuring SQLC to use the github.com/twpayne/go-geom package for PostGIS geometry types. It involves overriding the 'geometry' database type to map to 'go-geom.MultiPolygon' in Go, handling both nullable and non-nullable cases.

```SQL
-- Multipolygons in British National Grid (epsg:27700)
create table shapes(
  id serial,
  name varchar,
  geom geometry(Multipolygon, 27700)
);

-- name: GetShapes :many
SELECT * FROM shapes;
```

```JSON
{
  "version": "1",
  "packages": [
    {
      "path": "db",
      "engine": "postgresql",
      "schema": "query.sql",
      "queries": "query.sql"
    }
  ],
  "overrides": [
    {
      "db_type": "geometry",
      "go_type": "github.com/twpayne/go-geom.MultiPolygon"
    },
    {
      "db_type": "geometry",
      "go_type": "github.com/twpayne/go-geom.MultiPolygon",
      "nullable": true
    }
  ]
}
```

---

### Remove `ROLLBACK` statements from DDL

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Cleans up the `vet` component by removing `ROLLBACK` statements from Data Definition Language (DDL) operations, as they are typically not applicable in DDL contexts.

```go
-- Example DDL statement where ROLLBACK is not needed
/*
CREATE TABLE my_table (
  id serial primary key,
  name varchar(50)
);
-- DROP TABLE my_table;
*/

```

---

### Go Struct for sql-migrate People Table

Source: https://docs.sqlc.dev/en/latest/howto/ddl

A Go struct generated by sqlc for the 'people' table defined in an sql-migrate script. It maps the 'id' column to an int32.

```go
package db

type People struct {
	ID int32
}

```

---

### Remove temporary pool config

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Cleans up the PostgreSQL engine by removing temporary pool configuration settings that are no longer necessary.

```go
// Conceptual removal of temporary pool config
// package postgresql
//
// func NewDatabase() (*sql.DB, error) {
// 	// ...
// 	// Removed temporary pool settings
// 	// db.SetMaxIdleConns(10)
// 	// db.SetConnMaxLifetime(time.Hour)
// 	// ...
// 	return db, nil
// }

```

---

### Disable Remote Code Generation Locally

Source: https://docs.sqlc.dev/en/latest/reference/changelog

Disables remote code generation and forces local generation when the 'cloud' configuration block is present in sqlc.json. This is done by passing the `--no-remote` flag to the `sqlc generate` command.

```shell
sqlc generate --no-remote
```

---

### SQL Schema Alteration

Source: https://docs.sqlc.dev/en/latest/_sources/howto/verify

Alters the 'users' table by adding a new 'created_at' timestamp column. This schema change can introduce ambiguity in existing queries.

```sql
ALTER TABLE users ADD COLUMN created_at TIMESTAMP;
```

---

### SQL Query with Embedded Structs

Source: https://docs.sqlc.dev/en/latest/_sources/howto/embedding

An SQL query using sqlc's embed function to select student and test score data as separate embedded structs. This improves code organization and reduces manual mapping.

```sql
-- name: ScoreAndTests :many
SELECT sqlc.embed(students), sqlc.embed(test_scores)
FROM students
JOIN test_scores ON test_scores.student_id = students.id
WHERE students.id = $1;
```

---

### MySQL CAST Support Go Function

Source: https://docs.sqlc.dev/en/latest/reference/changelog

Generated Go function to execute MySQL queries with CAST operations. The function selects a boolean column and casts it to a BIGINT.

```go
package querytest

import (
	"context"
)

const selectColumnCast = `-- name: SelectColumnCast :many
SELECT CAST(bar AS BIGINT) FROM foo
`

func (q *Queries) SelectColumnCast(ctx context.Context) ([]int64, error) {
  // ... implementation details ...
}

```

---

### SQLC Configuration: Override UUID and Birthday column types

Source: https://docs.sqlc.dev/en/latest/howto/overrides

This configuration demonstrates how to override the default Go type for a PostgreSQL UUID type to use `github.com/google/uuid` and how to override the Go type for a specific column `users.birthday` to `time.Time`. It specifies the schema, queries, engine, and Go generation options.

```yaml
version: "2"sql:
- schema: "postgresql/schema.sql"
  queries: "postgresql/query.sql"
  engine: "postgresql"
  gen:
    go:
      package: "authors"
      out: "db"
      sql_package: "pgx/v5"
      overrides:
        - db_type: "uuid"
          nullable: true
          go_type:
            import: "github.com/google/uuid"
            type: "UUID"
        - column: "users.birthday"
          go_type: "time.Time"

```

---

### Customizing Field Names with 'rename' in sqlc Config

Source: https://docs.sqlc.dev/en/latest/howto/rename

Shows how to use the 'rename' mapping within the sqlc configuration file to specify custom struct field names for specific columns. This overrides the default naming.

```yaml
version: "2"
sql:
  - schema: "postgresql/schema.sql"
    queries: "postgresql/query.sql"
    engine: "postgresql"
    gen:
      go:
        package: "authors"
        out: "postgresql"
        rename:
          spotify_url: "SpotifyURL"
```

---

### Delete All Authors using :execrows

Source: https://docs.sqlc.dev/en/latest/_sources/reference/query-annotations

Generates a Go method that returns the number of affected rows from ExecContext. Specifically designed for queries where you only need the count of modified rows.

```sql
-- name: DeleteAllAuthors :execrows
DELETE FROM authors;
```

```go
func (q *Queries) DeleteAllAuthors(ctx context.Context) (int64, error) {
	_, err := q.db.ExecContext(ctx, deleteAllAuthors)
	// ...
}
```

---

### Create authors table

Source: https://docs.sqlc.dev/en/latest/_sources/howto/update

Defines the 'authors' table with an auto-incrementing 'id' and a 'bio' text field. This is a prerequisite for the update operations.

```sql
CREATE TABLE authors (
  id         SERIAL PRIMARY KEY,
  bio        text   NOT NULL
);
```

---

### Previous pgx Batch Closed Error Check

Source: https://docs.sqlc.dev/en/latest/reference/changelog

Illustrates the older method of checking if a pgx batch was closed by directly comparing the error message string. This is less robust than using `errors.Is`.

```go
err.Error() == "batch already closed"
```

---

### SQL Query for Scores and Tests (Flattened)

Source: https://docs.sqlc.dev/en/latest/_sources/howto/embedding

A standard SQL query to select student and test score data, joined by student ID. This query is typically used without embedding.

```sql
-- name: ScoreAndTests :many
SELECT students.*, test_scores.*
FROM students
JOIN test_scores ON test_scores.student_id = students.id
WHERE students.id = $1;
```

---

### Go: Delete All Authors Affected Rows

Source: https://docs.sqlc.dev/en/latest/reference/query-annotations

Go function to execute a delete all authors operation and return the count of affected rows.

```go
func (q *Queries) DeleteAllAuthors(ctx context.Context) (int64, error) {
	_, err := q.db.ExecContext(ctx, deleteAllAuthors)
	// ...
}
```

---

### Go Struct for Embedded Query Results

Source: https://docs.sqlc.dev/en/latest/_sources/howto/embedding

The Go struct generated by sqlc for query results using the embed function. It contains nested structs for each embedded table, improving data structure.

```go
type ScoreAndTestsRow struct {
	Student   Student
	TestScore TestScore
}
```

---

### SQL: Delete All Authors Query Annotation

Source: https://docs.sqlc.dev/en/latest/reference/query-annotations

Annotates a SQL query to delete all authors, returning the execution result.

```sql
-- name: DeleteAllAuthors :execresult
DELETE FROM authors;
```

---

### Configure Global Go Type Override for PostgreSQL timestamptz (sqlc v2)

Source: https://docs.sqlc.dev/en/latest/_sources/howto/overrides

This configuration globally overrides the Go type for PostgreSQL's 'timestamptz' to use 'null.Time' from the 'gopkg.in/guregu/null.v4' package. It's useful for handling nullable timestamp with time zone columns consistently across multiple generated Go packages. The 'engine' field is used to specify the database engine, particularly when multiple engines are configured.

```yaml
version: "2"
overrides:
  go:
    overrides:
      - db_type: "pg_catalog.timestamptz"
        nullable: true
        engine: "postgresql"
        go_type:
          import: "gopkg.in/guregu/null.v4"
          package: "null"
          type: "Time"
sql:
  - schema: "service1/schema.sql"
    queries: "service1/query.sql"
    engine: "postgresql"
    gen:
      go:
        package: "service1"
        out: "service1"
  - schema: "service2/schema.sql"
    queries: "service2/query.sql"
    engine: "postgresql"
    gen:
      go:
        package: "service2"
        out: "service2"
```

---

### Go Struct for Embedded High Score Query

Source: https://docs.sqlc.dev/en/latest/reference/changelog

The Go struct generated by sqlc for the `HighScoreEmbed` query. It includes a nested 'Student' struct, which represents the embedded 'students' table row, and the 'high_score'.

```go
type HighScoreRow struct {
	Student   Student
	HighScore int32
}
```

---

### Support `ENUM` in `ALTER` definition for MySQL

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Adds support for `ENUM` data types within `ALTER TABLE` statements for MySQL, allowing schema modifications involving enums.

```go
-- Example ALTER TABLE with ENUM in MySQL
/*
ALTER TABLE products
MODIFY COLUMN status ENUM('pending', 'processing', 'shipped') DEFAULT 'pending';
*/

```

---

### Overriding UUID Type Mapping in MySQL

Source: https://docs.sqlc.dev/en/latest/_sources/reference/datatypes

Provides a JSON configuration to override the default mapping for UUID columns in MySQL, converting BINARY(16) to `uuid.UUID`.

```json
{
  "overrides": [
    {
      "column": "*.uuid",
      "go_type": "github.com/google/uuid.UUID"
    }
  ]
}
```

---

### SQL: Insert Author Last ID Annotation

Source: https://docs.sqlc.dev/en/latest/reference/query-annotations

Annotates an SQL INSERT query to return the last inserted ID.

```sql
-- name: InsertAuthor :execlastid
INSERT INTO authors (name) VALUES (?);
```

---

### SQL: Delete All Authors Affected Rows Annotation

Source: https://docs.sqlc.dev/en/latest/reference/query-annotations

Annotates a SQL query to delete all authors, returning the number of affected rows.

```sql
-- name: DeleteAllAuthors :execrows
DELETE FROM authors;
```

---

### Delete author by ID (MySQL and SQLite)

Source: https://docs.sqlc.dev/en/latest/howto/delete

SQL statement for MySQL and SQLite to delete an author from the 'authors' table based on their ID. It uses the '?' placeholder for the ID parameter.

```sql
-- name: DeleteAuthor :exec
DELETE FROM authors WHERE id = ?;
```

---

### Delete author by ID (PostgreSQL)

Source: https://docs.sqlc.dev/en/latest/howto/delete

SQL statement for PostgreSQL to delete an author from the 'authors' table based on their ID. It uses the '$1' placeholder for the ID parameter.

```sql
-- name: DeleteAuthor :exec
DELETE FROM authors WHERE id = $1;
```

---

### Prevent filtering of embedded struct fields in Go

Source: https://docs.sqlc.dev/en/latest/_sources/reference/changelog

Corrects the Go code generation logic to prevent the accidental filtering of fields from embedded structs.

```go
// Conceptual example with embedded structs
// type Base struct {
// 	ID int
// }
//
// type User struct {
// 	Base
// 	Name string
// }
//
// // Generated code should include both ID and Name fields for User.

```

=== COMPLETE CONTENT === This response contains all available snippets from this library. No additional content exists. Do not make further requests.
